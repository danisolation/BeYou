# Phase 21 Plan: v1.4 privacy data contracts, authorization resources, policy helpers, and audit safeguards

**Created:** 2026-05-22  
**Status:** Complete

## Goal

Create the backend foundation required by v1.4 privacy-control features without exposing new raw sensitive data or adding external notification delivery.

## Tasks

- [x] Add SQLAlchemy models and Alembic migration for:
  - student notification preferences;
  - in-app mood reminder state;
  - student-controlled mood-note share grants;
  - school privacy policy defaults.
- [x] Add Pydantic schemas and service helpers for:
  - in-app-only channel boundaries;
  - allowed controlled reason codes;
  - default school policy response;
  - default student preference response;
  - external channel rejection.
- [x] Extend authorization resource recognition for v1.4 resources while preserving deny-by-default behavior.
- [x] Extend audit and operations sanitizers for new sensitive metadata keys.
- [x] Add targeted tests for schema/model registration, default contracts, external channel rejection, authorization, and audit safeguards.

## Verification

- `python -m alembic upgrade head`
- `python -m pytest tests\test_schema_models.py tests\test_phase21_privacy_controls.py -q`

## Risk Controls

- No external notification provider or worker added.
- No API route exposes private note text in this phase.
- Admin policy can be managed only by admin operations permission.
- Adult `shared_mood_note` permission is relationship-bound; later phase services must additionally require active student share grants.

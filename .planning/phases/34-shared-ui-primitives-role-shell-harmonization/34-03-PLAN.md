---
phase: 34-shared-ui-primitives-role-shell-harmonization
plan: 03
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - frontend/components/adult-student-list.tsx
  - frontend/app/(authenticated)/teacher/page.tsx
  - frontend/app/(authenticated)/parent/page.tsx
  - frontend/tests/phase34-adult-shared-presentation.test.tsx
autonomous: true
requirements:
  - UIC-02
  - UIC-03
  - UIC-04
  - ROLE-05
must_haves:
  truths:
    - "Parent no longer imports shared presentation from the Teacher route page."
    - "Teacher and Parent keep separate role-specific data fetching."
    - "Adult pages preserve support-not-surveillance, Parent read-only, Teacher SOS handling, and no raw private content copy."
    - "Shared adult presentation lives under neutral `frontend/components/`."
  artifacts:
    - path: "frontend/components/adult-student-list.tsx"
      provides: "Neutral AdultStudentList presentation for Teacher/Parent dashboards"
      exports: ["AdultStudentList"]
    - path: "frontend/app/(authenticated)/teacher/page.tsx"
      provides: "Teacher data owner using neutral adult presentation"
    - path: "frontend/app/(authenticated)/parent/page.tsx"
      provides: "Parent data owner using neutral adult presentation"
    - path: "frontend/tests/phase34-adult-shared-presentation.test.tsx"
      provides: "No cross-role import and adult boundary tests"
  key_links:
    - from: "parent/page.tsx"
      to: "adult-student-list.tsx"
      via: "neutral component import"
      pattern: "@/components/adult-student-list"
    - from: "teacher/page.tsx"
      to: "apiFetch('/api/teacher/students')"
      via: "route-owned data fetching"
      pattern: "/api/teacher/students"
    - from: "parent/page.tsx"
      to: "apiFetch('/api/parent/students')"
      via: "route-owned data fetching"
      pattern: "/api/parent/students"
---

<objective>
Move shared Teacher/Parent adult dashboard presentation to a neutral component while preserving role-specific data and privacy boundaries.

Purpose: Remove cross-role route imports and make adult support guidance reusable without weakening SOS-only/summary-only boundaries.

Output: `AdultStudentList`, updated Teacher/Parent pages, and regression tests.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-CONTEXT.md
@.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-UI-SPEC.md
@frontend/app/(authenticated)/teacher/page.tsx
@frontend/app/(authenticated)/parent/page.tsx
@frontend/components/ui-primitives.tsx
@frontend/components/demo-badge.tsx
@frontend/components/demo-guide-card.tsx
@frontend/tests/role-dashboards.test.tsx

<interfaces>
Existing route-local type:
`LinkedStudent = { id; full_name; email; school; class_name; relationship_type; link_status; is_demo }`

Existing external types:
`AdultSupportOverviewItem`, `InAppNotification`, `sosStatusLabels` from `@/lib/sos-api`.

Target neutral export:
`AdultStudentList(props)` with:
- `roleContext: "teacher" | "parent"`
- `title`, `subtitle`, `summaryTitle`, `summaryBasePath`, `summaryCta`, `sosBasePath`, `sosCta`
- `students`, `emptyBody`, `supportOverview`, `notifications`
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add adult shared presentation tests</name>
  <read_first>
    frontend/app/(authenticated)/teacher/page.tsx;
    frontend/app/(authenticated)/parent/page.tsx;
    frontend/tests/role-dashboards.test.tsx;
    .planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-UI-SPEC.md
  </read_first>
  <files>frontend/tests/phase34-adult-shared-presentation.test.tsx</files>
  <behavior>
    - Static source check: `parent/page.tsx` must not import `@/app/(authenticated)/teacher/page`.
    - Teacher page still fetches `/api/teacher/students` and `getTeacherSupportOverview`.
    - Parent page still fetches `/api/parent/students` and `getParentSupportOverview`.
    - Teacher renders `Ranh giới hỗ trợ của giáo viên` and Teacher SOS handling CTA.
    - Parent renders `Ranh giới hỗ trợ của phụ huynh` and Parent read-only/SOS status copy.
    - Adult rendered text excludes raw private content markers: `transcript`, `self_check_answer`, `scenario_answer`, `raw_claims`, `access_token`, `risk leaderboard`, `xếp hạng nguy cơ`.
  </behavior>
  <action>
    Create `frontend/tests/phase34-adult-shared-presentation.test.tsx`. Reuse fetch mocking patterns from `role-dashboards.test.tsx`. Include source checks using `readFileSync` for Parent/Teacher imports and route-owned fetching. Render TeacherDashboardPage and ParentDashboardPage with linked student mocks and assert role-specific Vietnamese boundary copy remains distinct.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-adult-shared-presentation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains exact forbidden import string `@/app/(authenticated)/teacher/page`.
    - Test file asserts distinct strings `Ranh giới hỗ trợ của giáo viên` and `Ranh giới hỗ trợ của phụ huynh`.
    - Test file asserts `/api/teacher/students` and `/api/parent/students` remain in route files.
  </acceptance_criteria>
  <done>Tests define neutral adult presentation and no cross-role import contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract AdultStudentList to neutral component</name>
  <read_first>
    frontend/app/(authenticated)/teacher/page.tsx;
    frontend/app/(authenticated)/parent/page.tsx;
    frontend/components/ui-primitives.tsx;
    frontend/tests/phase34-adult-shared-presentation.test.tsx
  </read_first>
  <files>frontend/components/adult-student-list.tsx, frontend/app/(authenticated)/teacher/page.tsx, frontend/app/(authenticated)/parent/page.tsx</files>
  <action>
    Move `RoleStudentList`, `PrivacyBoundaryCard`, `NotificationList`, and `SupportOverviewCard` presentation from `teacher/page.tsx` into `frontend/components/adult-student-list.tsx` as exported `AdultStudentList`. Keep `apiFetch`, `getTeacherSupportOverview`, and `getParentSupportOverview` only in route pages. Replace Parent import from `@/app/(authenticated)/teacher/page` with `@/components/adult-student-list`. Use `PageHeader`, `SurfaceCard`, `EntryCard`, `StatusBadge`, `LoadingState`, and `EmptyState` where appropriate. Preserve exact role-specific adult privacy copy from current Teacher/Parent pages, including no raw self-check/chat content at adult portals and support-not-surveillance sentence. For SOS-related states, use red/danger only where true SOS/high-risk semantics apply; do not make SOS neutral.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-adult-shared-presentation.test.tsx tests/role-dashboards.test.tsx tests/phase20-responsive-smoke-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/adult-student-list.tsx` exports `AdultStudentList`.
    - `frontend/app/(authenticated)/parent/page.tsx` contains `@/components/adult-student-list` and does not contain `@/app/(authenticated)/teacher/page`.
    - `teacher/page.tsx` contains `/api/teacher/students`; `parent/page.tsx` contains `/api/parent/students`.
    - Adult shared component does not import `@/app/`, `@/lib/api`, or `@/lib/auth`.
  </acceptance_criteria>
  <done>Teacher/Parent shared presentation is neutral and data fetching remains role-owned.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Teacher route → neutral adult component | Teacher passes linked-student summary data and Teacher-specific copy. |
| Parent route → neutral adult component | Parent passes linked-student summary data and Parent read-only copy. |
| adult component → DOM | Shared presentation must not expose raw student private content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-09 | Information Disclosure | `adult-student-list.tsx` | mitigate | Neutral component accepts already-authorized summaries; no route/API/auth imports; raw content redline tests. |
| T-34-10 | Elevation of Privilege | Parent/Teacher pages | mitigate | Keep separate route-owned API calls and support overview functions. |
| T-34-11 | Information Disclosure | Parent dashboard | mitigate | Static test blocks Parent importing from Teacher route page. |
| T-34-12 | Integrity/Safety | SOS status display | mitigate | Preserve SOS CTA/copy and red semantics; no neutralization of SOS/high-risk states. |
</threat_model>

<verification>
Run:
`npm --prefix frontend run test -- tests/phase34-adult-shared-presentation.test.tsx tests/role-dashboards.test.tsx tests/phase20-responsive-smoke-ui.test.tsx`
</verification>

<success_criteria>
Parent no longer imports Teacher route presentation; Teacher/Parent retain role-specific data fetching and adult privacy boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-03-SUMMARY.md`.
</output>

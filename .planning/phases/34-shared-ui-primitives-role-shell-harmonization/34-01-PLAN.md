---
phase: 34-shared-ui-primitives-role-shell-harmonization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/ui-primitives.tsx
  - frontend/components/empty-state.tsx
  - frontend/components/demo-guide-card.tsx
  - frontend/tests/phase34-ui-primitives.test.tsx
autonomous: true
requirements:
  - UIC-02
  - UIC-03
  - UIC-04
must_haves:
  truths:
    - "Builders can import shared presentation-only Peerlight AI primitives from neutral component paths."
    - "Loading and error states announce status through accessible roles."
    - "Empty states remain Vietnamese-first and route-copy driven."
    - "SOS/high-risk badge tone remains visually red and distinct from neutral/safe tones."
  artifacts:
    - path: "frontend/components/ui-primitives.tsx"
      provides: "PageHeader, Section, SurfaceCard, EntryCard, StatusBadge, ResponsiveTable, LoadingState, ErrorState, PrivacyBoundaryCard"
      exports: ["PageHeader", "Section", "SurfaceCard", "EntryCard", "StatusBadge", "ResponsiveTable", "LoadingState", "ErrorState", "PrivacyBoundaryCard"]
    - path: "frontend/components/empty-state.tsx"
      provides: "Enhanced EmptyState with action slot and accessible presentation"
    - path: "frontend/tests/phase34-ui-primitives.test.tsx"
      provides: "Primitive semantic and static import regression coverage"
  key_links:
    - from: "frontend/components/ui-primitives.tsx"
      to: "route pages"
      via: "presentation-only exported components"
      pattern: "export function (PageHeader|SurfaceCard|EntryCard|StatusBadge|ResponsiveTable|LoadingState|ErrorState|PrivacyBoundaryCard)"
    - from: "frontend/components/ui-primitives.tsx"
      to: "accessibility contract"
      via: "role=status and role=alert"
      pattern: "role=\"(status|alert)\""
---

<objective>
Create the shared presentation-only primitive foundation for Phase 34.

Purpose: Reduce duplicated UI rhythm while preserving role-specific privacy meaning and avoiding route/API/auth/business-logic imports.

Output: Neutral primitives, enhanced EmptyState, aligned DemoGuideCard styling, and primitive tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-CONTEXT.md
@.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-RESEARCH.md
@.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-UI-SPEC.md
@frontend/components/empty-state.tsx
@frontend/components/demo-guide-card.tsx
@frontend/tests/role-dashboards.test.tsx

<interfaces>
Existing:
- `EmptyState({ heading?: string, body?: string })`
- `DemoGuideCard({ title, body, steps, actions })`

Create:
- `PageHeader`
- `Section`
- `SurfaceCard`
- `EntryCard`
- `StatusBadge`
- `ResponsiveTable`
- `LoadingState`
- `ErrorState`
- `PrivacyBoundaryCard`
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add primitive tests first</name>
  <read_first>
    .planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-CONTEXT.md;
    .planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-UI-SPEC.md;
    frontend/components/empty-state.tsx;
    frontend/components/demo-guide-card.tsx;
    frontend/tests/role-dashboards.test.tsx
  </read_first>
  <files>frontend/tests/phase34-ui-primitives.test.tsx</files>
  <behavior>
    - LoadingState renders `Đang tải thông tin...` with `role="status"`.
    - ErrorState renders safe Vietnamese recovery copy with `role="alert"`.
    - StatusBadge `tone="sos"` or `tone="danger"` includes red styling, not neutral/secondary-only styling.
    - ResponsiveTable keeps child `<table>` semantics and wraps with `overflow-x-auto`.
    - Static source check confirms `ui-primitives.tsx` does not import `@/app/`, `@/lib/api`, `@/lib/auth`, or `@/lib/sos-api`.
  </behavior>
  <action>
    Create `frontend/tests/phase34-ui-primitives.test.tsx`. Use React Testing Library for render assertions and `readFileSync(join(process.cwd(), "components/ui-primitives.tsx"), "utf8")` for static import checks. Include requirement trace comments for UIC-02/UIC-04. Do not include raw student emails, IDs, notes, transcripts, answers, provider claims, secrets, browser tokens, exports, risk leaderboards, or destructive reset labels.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-ui-primitives.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains exact strings: `role="status"`, `role="alert"`, `overflow-x-auto`, `tone="sos"`.
    - Test file asserts forbidden imports are absent: `@/app/`, `@/lib/api`, `@/lib/auth`, `@/lib/sos-api`.
  </acceptance_criteria>
  <done>Primitive contract tests exist and fail until primitives are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement presentation-only primitives</name>
  <read_first>
    frontend/tests/phase34-ui-primitives.test.tsx;
    .planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-UI-SPEC.md;
    frontend/components/empty-state.tsx
  </read_first>
  <files>frontend/components/ui-primitives.tsx, frontend/components/empty-state.tsx</files>
  <action>
    Create `frontend/components/ui-primitives.tsx` exporting `PageHeader`, `Section`, `SurfaceCard`, `EntryCard`, `StatusBadge`, `ResponsiveTable`, `LoadingState`, `ErrorState`, and `PrivacyBoundaryCard`. Use only React/Next `Link` if needed and Tailwind classes from the UI spec: `rounded-3xl`, `bg-white`, `bg-secondary`, `text-display`, `text-heading`, `text-body`, `text-label`, `text-accent`, `min-h-11`. Do not import route pages, API clients, auth helpers, role-specific service modules, or business logic. Enhance `EmptyState` in its existing file to accept optional `children`, `action`, or `className` without breaking existing props. Loading default copy must be `Đang tải thông tin...`; Error default copy must be `Chưa tải được thông tin. Hãy thử lại hoặc quay về cổng phù hợp để tiếp tục an toàn.`. StatusBadge tones must include `safe`, `neutral`, `warning`, `danger`, and `sos`, with `danger`/`sos` using red classes.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-ui-primitives.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/ui-primitives.tsx` exports all named primitives.
    - `LoadingState` includes `role="status"`.
    - `ErrorState` includes `role="alert"`.
    - `ResponsiveTable` includes `overflow-x-auto`.
    - No primitive import matches `@/app/|@/lib/api|@/lib/auth|@/lib/sos-api`.
  </acceptance_criteria>
  <done>Shared primitive foundation passes tests and remains presentation-only.</done>
</task>

<task type="auto">
  <name>Task 3: Align DemoGuideCard to primitive rhythm</name>
  <read_first>
    frontend/components/demo-guide-card.tsx;
    frontend/components/ui-primitives.tsx;
    .planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-CONTEXT.md
  </read_first>
  <files>frontend/components/demo-guide-card.tsx, frontend/tests/phase34-ui-primitives.test.tsx</files>
  <action>
    Refactor `DemoGuideCard` to use `SurfaceCard` or `Section` classes from `ui-primitives.tsx` while preserving the exact public props and existing visible copy behavior. Keep action links with `min-h-11`, primary action accent styling, secondary action border/accent styling, and no new imports from routes/API/auth. Update primitive tests to assert DemoGuideCard still renders `Gợi ý demo` and action links with their hrefs.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-ui-primitives.test.tsx tests/role-dashboards.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `DemoGuideCard` still exports `DemoGuideCard`.
    - Existing tests that query demo guide dashboard copy still pass.
    - Action link class strings still include `min-h-11`.
  </acceptance_criteria>
  <done>DemoGuideCard shares primitive rhythm without behavior or route-data changes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| route pages → shared primitives | Role-specific pages pass copy/data into neutral presentation components. |
| sensitive UI data → DOM | Sensitive role data must not be newly exposed by generic components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-01 | Information Disclosure | `ui-primitives.tsx` | mitigate | Static test blocks route/API/auth/service imports; primitives receive presentation props only. |
| T-34-02 | Information Disclosure | `EmptyState` | mitigate | EmptyState remains route-copy driven and does not add raw data fields. |
| T-34-03 | Integrity/Safety | `StatusBadge` | mitigate | Explicit `sos`/`danger` tones preserve red styling for SOS/high-risk semantics. |
| T-34-04 | Denial of Accessibility | `LoadingState`/`ErrorState` | mitigate | Require `role="status"` and `role="alert"` tests. |
</threat_model>

<verification>
Run:
`npm --prefix frontend run test -- tests/phase34-ui-primitives.test.tsx tests/role-dashboards.test.tsx`
</verification>

<success_criteria>
UIC-02 primitive foundation exists under neutral `frontend/components/`; UIC-03 route-supplied Vietnamese copy remains possible; UIC-04 accessibility/SOS semantics are test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-01-SUMMARY.md`.
</output>

---
phase: 34-shared-ui-primitives-role-shell-harmonization
plan: 04
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
  - 34-03
files_modified:
  - frontend/app/(authenticated)/student/page.tsx
  - frontend/app/(authenticated)/admin/page.tsx
  - frontend/tests/phase34-final-regression.test.tsx
autonomous: true
requirements:
  - UIC-02
  - UIC-03
  - UIC-04
  - ROLE-05
must_haves:
  truths:
    - "Student and Admin representative dashboards use shared primitive rhythm without content redesign."
    - "Student SOS/high-risk visual semantics remain red and distinct."
    - "Admin remains metadata-only and does not gain unsafe controls."
    - "Phase 34 touched surfaces pass role dashboard, responsive, auth/no-token, and privacy redline regressions."
  artifacts:
    - path: "frontend/app/(authenticated)/student/page.tsx"
      provides: "Representative Student primitive adoption preserving privacy/SOS copy"
    - path: "frontend/app/(authenticated)/admin/page.tsx"
      provides: "Representative Admin entry-card primitive adoption preserving metadata-only posture"
    - path: "frontend/tests/phase34-final-regression.test.tsx"
      provides: "Final Phase 34 UI/privacy/static regression gate"
  key_links:
    - from: "student/page.tsx"
      to: "ui-primitives.tsx"
      via: "EntryCard/SurfaceCard/ResponsiveTable/LoadingState"
      pattern: "@/components/ui-primitives"
    - from: "admin/page.tsx"
      to: "ui-primitives.tsx"
      via: "EntryCard/LoadingState"
      pattern: "@/components/ui-primitives"
---

<objective>
Apply narrow representative Student/Admin primitive adoption and close Phase 34 regression coverage.

Purpose: Prove cross-role visual rhythm across all roles without moving Phase 35 dashboard redesign, Phase 36 backend work, or Phase 37 performance/data-loading work into this phase.

Output: Student/Admin low-risk primitive adoption and final targeted regression tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-CONTEXT.md
@.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-UI-SPEC.md
@.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md
@frontend/app/(authenticated)/student/page.tsx
@frontend/app/(authenticated)/admin/page.tsx
@frontend/components/ui-primitives.tsx
@frontend/tests/role-dashboards.test.tsx
@frontend/tests/phase20-responsive-smoke-ui.test.tsx
@frontend/tests/phase32-release-gates-ui.test.tsx

<interfaces>
Use primitives from `frontend/components/ui-primitives.tsx`:
`PageHeader`, `SurfaceCard`, `EntryCard`, `ResponsiveTable`, `LoadingState`, `StatusBadge`.

Do not change:
- Student data fetch calls: `/api/student/profile`, SOS, mood reminder.
- Admin data fetch calls: `/api/admin/users`, `/api/admin/links`.
- SOS confirmation copy: `Xác nhận gửi tín hiệu hỗ trợ` / `Ở lại trang này`.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add final Phase 34 regression gate</name>
  <read_first>
    frontend/app/(authenticated)/student/page.tsx;
    frontend/app/(authenticated)/admin/page.tsx;
    frontend/tests/phase32-release-gates-ui.test.tsx;
    .planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-UI-SPEC.md
  </read_first>
  <files>frontend/tests/phase34-final-regression.test.tsx</files>
  <behavior>
    - Student loading state uses accessible `role="status"`.
    - Student keeps privacy link `Ai có thể xem thông tin của em?`.
    - Student SOS button remains red and says `Gửi SOS hỗ trợ`.
    - Student SOS confirmation retains `Xác nhận gửi tín hiệu hỗ trợ` and `Ở lại trang này`.
    - Admin renders `Cổng quản trị`, `Vận hành metadata-only`, and no unsafe export/reset/drilldown/risk leaderboard controls.
    - Static source checks reject browser token storage and cross-role route imports.
  </behavior>
  <action>
    Create `frontend/tests/phase34-final-regression.test.tsx`. Use existing mock patterns. Include rendered DOM checks plus source checks for `student/page.tsx`, `admin/page.tsx`, `layout.tsx`, `parent/page.tsx`, and `components/ui-primitives.tsx`. Redline forbidden UI/control strings: `Export`, `Xuất`, `Download`, `Tải xuống`, `reset`, `drilldown`, `Chi tiết học sinh`, `risk leaderboard`, `xếp hạng nguy cơ`, `localStorage.setItem`, `sessionStorage.setItem`, `access_token`, `refresh_token`, `id_token`.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-final-regression.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains exact strings: `Gửi SOS hỗ trợ`, `Xác nhận gửi tín hiệu hỗ trợ`, `Ở lại trang này`, `Vận hành metadata-only`.
    - Test file checks forbidden controls with regex including `Export|Xuất|Download|Tải xuống|reset|drilldown|risk leaderboard|xếp hạng nguy cơ`.
  </acceptance_criteria>
  <done>Final regression gate is defined before representative adoption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply narrow Student primitive adoption</name>
  <read_first>
    frontend/app/(authenticated)/student/page.tsx;
    frontend/components/ui-primitives.tsx;
    frontend/tests/phase34-final-regression.test.tsx;
    .planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-CONTEXT.md
  </read_first>
  <files>frontend/app/(authenticated)/student/page.tsx</files>
  <action>
    Replace only low-risk duplicated presentation wrappers in the Student dashboard with primitives: use `LoadingState` for initial loading; use `SurfaceCard` for the welcome/profile and SOS status list containers; use `EntryCard` for `ChatEntryCard` and `WellbeingEntryCard`; use `ResponsiveTable` around the existing native table. Preserve all Student route data fetching, state, SOS sending behavior, mood reminder actions, privacy link, Vietnamese-first copy, and SOS red classes (`bg-red-600`, `hover:bg-red-700`, `text-red-700`, red border). Do not redesign dashboard content, do not change field visibility, and do not alter fetch timing/caching.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-final-regression.test.tsx tests/role-dashboards.test.tsx tests/phase20-responsive-smoke-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `student/page.tsx` imports from `@/components/ui-primitives`.
    - `student/page.tsx` contains `LoadingState` and `ResponsiveTable`.
    - SOS classes still include `bg-red-600`, `hover:bg-red-700`, or `text-red-700`.
    - Student API paths remain unchanged: `/api/student/profile` still appears.
  </acceptance_criteria>
  <done>Student representative dashboard uses primitives without privacy/SOS behavior changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply narrow Admin primitive adoption and run regression suite</name>
  <read_first>
    frontend/app/(authenticated)/admin/page.tsx;
    frontend/components/ui-primitives.tsx;
    frontend/tests/phase32-release-gates-ui.test.tsx;
    frontend/tests/phase34-final-regression.test.tsx
  </read_first>
  <files>frontend/app/(authenticated)/admin/page.tsx, frontend/tests/phase34-final-regression.test.tsx</files>
  <action>
    Replace Admin dashboard entry-card duplication with shared `EntryCard` and use `LoadingState` only for count labels if feasible without changing behavior. Preserve exact metadata-only copy, admin routes, and existing count behavior. Do not add raw exports, risk leaderboards, per-student drilldowns, raw audit browsers, destructive reset controls, raw IDs/emails/names/notes/transcripts/answers/provider claims/secrets/request bodies/free-text reasons/browser tokens, or new API calls. After implementation, run the Phase 34 final regression plus existing role/responsive/auth/redline tests, frontend lint, and frontend production build.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase34-final-regression.test.tsx tests/role-dashboards.test.tsx tests/phase20-responsive-smoke-ui.test.tsx tests/auth-portals.test.tsx tests/phase32-release-gates-ui.test.tsx</automated>
    <automated>npm --prefix frontend run lint</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `admin/page.tsx` imports from `@/components/ui-primitives`.
    - Admin copy still includes `Vận hành metadata-only` and `Báo cáo tổng hợp riêng tư`.
    - Rendered Admin DOM has no button/link matching `Export|Xuất|Download|Tải xuống|reset|drilldown|Chi tiết học sinh|risk leaderboard|xếp hạng nguy cơ`.
    - Admin API paths remain unchanged: `/api/admin/users` and `/api/admin/links`.
  </acceptance_criteria>
  <done>Admin representative dashboard uses primitives while preserving metadata-only posture and final regressions pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Student route → DOM | Student-owned sensitive wellbeing/SOS surfaces must preserve privacy and red SOS semantics. |
| Admin route → DOM | Admin metadata-only surfaces must not gain raw controls or private data display. |
| shared primitives → role dashboards | Shared presentation must not introduce cross-role imports or token storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-13 | Information Disclosure | Student dashboard | mitigate | Preserve existing data fields and privacy copy; no raw new adult-visible content; final tests. |
| T-34-14 | Integrity/Safety | Student SOS section | mitigate | Preserve exact SOS copy and red classes; test red semantics. |
| T-34-15 | Information Disclosure | Admin dashboard | mitigate | Redline tests block raw exports, risk leaderboards, drilldowns, raw audit browsers, and destructive reset controls. |
| T-34-16 | Spoofing/Session | touched frontend sources | mitigate | Static no-browser-token-storage regression checks. |
</threat_model>

<verification>
Run:
`npm --prefix frontend run test -- tests/phase34-final-regression.test.tsx tests/phase34-ui-primitives.test.tsx tests/phase34-role-shell.test.tsx tests/phase34-adult-shared-presentation.test.tsx tests/role-dashboards.test.tsx tests/phase20-responsive-smoke-ui.test.tsx tests/auth-portals.test.tsx tests/phase32-release-gates-ui.test.tsx`
`npm --prefix frontend run lint`
`npm --prefix frontend run build`
</verification>

<success_criteria>
All Phase 34 requirements are covered: UIC-02 primitives, UIC-03 role-specific copy, UIC-04 accessibility/responsive/SOS semantics, ROLE-05 active role and role-boundary guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/34-shared-ui-primitives-role-shell-harmonization/34-04-SUMMARY.md`.
</output>

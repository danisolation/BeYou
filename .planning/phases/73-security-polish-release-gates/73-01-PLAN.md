---
phase: 73-security-polish-release-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_phase73_release_gates.py
  - frontend/tests/phase73-release-gates-ui.test.tsx
  - frontend/scripts/release-gates.test.mjs
  - README.md
  - .planning/phases/73-security-polish-release-gates/73-VERIFICATION.md
autonomous: true
requirements:
  - SECURE-01
must_haves:
  truths:
    - "SMTP recipient emails, alert notes, and credentials never appear in serialized operations/audit metadata for v2.4 surfaces."
    - "Tenant_id columns added in Phase 72 do not surface as raw values in any current admin operations payload (forward-compat safety)."
    - "All v1.5 Phase 32 privacy invariants still hold after Phase 71-72 changes (zero regression)."
    - "Backend pytest, frontend vitest, frontend lint, frontend build, Node release-gates, smoke:demo all pass deterministically; smoke:pilot passes or is documented as constrained per D4."
    - "README v2.4 row in the privacy/release-gate matrix documents the SECURE-01 gate commands and constraint policy."
  artifacts:
    - path: "backend/tests/test_phase73_release_gates.py"
      provides: "Backend release-gate matrix asserting SECURE-01 + NOTIFY-01/02 + TENANT-01 forward-compat sanitizer invariants."
    - path: "frontend/tests/phase73-release-gates-ui.test.tsx"
      provides: "Frontend defense-in-depth gate ensuring admin operations DOM never renders raw recipient emails or raw tenant_id values from v2.4 metadata."
    - path: "frontend/scripts/release-gates.test.mjs"
      provides: "Node release-gate script extended with the Phase 73 row documenting the deterministic v2.4 gate command list."
    - path: "README.md"
      provides: "Updated Privacy, security, and release gates matrix with the SECURE-01 v2.4 row + smoke:pilot constraint policy."
    - path: ".planning/phases/73-security-polish-release-gates/73-VERIFICATION.md"
      provides: "Final evidence file recording exit codes, counts, and smoke:pilot constraint disposition."
  key_links:
    - from: "backend/tests/test_phase73_release_gates.py"
      to: "backend/app/services/admin_operations.py + backend/app/core/config.py"
      via: "build_operations_dashboard() + Settings.validate_smtp_configuration_rules()"
      pattern: "build_operations_dashboard|validate_smtp_configuration_rules"
    - from: "frontend/tests/phase73-release-gates-ui.test.tsx"
      to: "frontend admin operations page"
      via: "render() with synthetic v2.4 metadata payload, assert DOM has no recipient email or raw uuid tenant_id"
      pattern: "AdminOperationsPage|operationsDashboard"
    - from: "frontend/scripts/release-gates.test.mjs"
      to: "Phase 73 deterministic command list"
      via: "PHASE73_NODE_REQUIREMENT_IDS + LIVE_PILOT_CONSTRAINT_V24"
      pattern: "PHASE73_NODE_REQUIREMENT_IDS|smoke:pilot constrained"
---

<objective>
Lock v2.4 release gates: prove zero regression on privacy boundaries, metadata-only serialization, and the deterministic release-gate command matrix after Phase 71 (SMTP helpers) and Phase 72 (tenant schema scaffolding). Mirrors the v1.5 Phase 32 pattern per D1.

Purpose: Ship v2.4 with the same evidence rigor as v1.5 — no new product surfaces, only verification, sanitizer redlines, doc updates, and release-gate runs.
Output: One backend gate test, one frontend gate test, one Node gate row, README matrix row, and a `73-VERIFICATION.md` evidence file.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-security-polish-release-gates/73-CONTEXT.md
@.planning/phases/71-external-notification-helpers/71-SUMMARY.md
@.planning/phases/72-multi-school-tenant-schema-scaffolding/72-SUMMARY.md
@backend/tests/test_phase32_release_gates.py
@frontend/tests/phase32-release-gates-ui.test.tsx
@frontend/scripts/release-gates.test.mjs
@backend/app/services/admin_operations.py
@backend/app/core/config.py
@backend/app/services/sos_email.py

<interfaces>
<!-- Key entry points the executor will exercise. No new code is being introduced. -->

From backend/app/services/admin_operations.py:
```python
def build_operations_dashboard(db: OrmSession) -> AdminOperationsDashboard: ...
# Forbidden markers list at module top (FORBIDDEN_OPERATIONS_MARKERS) MUST already
# include recipient_email, raw_email, tenant_url, student_email, etc. Phase 73 only
# asserts these stay rejected when Phase 71/72 metadata flows through the dashboard.
```

From backend/app/core/config.py:
```python
class Settings:
    def validate_smtp_configuration_rules(self) -> None: ...
# Phase 71 added this. Phase 73 asserts it never raises with PII in the message
# and that fallback transitions log only redacted metadata.
```

From backend/app/services/sos_email.py:
```python
# Returns "smtp_configuration_missing" sentinel — never the raw recipient address.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend Phase 73 release-gate test suite</name>
  <files>backend/tests/test_phase73_release_gates.py</files>
  <behavior>
    Mirror `backend/tests/test_phase32_release_gates.py` structure. Required cases:
    - `test_phase73_requirement_ids_explicit` — asserts `PHASE73_BACKEND_REQUIREMENT_IDS == ("SECURE-01", "NOTIFY-01", "NOTIFY-02", "TENANT-01")`.
    - `test_phase73_forbidden_markers_extend_phase32` — asserts the Phase 73 forbidden marker tuple is a strict superset of `PHASE32_FORBIDDEN_BACKEND_MARKERS` and adds the v2.4 tokens: `"smtp_password"`, `"smtp_username"`, `"changeme"`, `"@gmail.com"`, `"@outlook.com"`, `"smtp.gmail.com"`, `"tenant_id"`, `"tenant_url"`.
    - `test_smtp_configuration_validation_does_not_leak_credentials` — sets `SMTP_HOST`, `SMTP_USERNAME="changeme"`, `SMTP_PASSWORD="changeme"`, calls `Settings().validate_smtp_configuration_rules()`, captures stdout/stderr via `capsys`; asserts no forbidden marker substring appears in captured output and that the fallback transition is logged as a safe metadata-only sentinel (e.g. `"smtp_configuration_missing"` or `"local_outbox"` only).
    - `test_operations_dashboard_redacts_v24_smtp_dispatch_metadata` — seeds a synthetic `SosNotificationDelivery` row with recipient_email set to a realistic address (e.g. `parent@example.com`) and an SMTP error string containing the credential placeholder; calls `build_operations_dashboard(db)`; serializes via `.model_dump_json()`; asserts NONE of the Phase 73 forbidden markers appear in the serialized payload.
    - `test_operations_dashboard_does_not_expose_raw_tenant_id` — creates two users with distinct `tenant_id` UUIDs (Phase 72 columns); calls `build_operations_dashboard(db)`; serializes; asserts the literal string of each `tenant_id` UUID does NOT appear anywhere in the serialized payload (forward-compat redline for TENANT-01).
    - `test_phase32_invariants_still_hold` — re-runs a tightened subset of Phase 32 assertions (re-import `PHASE32_FORBIDDEN_BACKEND_MARKERS`, re-serialize a dashboard against the same seed pattern) to prove zero regression.
    Each case MUST be deterministic and self-clean (reuse `_clean_phase32_database()` pattern or an equivalent local fixture).
  </behavior>
  <action>
    Create `backend/tests/test_phase73_release_gates.py`. Import the existing Phase 32 fixtures/cleaner where possible (`from tests.test_phase32_release_gates import _clean_phase32_database, PHASE32_FORBIDDEN_BACKEND_MARKERS` — if not importable, copy the cleaner verbatim and document why). Implement all six tests above. Do NOT introduce new sanitizer code — if any assertion fails it indicates a real regression in `admin_operations.py` or `config.py` that MUST be fixed in this task (the fix is part of the gate). Run `pytest backend/tests/test_phase73_release_gates.py -x` until green.
  </action>
  <verify>
    <automated>cd backend; pytest tests/test_phase73_release_gates.py -x</automated>
  </verify>
  <done>All 6 tests pass. Full backend suite `pytest` still passes with zero new failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend Phase 73 release-gate UI test + Node script extension</name>
  <files>frontend/tests/phase73-release-gates-ui.test.tsx, frontend/scripts/release-gates.test.mjs</files>
  <behavior>
    UI test (`frontend/tests/phase73-release-gates-ui.test.tsx`):
    - Mirror `frontend/tests/phase32-release-gates-ui.test.tsx`.
    - Build a synthetic `operationsDashboard` payload that injects v2.4-shaped poison into `sos_email.recent[]` (a recipient_email string `"parent@example.com"`, an `error_message` containing `"smtp.gmail.com"`) AND adds a synthetic `tenant_id` UUID string into a top-level `metadata_summary` field.
    - Mock `apiFetch` to return the poisoned payload, render `AdminOperationsPage`, await load.
    - Assert: `screen.queryByText(/parent@example.com/)` is null, `queryByText(/smtp\.gmail\.com/)` is null, `queryByText(/changeme/i)` is null, and `container.textContent` does NOT contain the synthetic tenant UUID string.
    - Assert `PHASE73_FRONTEND_REQUIREMENT_IDS === ["SECURE-01", "NOTIFY-02", "TENANT-01"]`.
    - Add a regression assertion: import the Phase 32 companion file list and assert it is still present on disk via `existsSync`.

    Node script extension (`frontend/scripts/release-gates.test.mjs`):
    - Append (do NOT remove Phase 32 cases) the following node:test cases:
      - `"Phase 73 v2.4 requirement ids are explicit"` — asserts `PHASE73_NODE_REQUIREMENT_IDS` equals `["SECURE-01"]`.
      - `"Phase 73 v2.4 release-gate command matrix is documented"` — asserts a top-level constant `PHASE73_GATE_COMMANDS` equals the exact deterministic list:
        ```
        [
          "cd backend && pytest",
          "cd backend && ruff check .",
          "npm --prefix frontend run lint",
          "npm --prefix frontend run build",
          "npm --prefix frontend test",
          "npm --prefix frontend run test:release-gates",
          "npm --prefix frontend run smoke:demo",
          "npm --prefix frontend run smoke:pilot",
          "npm --prefix frontend run guard:deploy",
        ]
        ```
      - `"Phase 73 smoke:pilot constraint policy documented"` — asserts a constant `LIVE_PILOT_CONSTRAINT_V24` exists and matches `/smoke:pilot constrained without safe BEYOU_FRONTEND_URL, BEYOU_BACKEND_URL, NEXT_PUBLIC_API_BASE_URL, and \/health\/ready=ready/`.
  </behavior>
  <action>
    Create the UI test file mirroring Phase 32 imports and `vi.mock` setup. Extend `release-gates.test.mjs` by adding the three new `test(...)` blocks plus the two new top-level constants near the existing `PHASE32_NODE_REQUIREMENT_IDS` declaration. Do NOT alter existing Phase 32 cases. Run gates locally and iterate until green.
  </action>
  <verify>
    <automated>npm --prefix frontend test -- phase73-release-gates-ui &amp;&amp; npm --prefix frontend run test:release-gates</automated>
  </verify>
  <done>Both new test files pass. Full `npm --prefix frontend test` still passes with zero new failures. `npm --prefix frontend run test:release-gates` passes including the new Phase 73 cases.</done>
</task>

<task type="auto">
  <name>Task 3: README v2.4 gate matrix + 73-VERIFICATION.md evidence file + privacy grep gates</name>
  <files>README.md, .planning/phases/73-security-polish-release-gates/73-VERIFICATION.md</files>
  <action>
    1. README.md — locate the existing `Privacy, security, and release gates` section. Add a new row (or sub-section) titled `### v2.4 (SECURE-01)` containing:
       - The Phase 73 gate command matrix verbatim from `PHASE73_GATE_COMMANDS` in Task 2.
       - The smoke:pilot constraint policy (per D4): "If `BEYOU_FRONTEND_URL`, `BEYOU_BACKEND_URL`, `NEXT_PUBLIC_API_BASE_URL` are not safe HTTPS pilot URLs OR `/health/ready` does not return `ready`, mark smoke:pilot **constrained** with the deterministic substitute `npm --prefix frontend run smoke:demo` and record the disposition in `73-VERIFICATION.md`. Do NOT mark the gate as failed solely due to live-environment unavailability."
       - The Phase 73 privacy grep gate commands (deterministic, run from repo root, PowerShell-safe):
         ```
         git grep -nE "smtp_password|smtp_username|changeme" -- backend/app frontend/app frontend/components
         git grep -nE "@(gmail|outlook|hotmail)\.com" -- backend/app frontend/app frontend/components
         git grep -nE "tenant_id" -- frontend/app frontend/components
         ```
         Document that the first two MUST return zero matches in production code paths (test fixtures excluded), and the third MUST return zero matches in any rendered surface.
       - Do NOT touch unrelated README sections.

    2. Create `.planning/phases/73-security-polish-release-gates/73-VERIFICATION.md` with sections:
       - `## Gate run evidence` — table of each gate command, exit code, test count, timestamp.
       - `## Privacy grep evidence` — output (or "0 matches") of the three grep commands above.
       - `## smoke:pilot disposition` — one of `passed` (with safe URLs documented), `constrained` (with substitute command + reason), or `failed` (with remediation). Per D4 prefer `constrained` over `failed` when only live-env URLs are missing.
       - `## SECURE-01 mapping` — table mapping each ROADMAP success criterion + each forbidden marker category to the test name in `test_phase73_release_gates.py` / `phase73-release-gates-ui.test.tsx`.
       - `## Zero-regression statement` — explicit claim that Phase 32 invariants still hold, with the exit code of the Phase 32 test run as evidence.

    3. Run the full gate matrix and fill in the evidence file with real exit codes/counts.
  </action>
  <verify>
    <automated>cd backend; pytest; cd ..; npm --prefix frontend run lint; npm --prefix frontend run build; npm --prefix frontend test; npm --prefix frontend run test:release-gates; npm --prefix frontend run smoke:demo</automated>
  </verify>
  <done>README contains the v2.4 SECURE-01 row with command matrix, constraint policy, and grep gates. `73-VERIFICATION.md` exists with all five sections populated from real runs. All gates pass or smoke:pilot is explicitly documented as constrained per D4. All three privacy grep commands return zero matches in production code paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SMTP provider → backend logs/audit | Phase 71 added SMTP validation. Recipient addresses and credentials must never cross into stdout, audit metadata, or admin operations payloads. |
| Phase 72 `tenant_id` column → admin operations serializer | Raw `tenant_id` UUIDs must not leak into the admin operations DOM until multi-tenant runtime separation ships (v2.6+). |
| Admin operations dashboard → frontend DOM | Defense-in-depth: even if backend sanitizer regresses, frontend must not render forbidden markers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Information Disclosure | `backend/app/services/admin_operations.py` `build_operations_dashboard` serializing SMTP dispatch metadata from Phase 71 | mitigate | Task 1 `test_operations_dashboard_redacts_v24_smtp_dispatch_metadata` asserts serialized payload contains zero Phase 73 forbidden markers (recipient email, smtp host, credential placeholder). |
| T-73-02 | Information Disclosure | `backend/app/core/config.py` `validate_smtp_configuration_rules` log output during fallback | mitigate | Task 1 `test_smtp_configuration_validation_does_not_leak_credentials` captures stdout/stderr and asserts only safe sentinels appear. |
| T-73-03 | Information Disclosure | Phase 72 `tenant_id` columns surfacing through admin operations payload as raw UUIDs | mitigate | Task 1 `test_operations_dashboard_does_not_expose_raw_tenant_id` + Task 2 UI test asserts UUID strings absent from DOM. |
| T-73-04 | Tampering / Regression | Phase 71-72 changes silently regressing v1.5 Phase 32 privacy invariants | mitigate | Task 1 `test_phase32_invariants_still_hold` re-asserts the Phase 32 forbidden marker set against current `build_operations_dashboard` output. |
| T-73-05 | Repudiation | Release gate failure undocumented (e.g. smoke:pilot silently skipped) | mitigate | Task 3 `73-VERIFICATION.md` `smoke:pilot disposition` section forces explicit `passed | constrained | failed` classification per D4. |
| T-73-06 | Information Disclosure | Production source code accidentally carrying SMTP credential placeholders or hard-coded recipient emails | mitigate | Task 3 privacy grep gates run against `backend/app` and `frontend/app`/`frontend/components`, MUST return zero matches. |
| T-73-07 | Elevation of Privilege | New v2.4 surfaces inadvertently introduce raw data admin views | accept | Out of scope per D3: this phase adds zero new surfaces. Future phase audits cover any new admin panel. |
</threat_model>

<verification>
## Phase-level gate matrix (Task 3 records evidence for each)

| Command | Purpose | Required disposition |
|---------|---------|----------------------|
| `cd backend && pytest` | Full backend regression + Phase 73 tests | pass |
| `cd backend && ruff check .` | Backend lint | pass |
| `npm --prefix frontend run lint` | Frontend lint | pass |
| `npm --prefix frontend run build` | Frontend build | pass |
| `npm --prefix frontend test` | Frontend vitest including phase73 UI test | pass |
| `npm --prefix frontend run test:release-gates` | Node release-gate matrix incl. Phase 73 rows | pass |
| `npm --prefix frontend run smoke:demo` | Public demo smoke | pass |
| `npm --prefix frontend run smoke:pilot` | Production pilot smoke | pass OR constrained per D4 |
| `npm --prefix frontend run guard:deploy` | Deployment guardrails | pass OR constrained per D4 |
| `git grep -nE "smtp_password|smtp_username|changeme" -- backend/app frontend/app frontend/components` | Privacy grep gate | zero matches |
| `git grep -nE "@(gmail|outlook|hotmail)\.com" -- backend/app frontend/app frontend/components` | Privacy grep gate | zero matches |
| `git grep -nE "tenant_id" -- frontend/app frontend/components` | Tenant raw-exposure grep gate | zero matches |
</verification>

<success_criteria>
Maps directly to ROADMAP Phase 73 success criterion (#1 "All backend and frontend test suites pass with zero exceptions") and SECURE-01 audit/release-gate scope:

- [ ] `backend/tests/test_phase73_release_gates.py` exists with the 6 required tests and passes.
- [ ] `frontend/tests/phase73-release-gates-ui.test.tsx` exists and passes.
- [ ] `frontend/scripts/release-gates.test.mjs` extended with `PHASE73_NODE_REQUIREMENT_IDS`, `PHASE73_GATE_COMMANDS`, `LIVE_PILOT_CONSTRAINT_V24` and 3 new node:test cases; passes.
- [ ] README has a v2.4 SECURE-01 row with the command matrix, smoke:pilot constraint policy per D4, and the three privacy grep gate commands.
- [ ] `73-VERIFICATION.md` exists with gate evidence, grep evidence, smoke:pilot disposition, SECURE-01 mapping, and zero-regression statement.
- [ ] Full backend pytest passes (no Phase 32 regression).
- [ ] Full frontend vitest, lint, build pass.
- [ ] All three privacy grep commands return zero matches in production code paths.
- [ ] smoke:pilot is either `passed` (with safe pilot URLs) or `constrained` (with deterministic substitute logged) — never silently failed.
- [ ] No new API endpoints, no new UI components, no new admin panels were introduced (per D3).
</success_criteria>

<commit_strategy>
Atomic commit boundaries (one commit per task):

1. `test(73): add backend Phase 73 release-gate matrix for SECURE-01 + NOTIFY-01/02 + TENANT-01`
   - Files: `backend/tests/test_phase73_release_gates.py` (+ any sanitizer fix in `admin_operations.py` or `config.py` if a real regression is uncovered).

2. `test(73): add frontend Phase 73 UI gate + extend Node release-gate matrix for v2.4`
   - Files: `frontend/tests/phase73-release-gates-ui.test.tsx`, `frontend/scripts/release-gates.test.mjs`.

3. `docs(73): document v2.4 SECURE-01 release-gate matrix + smoke:pilot constraint policy`
   - Files: `README.md`, `.planning/phases/73-security-polish-release-gates/73-VERIFICATION.md`.
</commit_strategy>

<output>
After completion, create `.planning/phases/73-security-polish-release-gates/73-01-SUMMARY.md` summarizing:
- Tests added (counts, file paths).
- Gate matrix run results (exit codes).
- smoke:pilot disposition (`passed` | `constrained` per D4).
- Privacy grep outcomes (zero matches confirmation).
- Any sanitizer fixes that were required (should be none if Phase 71-72 met their contracts).
- Confirmation that zero new product surfaces were added (D3 compliance).
</output>

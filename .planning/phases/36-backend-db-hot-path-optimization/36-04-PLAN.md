---
phase: 36-backend-db-hot-path-optimization
plan: 04
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - backend/app/services/adult_summaries.py
  - backend/tests/test_phase36_adult_summary_hot_paths.py
autonomous: true
requirements:
  - DBPERF-03
must_haves:
  truths:
    - "Adult self-check summaries query latest and recent rows with SQL-side ordering, cutoff, and limits."
    - "Adult mood summaries query latest and recent rows/counts with SQL-side cutoff instead of broad Python slicing."
    - "Reason gates, shared-note consent, support-summary audit, and raw-content redlines remain intact."
  artifacts:
    - path: "backend/app/services/adult_summaries.py"
      provides: "SQL-side adult summary filtering and limits"
    - path: "backend/tests/test_phase36_adult_summary_hot_paths.py"
      provides: "DBPERF-03 adult summary regression coverage"
  key_links:
    - from: "backend/app/services/adult_summaries.py"
      to: "backend/tests/test_phase36_adult_summary_hot_paths.py"
      via: "latest/recent SQL-side behavior"
      pattern: "RECENT_SUMMARY_LIMIT"
---

<objective>
Optimize adult self-check and support-summary SQL filtering.

Purpose: Satisfy DBPERF-03 by pushing adult summary ordering, cutoff, and limits into SQL while preserving controlled reason gates, summary-only boundaries, and student-consented shared-note rules.
Output: Updated adult summary service plus Phase 36 adult summary tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-backend-db-hot-path-optimization/36-CONTEXT.md
@.planning/phases/36-backend-db-hot-path-optimization/36-RESEARCH.md
@backend/app/services/adult_summaries.py
@backend/app/api/adult_summaries.py
@backend/app/services/mood_note_shares.py
@backend/tests/test_phase14_adult_support_summaries.py
@backend/tests/test_phase23_mood_note_shares.py
@backend/tests/test_phase24_reason_access.py
@backend/tests/phase36_hot_path_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adult summary SQL-side filtering tests</name>
  <files>backend/tests/test_phase36_adult_summary_hot_paths.py</files>
  <read_first>
    backend/tests/phase36_hot_path_utils.py
    backend/app/services/adult_summaries.py
    backend/tests/test_phase14_adult_support_summaries.py
    backend/tests/test_phase23_mood_note_shares.py
    backend/tests/test_phase24_reason_access.py
  </read_first>
  <action>
    Create `backend/tests/test_phase36_adult_summary_hot_paths.py` with tests named exactly `test_adult_self_check_summaries_use_sql_side_recent_limit`, `test_adult_support_mood_summary_uses_sql_cutoff_and_keeps_privacy`, and `test_adult_support_summary_reason_gate_still_blocks_before_loading_sensitive_summary`. Seed one student, teacher active link, SOS alert, at least 12 self-check attempts with mixed `completed_at` values where only 6 are within `RECENT_SUMMARY_DAYS`, and at least 8 mood check-ins where only 3 are within `RECENT_MOOD_DAYS`. Assert teacher self-check summary returns at most `RECENT_SUMMARY_LIMIT` recent rows and does not contain `raw_answers`, `private_note`, or `transcript`. Assert support summary with valid `reason_code=support_plan_context` has `recent_checkin_count == 3`, rejects raw private note text, records metadata-only audit, and query count is `<= 12`. Assert support summary without required reason still returns `428` when policy requires reasons.
  </action>
  <acceptance_criteria>
    - `grep -n "test_adult_self_check_summaries_use_sql_side_recent_limit" backend/tests/test_phase36_adult_summary_hot_paths.py` finds self-check SQL limit test.
    - `grep -n "test_adult_support_mood_summary_uses_sql_cutoff_and_keeps_privacy" backend/tests/test_phase36_adult_summary_hot_paths.py` finds mood cutoff test.
    - `grep -n "recent_checkin_count == 3" backend/tests/test_phase36_adult_summary_hot_paths.py` finds exact cutoff assertion.
    - `grep -n "428" backend/tests/test_phase36_adult_summary_hot_paths.py` finds reason gate regression.
    - `grep -n "<= 12" backend/tests/test_phase36_adult_summary_hot_paths.py` finds query-count ceiling.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_summary_hot_paths.py -q</automated>
  </verify>
  <done>DBPERF-03 adult summary tests prove SQL-side cutoff/limit behavior and privacy gates.</done>
</task>

<task type="auto">
  <name>Task 2: Move self-check recent filtering and limit into SQL</name>
  <files>backend/app/services/adult_summaries.py</files>
  <read_first>
    backend/app/services/adult_summaries.py
    backend/app/core/authorization.py
    backend/app/db/models.py
    backend/tests/test_phase36_adult_summary_hot_paths.py
    backend/tests/test_phase3_adult_summaries.py
  </read_first>
  <action>
    In `get_adult_self_check_summaries`, replace the unbounded `attempts = list(db.scalars(select(SelfCheckAttempt)...))` load and Python cutoff/slice with two SQL queries. Add `latest_attempt = db.scalar(select(SelfCheckAttempt).where(SelfCheckAttempt.student_id == student_id).order_by(SelfCheckAttempt.completed_at.desc(), SelfCheckAttempt.id.desc()).limit(1))`. Add `recent_attempts = list(db.scalars(select(SelfCheckAttempt).where(SelfCheckAttempt.student_id == student_id, SelfCheckAttempt.completed_at >= recent_cutoff).order_by(SelfCheckAttempt.completed_at.desc(), SelfCheckAttempt.id.desc()).limit(RECENT_SUMMARY_LIMIT)))`. Set `latest_summary = _summary_item(latest_attempt) if latest_attempt else None` and `recent_summaries = [_summary_item(attempt) for attempt in recent_attempts]`. Preserve existing `require_permission`, student lookup, `record_audit_event`, `db.commit()`, response model, and metadata keys.
  </action>
  <acceptance_criteria>
    - `grep -n "latest_attempt = db.scalar" backend/app/services/adult_summaries.py` finds latest SQL query.
    - `grep -n "SelfCheckAttempt.completed_at >= recent_cutoff" backend/app/services/adult_summaries.py` finds SQL-side cutoff.
    - `grep -n ".limit(RECENT_SUMMARY_LIMIT)" backend/app/services/adult_summaries.py` finds SQL-side recent limit.
    - `grep -n "for attempt in attempts" backend/app/services/adult_summaries.py` does not find the old broad Python slicing loop.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_summary_hot_paths.py tests/test_phase3_adult_summaries.py tests/test_phase14_adult_support_summaries.py -q</automated>
  </verify>
  <done>Adult self-check summaries use SQL-side latest/recent bounded queries.</done>
</task>

<task type="auto">
  <name>Task 3: Move mood summary cutoff/counts into SQL and preserve support gates</name>
  <files>backend/app/services/adult_summaries.py</files>
  <read_first>
    backend/app/services/adult_summaries.py
    backend/app/services/mood_note_shares.py
    backend/app/services/privacy_controls.py
    backend/tests/test_phase36_adult_summary_hot_paths.py
    backend/tests/test_phase23_mood_note_shares.py
    backend/tests/test_phase24_reason_access.py
  </read_first>
  <action>
    In `_mood_summary`, replace `items = list(...limit(RECENT_SUMMARY_LIMIT))` plus Python cutoff filtering with three SQL-backed values: `latest = db.scalar(select(MoodCheckIn).where(MoodCheckIn.student_id == student_id).order_by(MoodCheckIn.created_at.desc(), MoodCheckIn.id.desc()).limit(1))`; `recent_items = list(db.scalars(select(MoodCheckIn).where(MoodCheckIn.student_id == student_id, MoodCheckIn.created_at >= cutoff).order_by(MoodCheckIn.created_at.desc(), MoodCheckIn.id.desc()).limit(RECENT_SUMMARY_LIMIT)))`; `high_concern_count = db.scalar(select(func.count()).select_from(MoodCheckIn).where(MoodCheckIn.student_id == student_id, MoodCheckIn.created_at >= cutoff, MoodCheckIn.trend_label == "Cần hỗ trợ sớm")) or 0`. Use `recent_items` for `recent_checkin_count` and `recent_trend_labels`. Keep private notes out of the response. In `_support_plan_summary`, if a plan exists, replace the separate selected count and selected-for-viewer queries with one query for `StudentSupportPlanAdult.adult_id` rows for `support_plan_id == plan.id`, deriving `selected_adult_count` and `shared_with_viewer` from that list. Do not change `_enforce_access_reason`, reason error statuses, or shared mood note access.
  </action>
  <acceptance_criteria>
    - `grep -n "latest = db.scalar" backend/app/services/adult_summaries.py` finds latest mood query.
    - `grep -n "MoodCheckIn.created_at >= cutoff" backend/app/services/adult_summaries.py` finds SQL-side mood cutoff.
    - `grep -n "MoodCheckIn.trend_label == \"Cần hỗ trợ sớm\"" backend/app/services/adult_summaries.py` finds SQL-side high-concern count.
    - `grep -n "selected_adult_ids" backend/app/services/adult_summaries.py` finds one-query support-plan selected adult derivation.
    - `grep -n "_enforce_access_reason" backend/app/services/adult_summaries.py` still finds reason-gate call.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_summary_hot_paths.py tests/test_phase14_adult_support_summaries.py tests/test_phase23_mood_note_shares.py tests/test_phase24_reason_access.py -q</automated>
  </verify>
  <done>Adult support summary mood/support-plan sections use SQL-side bounded data and preserve reason/privacy gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| adult support route -> protected summaries | Reason gates and active link/SOS authorization must run before protected summary data is returned. |
| mood/self-check tables -> adult response | Adults receive summaries only, not raw answers or private notes. |
| shared mood notes -> support summary | Only student-consented shared notes may appear through existing share service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-04-01 | Information Disclosure | adult summary responses | mitigate | Tests reject raw answer/private note/transcript markers and preserve existing response schemas. |
| T-36-04-02 | Elevation of Privilege | reason-gated support summary | mitigate | Missing reason still returns 428 before protected summary is returned. |
| T-36-04-03 | Repudiation | support summary audit | mitigate | Existing metadata-only audit event remains after SQL refactor. |
| T-36-04-04 | Denial of Service | broad history loads | mitigate | Latest/recent self-check and mood data use SQL-side order, cutoff, and limit. |
</threat_model>

<verification>
Run:
`cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_summary_hot_paths.py tests/test_phase3_adult_summaries.py tests/test_phase14_adult_support_summaries.py tests/test_phase23_mood_note_shares.py tests/test_phase24_reason_access.py -q`
</verification>

<success_criteria>
DBPERF-03 is satisfied for adult self-check/support summary paths while reason gates and summary-only boundaries remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/36-backend-db-hot-path-optimization/36-04-SUMMARY.md`
</output>


---
phase: 36-backend-db-hot-path-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/phase36_hot_path_utils.py
  - backend/tests/test_phase36_hot_path_utils.py
autonomous: true
requirements:
  - DBPERF-01
  - DBPERF-02
  - DBPERF-03
  - DBPERF-04
  - DBPERF-05
must_haves:
  truths:
    - "Phase 36 has aggregate-only backend query-count helpers before hot-path edits."
    - "Phase 36 tests can seed users, active/revoked links, SOS alerts, self-check attempts, mood check-ins, support plans, and audit events without raw sensitive evidence."
    - "The harness supports deterministic boundedness checks for admin, adult visibility, support summary, operations, and schema/index work."
  artifacts:
    - path: "backend/tests/phase36_hot_path_utils.py"
      provides: "Reusable Phase 36 backend query-count and seed helpers"
    - path: "backend/tests/test_phase36_hot_path_utils.py"
      provides: "Self-test coverage for Phase 36 helpers"
  key_links:
    - from: "backend/tests/phase36_hot_path_utils.py"
      to: "backend/tests/test_phase36_hot_path_utils.py"
      via: "helper self-test import and QueryCounter assertion"
      pattern: "QueryCounter"
---

<objective>
Create the backend-only Phase 36 test harness used by all hot-path optimization plans.

Purpose: Give later plans deterministic, aggregate-only query-count and seed helpers without logging raw student content, identifiers, cookies, tokens, request bodies, private notes, transcripts, free-text reasons, exports, or per-student drilldowns.
Output: Phase 36 test utility module plus self-tests that pass before production code changes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-backend-db-hot-path-optimization/36-CONTEXT.md
@.planning/phases/36-backend-db-hot-path-optimization/36-RESEARCH.md
@.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md
@backend/tests/test_phase33_performance_baseline.py
@backend/tests/test_admin_users_links.py
@backend/tests/test_phase4_sos_backend.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aggregate-only Phase 36 query-count helpers</name>
  <files>backend/tests/phase36_hot_path_utils.py</files>
  <read_first>
    backend/tests/test_phase33_performance_baseline.py
    backend/app/core/security.py
    backend/app/main.py
    backend/app/db/session.py
    backend/app/db/models.py
  </read_first>
  <action>
    Create `backend/tests/phase36_hot_path_utils.py` with these exact exported names: `ORIGIN_HEADERS`, `PASSWORD`, `QueryCounter`, `make_user`, `login_client`, `measure_queries`, `seed_student_link`, `seed_sos_alert`, `assert_aggregate_only_text`. Implement `QueryCounter` using `sqlalchemy.event.listen(engine, "before_cursor_execute", ...)` and `event.remove(...)`, mirroring `backend/tests/test_phase33_performance_baseline.py`. Implement `make_user(db, email, role, full_name, school=None, class_name=None, is_demo=True)` with `hash_password(PASSWORD)` and `AccountStatus.ACTIVE.value`. Implement `login_client(email)` with `TestClient(app)` and POST `/api/auth/login` using `ORIGIN_HEADERS`. Implement `measure_queries(callable)` returning `(result, count)` without storing request or response bodies. Implement `assert_aggregate_only_text(text)` to reject these exact markers case-insensitively: `access_token`, `refresh_token`, `id_token`, `password_hash`, `private_note`, `transcript`, `raw_answers`, `provider_subject`, `raw_claims`, `export_url`, `risk_leaderboard`, `reason_text`.
  </action>
  <acceptance_criteria>
    - `grep -n "class QueryCounter" backend/tests/phase36_hot_path_utils.py` finds the helper class.
    - `grep -n "before_cursor_execute" backend/tests/phase36_hot_path_utils.py` finds SQLAlchemy query-count wiring.
    - `grep -n "def make_user" backend/tests/phase36_hot_path_utils.py` finds user seed helper.
    - `grep -n "def login_client" backend/tests/phase36_hot_path_utils.py` finds login helper.
    - `grep -n "access_token" backend/tests/phase36_hot_path_utils.py` finds aggregate-only redline coverage.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_hot_path_utils.py -q</automated>
  </verify>
  <done>Phase 36 helper module exists and exposes aggregate-only query-count/seed helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Self-test Phase 36 helpers</name>
  <files>backend/tests/test_phase36_hot_path_utils.py</files>
  <read_first>
    backend/tests/phase36_hot_path_utils.py
    backend/tests/conftest.py
    backend/app/db/models.py
  </read_first>
  <action>
    Create `backend/tests/test_phase36_hot_path_utils.py` with two tests: `test_phase36_query_counter_counts_selects` and `test_phase36_aggregate_redline_helper_rejects_sensitive_markers`. The first test must create one admin user with `make_user`, call `measure_queries(lambda: db.scalar(select(func.count()).select_from(User)))`, and assert the count is `>= 1` and the scalar result is `>= 1`. The second test must assert `assert_aggregate_only_text("safe aggregate status")` returns normally and raises `AssertionError` for `private_note` and `access_token`.
  </action>
  <acceptance_criteria>
    - `grep -n "test_phase36_query_counter_counts_selects" backend/tests/test_phase36_hot_path_utils.py` finds the query counter self-test.
    - `grep -n "test_phase36_aggregate_redline_helper_rejects_sensitive_markers" backend/tests/test_phase36_hot_path_utils.py` finds redline self-test.
    - `grep -n "private_note" backend/tests/test_phase36_hot_path_utils.py` finds forbidden marker assertion.
    - `grep -n "access_token" backend/tests/test_phase36_hot_path_utils.py` finds forbidden marker assertion.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_hot_path_utils.py -q</automated>
  </verify>
  <done>Helper self-tests pass and remain aggregate-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tests -> database | Helpers seed demo-safe rows and count SQL statements only. |
| query measurement -> evidence | Evidence remains aggregate counts/status, not payload dumps. |
| helper reuse -> later plans | Later plans use shared helpers without weakening auth or privacy redlines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-01-01 | Information Disclosure | test helpers | mitigate | `assert_aggregate_only_text` rejects token, private note, transcript, raw answer, provider claim, export, risk leaderboard, and reason-text markers. |
| T-36-01-02 | Tampering | query-count helper | mitigate | `QueryCounter` attaches/removes SQLAlchemy event listeners in a context manager to avoid polluting other tests. |
| T-36-01-03 | Denial of Service | seeded data volume | mitigate | Helper self-tests seed minimal rows; later plans own high-volume cases. |
</threat_model>

<verification>
Run:
`cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_hot_path_utils.py -q`
</verification>

<success_criteria>
Phase 36 has passing helper tests and reusable aggregate-only hot-path measurement utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/36-backend-db-hot-path-optimization/36-01-SUMMARY.md`
</output>


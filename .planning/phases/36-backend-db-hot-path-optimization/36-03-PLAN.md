---
phase: 36-backend-db-hot-path-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - backend/app/services/adult_visibility.py
  - backend/app/api/teacher.py
  - backend/app/api/parent.py
  - backend/app/services/sos.py
  - backend/tests/test_phase36_adult_visibility_hot_paths.py
autonomous: true
requirements:
  - DBPERF-02
  - DBPERF-03
must_haves:
  truths:
    - "Teacher and Parent linked-student lists batch active-link plus SOS-signal visibility checks."
    - "Support overview batches visible students, latest self-checks, latest SOS alerts, open SOS counts, and latest-alert events where practical."
    - "Non-SOS, revoked-link, wrong-relationship, and outsider adults cannot see hidden students."
  artifacts:
    - path: "backend/app/services/adult_visibility.py"
      provides: "Shared SOS-visible adult linked-student predicate"
    - path: "backend/app/services/sos.py"
      provides: "Batched support overview data loading"
    - path: "backend/tests/test_phase36_adult_visibility_hot_paths.py"
      provides: "DBPERF-02 and overview DBPERF-03 regression coverage"
  key_links:
    - from: "backend/app/api/teacher.py"
      to: "backend/app/services/adult_visibility.py"
      via: "list_sos_visible_linked_student_rows"
      pattern: "relationship_type=UserRole.TEACHER.value"
    - from: "backend/app/api/parent.py"
      to: "backend/app/services/adult_visibility.py"
      via: "list_sos_visible_linked_student_rows"
      pattern: "relationship_type=UserRole.PARENT.value"
---

<objective>
Batch Teacher/Parent SOS-visible linked-student and support overview hot paths.

Purpose: Satisfy DBPERF-02 and the dashboard/overview part of DBPERF-03 without weakening active relationship, role-specific relationship type, or SOS-only adult visibility rules.
Output: Shared adult visibility service, updated Teacher/Parent route handlers, batched support overview internals, and regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-backend-db-hot-path-optimization/36-CONTEXT.md
@.planning/phases/36-backend-db-hot-path-optimization/36-RESEARCH.md
@backend/app/api/teacher.py
@backend/app/api/parent.py
@backend/app/core/authorization.py
@backend/app/services/sos.py
@backend/tests/test_phase4_sos_backend.py
@backend/tests/test_phase2_security_regression.py
@backend/tests/phase36_hot_path_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adult visibility batching regression tests</name>
  <files>backend/tests/test_phase36_adult_visibility_hot_paths.py</files>
  <read_first>
    backend/tests/phase36_hot_path_utils.py
    backend/tests/test_phase4_sos_backend.py
    backend/tests/test_phase2_security_regression.py
    backend/app/api/teacher.py
    backend/app/api/parent.py
    backend/app/services/sos.py
    backend/app/core/authorization.py
  </read_first>
  <action>
    Create `backend/tests/test_phase36_adult_visibility_hot_paths.py` with tests named exactly `test_teacher_parent_students_batch_sos_visibility_without_leaks` and `test_support_overview_batches_latest_signals_and_keeps_audit_metadata_only`. Seed one admin, one teacher, one parent, one outsider adult, at least 12 linked students with SOS alerts, one active linked student without SOS, one revoked linked student with SOS, and one wrong-relationship link with SOS. Assert `/api/teacher/students` and `/api/parent/students` return only the 12 SOS-visible active relationship rows and never include no-SOS/revoked/wrong-relationship students. Use `measure_queries` and assert teacher and parent student-list query counts are `<= 8`. For support overview, seed self-check attempts and SOS status events for visible students, call `/api/teacher/support-overview`, assert status `200`, visible length `12`, response text does not contain `raw_answers`, `private_note`, `transcript`, or `access_token`, and query count is `<= 14`.
  </action>
  <acceptance_criteria>
    - `grep -n "test_teacher_parent_students_batch_sos_visibility_without_leaks" backend/tests/test_phase36_adult_visibility_hot_paths.py` finds visibility test.
    - `grep -n "test_support_overview_batches_latest_signals_and_keeps_audit_metadata_only" backend/tests/test_phase36_adult_visibility_hot_paths.py` finds support overview test.
    - `grep -n "<= 8" backend/tests/test_phase36_adult_visibility_hot_paths.py` finds student-list query-count ceiling.
    - `grep -n "<= 14" backend/tests/test_phase36_adult_visibility_hot_paths.py` finds support overview query-count ceiling.
    - `grep -n "revoked" backend/tests/test_phase36_adult_visibility_hot_paths.py` finds revoked-link regression.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_visibility_hot_paths.py -q</automated>
  </verify>
  <done>Adult visibility tests prove batching requirements and privacy boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared SOS-visible adult linked-student service</name>
  <files>backend/app/services/adult_visibility.py, backend/app/api/teacher.py, backend/app/api/parent.py</files>
  <read_first>
    backend/app/api/teacher.py
    backend/app/api/parent.py
    backend/app/core/authorization.py
    backend/app/db/models.py
    backend/tests/test_phase36_adult_visibility_hot_paths.py
  </read_first>
  <action>
    Create `backend/app/services/adult_visibility.py` exporting `list_sos_visible_linked_student_rows(db: OrmSession, *, adult: User, relationship_type: str, limit: int = 200) -> list[tuple[StudentAdultLink, User]]`. Implement it with `select(StudentAdultLink, User).join(User, User.id == StudentAdultLink.student_id).where(StudentAdultLink.adult_id == adult.id, StudentAdultLink.relationship_type == relationship_type, StudentAdultLink.status == LinkStatus.ACTIVE.value, exists(select(SosAlert.id).where(SosAlert.student_id == User.id))).order_by(User.full_name.asc(), User.email.asc(), User.id.asc()).limit(limit)`. In `backend/app/api/teacher.py` and `backend/app/api/parent.py`, remove per-row calls to `has_student_sos_signal` and `require_permission` from the list endpoints, call `require_role` unchanged, call `list_sos_visible_linked_student_rows` with the role relationship type, and build the same `LinkedStudentResponse` fields. Do not change route paths or response models.
  </action>
  <acceptance_criteria>
    - `grep -n "def list_sos_visible_linked_student_rows" backend/app/services/adult_visibility.py` finds helper.
    - `grep -n "exists(select(SosAlert.id)" backend/app/services/adult_visibility.py` finds batched SOS predicate.
    - `grep -n "StudentAdultLink.status == LinkStatus.ACTIVE.value" backend/app/services/adult_visibility.py` finds active-link predicate.
    - `grep -n "list_sos_visible_linked_student_rows" backend/app/api/teacher.py` finds teacher route usage.
    - `grep -n "list_sos_visible_linked_student_rows" backend/app/api/parent.py` finds parent route usage.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_visibility_hot_paths.py tests/test_phase2_security_regression.py tests/test_auth_privacy_portals.py -q</automated>
  </verify>
  <done>Teacher/Parent student list hot paths batch equivalent visibility predicates.</done>
</task>

<task type="auto">
  <name>Task 3: Batch support overview latest signals</name>
  <files>backend/app/services/sos.py</files>
  <read_first>
    backend/app/services/sos.py
    backend/app/services/adult_visibility.py
    backend/app/core/authorization.py
    backend/app/db/models.py
    backend/tests/test_phase36_adult_visibility_hot_paths.py
    backend/tests/test_phase4_sos_backend.py
  </read_first>
  <action>
    In `backend/app/services/sos.py`, import `list_sos_visible_linked_student_rows`. Update `get_support_overview` to call that helper once. Add private helpers `_latest_self_checks_by_student(db, student_ids) -> dict[uuid.UUID, SelfCheckAttempt]`, `_latest_sos_alerts_by_student(db, student_ids) -> dict[uuid.UUID, SosAlert]`, `_open_sos_counts_by_student(db, student_ids) -> dict[uuid.UUID, int]`, and `_events_by_alert_id(db, alert_ids) -> dict[uuid.UUID, list[SosStatusEvent]]`. Implement latest self-check/latest SOS helpers with SQLAlchemy `func.row_number().over(partition_by=..., order_by=(...desc(), ...desc()))` subqueries filtered on `row_number == 1`; implement open counts with `select(SosAlert.student_id, func.count(SosAlert.id)).where(SosAlert.student_id.in_(student_ids), SosAlert.current_status.in_(ACTIVE_SOS_STATUSES)).group_by(SosAlert.student_id)`. Update `_alert_response` signature to accept `preloaded_events: list[SosStatusEvent] | None = None` and use preloaded events when provided; keep existing `_events(db, alert.id)` behavior for other callers. Preserve existing audit event metadata keys and calls for latest summary/SOS reads.
  </action>
  <acceptance_criteria>
    - `grep -n "def _latest_self_checks_by_student" backend/app/services/sos.py` finds batched self-check helper.
    - `grep -n "row_number().over" backend/app/services/sos.py` finds latest-row batching query.
    - `grep -n "def _open_sos_counts_by_student" backend/app/services/sos.py` finds grouped open-count helper.
    - `grep -n "def _events_by_alert_id" backend/app/services/sos.py` finds batched event helper.
    - `grep -n "preloaded_events" backend/app/services/sos.py` finds non-N+1 alert response support.
  </acceptance_criteria>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_visibility_hot_paths.py tests/test_phase4_sos_backend.py tests/test_phase3_security_regression.py -q</automated>
  </verify>
  <done>Support overview batches latest signals, open counts, and latest-alert events while preserving audit/privacy semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| adult route -> visibility SQL | Batched SQL must remain equivalent to active-link plus SOS-signal authorization semantics. |
| support overview -> audit | Batched data loading cannot remove metadata-only read audit events. |
| alert event batching -> response | Preloaded events must only be for already-visible latest alerts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-03-01 | Elevation of Privilege | adult visibility helper | mitigate | SQL predicate includes adult id, relationship type, active link status, and SOS existence; tests cover no-SOS, revoked, wrong relationship, outsider. |
| T-36-03-02 | Information Disclosure | support overview | mitigate | Tests reject raw answer/private note/transcript/token markers and preserve SOS-only row count. |
| T-36-03-03 | Repudiation | support overview audit | mitigate | Existing `sensitive_resource_read` audit calls remain for latest self-check/SOS reads. |
| T-36-03-04 | Denial of Service | per-student overview queries | mitigate | Batched helpers and query-count ceilings prevent per-row query growth. |
</threat_model>

<verification>
Run:
`cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase36_adult_visibility_hot_paths.py tests/test_phase4_sos_backend.py tests/test_phase2_security_regression.py tests/test_auth_privacy_portals.py tests/test_phase3_security_regression.py -q`
</verification>

<success_criteria>
DBPERF-02 and support-overview DBPERF-03 are satisfied without weakening active relationship, SOS-only visibility, or audit redlines.
</success_criteria>

<output>
After completion, create `.planning/phases/36-backend-db-hot-path-optimization/36-03-SUMMARY.md`
</output>


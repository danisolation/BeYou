---
phase: 39-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.ts
  - frontend/app/globals.css
  - frontend/components/stitch-card.tsx
  - frontend/components/ui-primitives.tsx
  - frontend/components/navigation/student-sidebar.tsx
  - frontend/components/navigation/mobile-bottom-nav.tsx
  - frontend/components/layout-shell.tsx
  - frontend/app/(authenticated)/layout.tsx
autonomous: true
requirements: [UICONS-01, UICONS-02]

must_haves:
  truths:
    - "All Stitch mockup design tokens (colors, typography, spacing, radii) are available as Tailwind utilities"
    - "StitchCard component renders circular and rounded variants with icon, title, description, CTA"
    - "Desktop sidebar navigation renders on lg+ screens across student pages"
    - "Mobile bottom navigation renders on small screens with consistent icons"
    - "Layout wrapper provides consistent page structure with responsive behavior"
  artifacts:
    - path: "frontend/tailwind.config.ts"
      provides: "Full Stitch design token palette, typography scale, spacing, border-radius"
      contains: "primary.*00604e"
    - path: "frontend/components/stitch-card.tsx"
      provides: "StitchCard with circular/rounded variants"
      exports: ["StitchCard"]
    - path: "frontend/components/navigation/student-sidebar.tsx"
      provides: "Desktop sidebar navigation component"
      exports: ["StudentSidebar"]
    - path: "frontend/components/navigation/mobile-bottom-nav.tsx"
      provides: "Mobile bottom navigation component"
      exports: ["MobileBottomNav"]
    - path: "frontend/components/layout-shell.tsx"
      provides: "Layout wrapper with header, content area, responsive behavior"
      exports: ["LayoutShell"]
  key_links:
    - from: "frontend/app/(authenticated)/layout.tsx"
      to: "frontend/components/navigation/student-sidebar.tsx"
      via: "import and render"
      pattern: "import.*StudentSidebar"
    - from: "frontend/app/(authenticated)/layout.tsx"
      to: "frontend/components/navigation/mobile-bottom-nav.tsx"
      via: "import and render"
      pattern: "import.*MobileBottomNav"
    - from: "frontend/components/stitch-card.tsx"
      to: "frontend/tailwind.config.ts"
      via: "Tailwind classes using design tokens"
      pattern: "rounded-\\[32px\\]|bg-surface-container"
---

<objective>
Establish the Stitch design system foundation: synchronized Tailwind config with all design tokens, a shared StitchCard component, extracted navigation components (sidebar + mobile bottom nav), and a layout shell. All subsequent UI phases will consume these tokens and components.

Purpose: Create a single source of truth for design tokens and shared UI components so all future pages are visually consistent with Stitch mockups without duplicating styles.

Output: Updated tailwind.config.ts, globals.css, new StitchCard component, extracted navigation components, layout shell wrapper.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/tailwind.config.ts
@frontend/app/globals.css
@frontend/components/ui-primitives.tsx
@frontend/app/(authenticated)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind config + globals.css — full Stitch design tokens</name>
  <files>frontend/tailwind.config.ts, frontend/app/globals.css</files>
  <action>
Rewrite frontend/tailwind.config.ts to include the complete Stitch design token system while preserving backward compatibility with existing color names (background, secondary, accent, warning, destructive must still resolve to usable values — map them to the nearest Stitch equivalents).

Colors to add under theme.extend.colors as a flat namespace:
- primary: #00604e, on-primary: #ffffff, primary-container: #1e7a65, on-primary-container: #b1ffe6
- primary-fixed: #9ef3d9, primary-fixed-dim: #82d6bd
- secondary: #55615f (NOTE: existing secondary was #EAF7F3 — keep old value as secondary-legacy for migration, map secondary to Stitch value), on-secondary: #ffffff, secondary-container: #d9e5e2
- tertiary: #854034, on-tertiary: #ffffff, tertiary-container: #a3574a
- error: #ba1a1a, error-container: #ffdad6
- surface: #f8f9ff, surface-dim: #ccdbf3, surface-bright: #f8f9ff
- surface-container: #e6eeff, surface-container-low: #eff4ff, surface-container-high: #dce9ff
- background: #f8f9ff (replaces old #F8FCFF), on-background: #0d1c2e
- student-blue: #3B82F6, teacher-amber: #D97706, parent-purple: #8B5CF6, admin-slate: #334155
- outline: #6e7a75, outline-variant: #bec9c4
- accent: #00604e (alias to primary for backward compat), destructive: #ba1a1a (alias to error)
- warning: #F59E0B (keep as-is)

For backward compat, add secondary-legacy: #EAF7F3 so existing bg-secondary references can be migrated gradually.

Typography under theme.extend.fontSize — add Stitch scale alongside existing (keep old label/body/heading/display for compat):
- display-stitch: [48px, { lineHeight: "1.15", fontWeight: "700", letterSpacing: "-0.02em" }]
- headline-lg: [32px, { lineHeight: "1.25", fontWeight: "700" }]
- headline-md: [24px, { lineHeight: "1.3", fontWeight: "600" }]
- body-lg: [18px, { lineHeight: "1.6", fontWeight: "400" }]
- body-md: [16px, { lineHeight: "1.6", fontWeight: "400" }]
- label-md: [14px, { lineHeight: "1.5", fontWeight: "600" }]
- label-sm: [12px, { lineHeight: "1.5", fontWeight: "500" }]

Spacing under theme.extend.spacing:
- gutter: 24px, margin-desktop: 64px, margin-mobile: 20px

Add theme.extend.maxWidth:
- container-stitch: 1280px

Add theme.extend.borderRadius:
- card: 32px, hero: 48px, button: 16px (rounded-2xl equivalent but explicit)

Add theme.extend.fontFamily:
- sans: ["Plus Jakarta Sans", "Inter", "ui-sans-serif", "system-ui", "-apple-system", "BlinkMacSystemFont", "Segoe UI", "sans-serif"]

In frontend/app/globals.css:
- Add @import for Plus Jakarta Sans from Google Fonts (use @import url at top before @tailwind directives)
- Update CSS custom properties in :root to reflect new Stitch tokens (--background: #f8f9ff, --surface: #f8f9ff, --foreground: #0d1c2e, --primary: #00604e, --outline: #6e7a75, --outline-variant: #bec9c4)
- Keep body font-family fallback chain but add Plus Jakarta Sans first
- Keep all existing resets and utility styles unchanged
  </action>
  <verify>
    <automated>cd /mnt/d/BeYou/frontend && npx tailwindcss --content "./app/**/*.tsx" --config ./tailwind.config.ts 2>&amp;1 | head -5 && node -e "const c = require('./tailwind.config.ts'); console.log('has primary:', !!c)" 2>/dev/null || npx tsc --noEmit tailwind.config.ts 2>&amp;1 | head -10</automated>
  </verify>
  <done>tailwind.config.ts compiles without errors; contains all Stitch color tokens (primary, surface-container, student-blue, etc.), typography scale, spacing, border-radius, and font-family. globals.css imports Plus Jakarta Sans and declares updated CSS variables. Existing class names (bg-background, bg-secondary, text-accent) still resolve.</done>
</task>

<task type="auto">
  <name>Task 2: StitchCard component + update ui-primitives tokens</name>
  <files>frontend/components/stitch-card.tsx, frontend/components/ui-primitives.tsx</files>
  <action>
Create frontend/components/stitch-card.tsx exporting a StitchCard component using class-variance-authority (cva) already in dependencies.

StitchCard props interface:
- variant: "circular" | "rounded" (default "rounded")
- icon: ReactNode (a Lucide icon or any element)
- title: string
- description?: string
- ctaLabel?: string
- ctaHref?: string
- onCtaClick?: () => void
- className?: string

Variant styles:
- "rounded": outer container uses rounded-[32px] bg-surface-container p-6 with standard card shadow
- "circular": icon container is a circle (w-16 h-16 rounded-full bg-primary-container flex items-center justify-center text-on-primary-container), card itself is rounded-[32px]

Layout:
- Icon area at top (for circular: centered circle; for rounded: icon inline with subtle bg)
- title below icon, using text-headline-md font-semibold text-on-background
- description below title, using text-body-md text-on-background/70
- CTA button at bottom if ctaLabel provided: rounded-[16px] bg-primary text-on-primary px-6 py-3 font-semibold. If ctaHref, render as Link; if onCtaClick, render as button.

Use cva for variant styles, clsx + tailwind-merge (import from a cn util or inline) for className merging.

Also update frontend/components/ui-primitives.tsx:
- Replace hardcoded color values with new token classes where safe:
  - #D7EFE8 ring colors → ring-outline-variant
  - bg-secondary → bg-secondary-legacy (to keep visual parity until pages migrate)
  - text-accent → text-primary
- This is a light-touch update; do NOT break existing component APIs or visual output. Only swap internal class references to use the token names from the new config.
  </action>
  <verify>
    <automated>cd /mnt/d/BeYou/frontend && npx tsc --noEmit --strict src/../components/stitch-card.tsx 2>&amp;1 || npx tsc --noEmit 2>&amp;1 | grep -E "stitch-card|ui-primitives" | head -10</automated>
  </verify>
  <done>StitchCard component exists at frontend/components/stitch-card.tsx, exports StitchCard with circular and rounded variants, accepts icon/title/description/ctaLabel props. ui-primitives.tsx uses new token class names (ring-outline-variant, text-primary) instead of hardcoded hex values. Both files pass TypeScript type checking.</done>
</task>

<task type="auto">
  <name>Task 3: Extract navigation components + layout shell + wire into authenticated layout</name>
  <files>frontend/components/navigation/student-sidebar.tsx, frontend/components/navigation/mobile-bottom-nav.tsx, frontend/components/layout-shell.tsx, frontend/app/(authenticated)/layout.tsx</files>
  <action>
Extract navigation from the existing authenticated layout into dedicated components. Create the navigation directory first.

frontend/components/navigation/student-sidebar.tsx:
- Extract the aside element (lines 208-262 of current layout) into a standalone StudentSidebar component
- Props: pathname (string), collapsed (boolean), onToggleCollapse (() => void), onLogout (() => void)
- Import studentNav items array (move to a shared constant file or define inline)
- Use new design token classes: bg-surface-container-low instead of bg-white/90, border-outline-variant instead of border-[#D7EFE8], active item bg-primary text-on-primary instead of bg-accent text-white
- Keep all existing nav items (Dashboard, Chat AI, Self-checks, Mood check-ins, Scenarios, Support Plan, SOS, Settings, Logout)
- Keep Lucide icons, aria-labels, collapsible behavior
- Export studentNav array for reuse by mobile nav

frontend/components/navigation/mobile-bottom-nav.tsx:
- Create a fixed-bottom mobile navigation bar (visible on screens < lg, hidden on lg+)
- Render the top 5 studentNav items as icon-only buttons/links in a horizontal row
- Each item: icon (Lucide, size 20) + label text below (text-label-sm)
- Active state: text-primary, inactive: text-on-background/60
- Container: fixed bottom-0 left-0 right-0 z-40 bg-surface border-t border-outline-variant px-margin-mobile py-2 safe-area-inset padding (pb-[env(safe-area-inset-bottom)])
- Include a "More" button (ellipsis or Menu icon) if needed, or show all 6 items compactly
- This REPLACES the current horizontal scrolling nav bar on mobile (lines 264-302 of current layout)

frontend/components/layout-shell.tsx:
- Create a LayoutShell wrapper component that provides consistent page structure
- Props: children (ReactNode), className?: string
- Structure: max-w-container-stitch mx-auto px-margin-mobile lg:px-margin-desktop py-gutter
- This is a simple content wrapper for the main content area, used inside the authenticated layout

frontend/app/(authenticated)/layout.tsx:
- Import StudentSidebar, MobileBottomNav, LayoutShell
- Replace the inline aside block with StudentSidebar component
- Replace the inline mobile nav block with MobileBottomNav component
- Wrap the children content area with LayoutShell
- Keep ALL existing logic unchanged: auth check, privacy redirect, wrong role handling, demo banner, header, logout
- Update header classes to use new tokens: border-outline-variant, bg-surface
- The layout still owns state (user, collapsed, pathname) and passes to child components
  </action>
  <verify>
    <automated>cd /mnt/d/BeYou/frontend && npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <done>StudentSidebar renders desktop sidebar with collapsible behavior and all student nav items using design tokens. MobileBottomNav renders fixed bottom nav on mobile with icons and labels. LayoutShell provides consistent content wrapper. Authenticated layout imports and uses all three components, maintaining existing auth/privacy/routing logic. No TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is purely frontend UI/styling — no new data flows or API changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Spoofing | Navigation links | accept | All nav links are internal routes, authenticated layout already validates user role before rendering |
| T-39-02 | Tampering | Tailwind config | accept | Build-time only artifact, no runtime exposure |
</threat_model>

<verification>
1. Run `npx tsc --noEmit` in frontend/ — zero errors
2. Run `npx next build` in frontend/ — builds successfully
3. Visual check: student pages render with sidebar on desktop, bottom nav on mobile
4. StitchCard renders both variants without layout issues
</verification>

<success_criteria>
- Tailwind config contains all 30+ Stitch color tokens, 7 typography sizes, spacing values, and border-radius tokens
- StitchCard component exported with circular and rounded variants accepting icon, title, description, CTA
- StudentSidebar component renders on lg+ screens with all student nav items
- MobileBottomNav component renders fixed at bottom on mobile with icon + label for each nav item
- LayoutShell provides consistent max-width and padding wrapper
- Authenticated layout uses extracted components (no inline nav blocks)
- `next build` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/39-design-system-foundation/39-01-SUMMARY.md` when done
</output>

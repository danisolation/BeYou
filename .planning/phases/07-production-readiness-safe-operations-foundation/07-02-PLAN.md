# 07-02 Plan: Readiness Validation Tests

<objective>
Add regression tests proving readiness checks catch unsafe configuration, enforce admin-only detail access, validate database/migration health, and mask sensitive values.
</objective>

## Tasks

1. Add backend tests for public liveness/readiness shape.
2. Add backend tests for admin-only readiness access.
3. Add service-level tests for unsafe production config, insecure cookies/origins, demo seed, provider secret gaps, and secret masking.
4. Add a database/migration health assertion that is safe in the existing test environment.

## Acceptance

- Tests cover READY-01 through READY-06.
- Tests assert secret/database credential values are absent from readiness responses.
- Existing auth/privacy/report tests remain unaffected.


---
phase: 32-privacy-security-release-gates
plan: 04
type: execute
wave: 2
depends_on:
  - 32-02
files_modified:
  - README.md
autonomous: true
requirements:
  - QA-05
  - QA-06
must_haves:
  truths:
    - "README documents the exact local release-gate command set."
    - "README documents live `smoke:pilot` constraints when real pilot URL/configuration is absent."
    - "README privacy/release guidance rejects raw data, exports, destructive reset, risk leaderboards, and surveillance framing."
  artifacts:
    - path: "README.md"
      provides: "Operator release-gate command guidance and privacy grep guidance"
  key_links:
    - from: "README.md"
      to: "frontend/package.json"
      via: "documented `test:release-gates` script"
      pattern: "test:release-gates"
    - from: "README.md"
      to: "backend/tests/test_phase32_release_gates.py"
      via: "documented backend Phase 32 pytest gate"
      pattern: "test_phase32_release_gates.py"
---

<objective>
Update operator documentation with exact release-gate commands, docs/privacy grep gates, and live pilot smoke constraints.

Purpose: QA-06 requires broad local release gates and explicit accepted constraints; QA-05 requires docs/copy to preserve metadata-only support-not-surveillance boundaries.
Output: Updated `README.md`
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
@.planning/phases/32-privacy-security-release-gates/32-RESEARCH.md
@README.md
@frontend/package.json
@backend/pyproject.toml
@render.yaml
@.planning/phases/31-school-pilot-operations-safe-launch/31-VERIFICATION.md

<interfaces>
Documented commands must match actual scripts:
- `python -m pytest`
- `python -m ruff check .`
- `npm test`
- `npm run lint`
- `npm run build`
- `npm run test:deploy-guardrails`
- `npm run test:smoke-profiles`
- `npm run test:release-gates`
- `npm --prefix frontend run guard:deploy`
- `npm --prefix frontend run smoke:demo`
- `npm --prefix frontend run smoke:pilot`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact Phase 32 release-gate command matrix to README</name>
  <files>README.md</files>
  <read_first>
    README.md
    frontend/package.json
    backend/pyproject.toml
    .planning/REQUIREMENTS.md
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
    .planning/phases/32-privacy-security-release-gates/32-RESEARCH.md
  </read_first>
  <action>
    Add a concise README section titled exactly `## Privacy, security, and release gates` after the existing `## Verification` section. Include a markdown table with columns `Requirement`, `Gate`, and `Command`. Rows must map `QA-01` to `Set-Location D:\BeYou\backend; python -m pytest tests\test_phase32_release_gates.py tests\test_phase7_readiness.py tests\test_demo_seed.py -q`, `QA-02` to `Set-Location D:\BeYou\frontend; npm run test:deploy-guardrails; npm run test:smoke-profiles; npm run test:release-gates`, `QA-03` to backend auth pytest plus frontend `tests\phase32-release-gates-ui.test.tsx`, `QA-04` to backend reason/access pytest plus frontend phase23/phase24 tests, `QA-05` to backend/frontend Phase 32 privacy gates and README grep, and `QA-06` to full backend pytest/ruff plus frontend test/lint/build plus Node guard/smoke tests. Use exact command names; do not introduce a new framework per D-03.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path README.md -Pattern "Privacy, security, and release gates","QA-01","QA-02","QA-03","QA-04","QA-05","QA-06","test:release-gates","test_phase32_release_gates.py"</automated>
  </verify>
  <acceptance_criteria>
    - README contains heading `## Privacy, security, and release gates`.
    - README contains all QA IDs `QA-01` through `QA-06`.
    - README contains exact strings `npm run test:release-gates` and `tests\test_phase32_release_gates.py`.
  </acceptance_criteria>
  <done>README maps QA-01 through QA-06 to exact executable release-gate commands per D-02 and D-22.</done>
</task>

<task type="auto">
  <name>Task 2: Document live smoke:pilot constraints and deterministic substitutes</name>
  <files>README.md</files>
  <read_first>
    README.md
    frontend/scripts/pilot-smoke.mjs
    frontend/scripts/smoke-profiles.test.mjs
    frontend/scripts/release-gates.test.mjs
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <action>
    In the new README release-gates section, add a subsection titled exactly `### Live smoke constraints`. It must state: `smoke:pilot is constrained unless BEYOU_FRONTEND_URL, BEYOU_BACKEND_URL, NEXT_PUBLIC_API_BASE_URL, and a production_pilot deployment with /health/ready status ready are available.` It must also state that when those are absent, Phase 32 verification records the live pilot smoke as constrained and relies on deterministic local substitutes: `npm run test:smoke-profiles`, `npm run test:release-gates`, backend readiness/admin metadata tests, and `npm --prefix frontend run guard:deploy`. Do not require real student accounts, school domains, IdP credentials, or secrets per D-11 and D-12.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path README.md -Pattern "Live smoke constraints","smoke:pilot is constrained unless BEYOU_FRONTEND_URL, BEYOU_BACKEND_URL, NEXT_PUBLIC_API_BASE_URL, and a production_pilot deployment with /health/ready status ready are available","deterministic local substitutes","real student accounts","school domains","IdP credentials"</automated>
  </verify>
  <acceptance_criteria>
    - README contains the exact live smoke constraint sentence.
    - README names deterministic substitute commands `npm run test:smoke-profiles`, `npm run test:release-gates`, and `npm --prefix frontend run guard:deploy`.
    - README explicitly says real student accounts, school domains, IdP credentials, and secrets are not required for Phase 32 verification.
  </acceptance_criteria>
  <done>Live pilot smoke constraints are explicit and cannot be silently marked as passed per D-11 and D-23.</done>
</task>

<task type="auto">
  <name>Task 3: Add README privacy grep guidance for operations/readiness redlines</name>
  <files>README.md</files>
  <read_first>
    README.md
    .planning/PROJECT.md
    .planning/phases/31-school-pilot-operations-safe-launch/31-CONTEXT.md
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <action>
    In the new README release-gates section, add a subsection titled exactly `### Privacy grep gates`. Include a PowerShell `Select-String` command that checks safe required terms: `"metadata-only","support-not-surveillance","no raw exports","no destructive database reset","no risk leaderboards","no per-student drilldowns"`. Include a second command that searches app and docs surfaces for forbidden operational markers and instructs operators to investigate any match outside explicit rejection/warning copy: `"raw export","destructive reset","risk leaderboard","xếp hạng nguy cơ","per-student drilldown","provider_subject","raw_claims","private_note","sos_note","transcript","self_check_answer","scenario_answer","access_token","refresh_token","id_token"`. Keep wording support-oriented; do not add surveillance-shaped language per D-21.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path README.md -Pattern "Privacy grep gates","metadata-only","support-not-surveillance","no raw exports","no destructive database reset","no risk leaderboards","no per-student drilldowns","provider_subject","raw_claims","access_token","refresh_token","id_token"</automated>
  </verify>
  <acceptance_criteria>
    - README contains subsection `### Privacy grep gates`.
    - README contains safe required terms and forbidden marker list exactly as specified.
    - README does not instruct operators to create exports, destructive reset workflows, raw-data views, risk leaderboards, or per-student drilldowns.
  </acceptance_criteria>
  <done>README QA-05/QA-06 guidance preserves metadata-only operations and support-not-surveillance framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→operator action | README commands guide release decisions. |
| live environment absence→verification status | Missing pilot config can be mistaken for a pass. |
| docs copy→privacy posture | Documentation can normalize unsafe export/reset/surveillance behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-13 | R | README release matrix | mitigate | README maps every QA ID to exact commands and constrained states. |
| T-32-14 | I | README operator guidance | mitigate | Privacy grep gates explicitly reject raw data, secrets, exports, reset controls, and risk ranking markers. |
| T-32-15 | S | live smoke status | mitigate | README states `smoke:pilot` constraints and deterministic substitutes when real pilot env is absent. |
| T-32-16 | T | release command drift | mitigate | README command strings are verified against package scripts and backend pytest paths. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou; Select-String -Path README.md -Pattern "Privacy, security, and release gates","Live smoke constraints","Privacy grep gates","QA-01","QA-02","QA-03","QA-04","QA-05","QA-06"`
- `Set-Location D:\BeYou\frontend; npm run test:release-gates`
</verification>

<success_criteria>
README provides exact release-gate commands, live pilot smoke constraints, and privacy grep guidance with no new product behavior or unsafe operational defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/32-privacy-security-release-gates/32-04-SUMMARY.md`
</output>


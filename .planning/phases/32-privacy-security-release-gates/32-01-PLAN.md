---
phase: 32-privacy-security-release-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_phase32_release_gates.py
autonomous: true
requirements:
  - QA-01
  - QA-03
  - QA-04
  - QA-05
must_haves:
  truths:
    - "Backend release gates prove production-pilot readiness semantics remain strict."
    - "Backend release gates prove identity metadata and external identity contracts do not authorize adult visibility."
    - "Backend release gates prove operations/readiness responses remain metadata-only."
  artifacts:
    - path: "backend/tests/test_phase32_release_gates.py"
      provides: "Consolidated backend Phase 32 release-gate assertions for QA-01, QA-03, QA-04, QA-05"
  key_links:
    - from: "backend/tests/test_phase32_release_gates.py"
      to: "backend/app/services/readiness.py"
      via: "evaluate_static_readiness_checks assertions"
      pattern: "evaluate_static_readiness_checks"
    - from: "backend/tests/test_phase32_release_gates.py"
      to: "backend/app/services/admin_operations.py"
      via: "build_operations_dashboard metadata redline assertions"
      pattern: "build_operations_dashboard"
    - from: "backend/tests/test_phase32_release_gates.py"
      to: "backend/app/core/authorization.py"
      via: "require_permission app-owned authorization assertions"
      pattern: "require_permission"
---

<objective>
Create the backend Phase 32 release-gate matrix for runtime/readiness, identity/auth, cross-role privacy, and operations metadata boundaries.

Purpose: Phase 32 is a release candidate gate per D-01 and must prove existing production, identity, privacy, and operations behavior still holds without adding product behavior.
Output: `backend/tests/test_phase32_release_gates.py`
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
@.planning/phases/32-privacy-security-release-gates/32-RESEARCH.md
@backend/app/core/config.py
@backend/app/services/readiness.py
@backend/app/seeds/demo_seed.py
@backend/app/api/auth.py
@backend/app/api/me.py
@backend/app/core/sessions.py
@backend/app/core/authorization.py
@backend/app/services/admin_operations.py
@backend/tests/test_phase7_readiness.py
@backend/tests/test_demo_seed.py
@backend/tests/test_auth_privacy_portals.py
@backend/tests/test_phase24_reason_access.py
@backend/tests/test_phase25_admin_policy_operations.py
@backend/tests/test_phase31_school_pilot_operations.py

<interfaces>
Existing contracts the executor must use:
- `Settings(RUNTIME_MODE="local_demo" | "public_demo" | "production_pilot", ALLOW_DEMO_SEED=..., ALLOW_DEMO_LOGIN=...)`
- `evaluate_static_readiness_checks(settings) -> list[ReadinessCheck]`
- `seed_demo_data(db, settings) -> bool`
- `build_operations_dashboard(db, readiness_report, settings).model_dump_json()`
- `require_permission(db, actor, resource_type, action, purpose, student_id=...)`
- Auth/session metadata uses `AuthSessionMethod.PASSWORD`, `DEMO_PASSWORD`, and provider key strings; browser token storage is forbidden elsewhere.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QA-01 runtime/readiness and secret-masking backend gate</name>
  <files>backend/tests/test_phase32_release_gates.py</files>
  <read_first>
    backend/tests/test_phase7_readiness.py
    backend/tests/test_demo_seed.py
    backend/app/core/config.py
    backend/app/services/readiness.py
    backend/app/seeds/demo_seed.py
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - Test constant `PHASE32_BACKEND_REQUIREMENT_IDS` contains exactly `("QA-01", "QA-03", "QA-04", "QA-05")`.
    - Production-pilot settings with `ALLOW_DEMO_SEED=true` or `ALLOW_DEMO_LOGIN=true` produce failing `demo_seed_policy` and `demo_login_policy` checks per D-06.
    - Production-pilot safe settings with secure cookies, exact HTTPS origin, disabled demo seed/login, safe database URL, and safe provider metadata produce only pass static checks per D-06 and D-07.
    - Rendered readiness checks do not contain `DATABASE_URL`, `beyou_dev_password`, `FREEMODEL_API_KEY`, `SMTP_PASSWORD`, `SESSION_COOKIE_NAME`, `client_secret`, `access_token`, `refresh_token`, `id_token`, raw URL credentials, or `eyJ` token-like values per D-08.
    - `seed_demo_data` returns `False` and leaves `User`, `StudentAdultLink`, `SelfCheckTest`, `Scenario`, and `MoodCheckInConfig` empty when `RUNTIME_MODE=production_pilot` even if `ALLOW_DEMO_SEED=true` per D-06.
  </behavior>
  <action>
    Create `backend/tests/test_phase32_release_gates.py` using existing pytest fixture style from Phase 31. Include helpers named `_clean_phase32_database`, `_pilot_settings`, `_readiness`, and `_assert_forbidden_markers_absent`. Do not modify production files unless a test exposes a real regression. Use concrete forbidden marker tuple `PHASE32_FORBIDDEN_BACKEND_MARKERS` containing: `DATABASE_URL`, `beyou_dev_password`, `FREEMODEL_API_KEY`, `SMTP_PASSWORD`, `SESSION_COOKIE_NAME`, `client_secret`, `access_token`, `refresh_token`, `id_token`, `provider_subject`, `raw_claims`, `student.demo@beyou.local`, `private_note`, `sos_note`, `transcript`, `self_check_answer`, `scenario_answer`, `reason_text`, `student_id`, `recipient_id`, `export_url`, `risk_leaderboard`, `xếp hạng nguy cơ`. Implement QA-01 assertions per D-06, D-07, and D-08.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase32_release_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\backend\tests\test_phase32_release_gates.py -Pattern "QA-01","demo_seed_policy","demo_login_policy","evaluate_static_readiness_checks","seed_demo_data","PHASE32_FORBIDDEN_BACKEND_MARKERS"` finds all listed strings.
    - `Set-Location D:\BeYou\backend; python -m pytest tests\test_phase32_release_gates.py -q` exits 0.
  </acceptance_criteria>
  <done>QA-01 backend gate exists and proves production-pilot readiness, demo seed/login blocking, and secret masking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add QA-03 and QA-04 identity/cross-role privacy backend gate</name>
  <files>backend/tests/test_phase32_release_gates.py</files>
  <read_first>
    backend/tests/test_auth_privacy_portals.py
    backend/tests/test_phase24_reason_access.py
    backend/app/api/auth.py
    backend/app/core/sessions.py
    backend/app/core/authorization.py
    backend/app/db/models.py
    .planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - Provider readiness and auth capabilities expose safe booleans/labels/modes only, with no callback URL, issuer URL, token, raw claim, provider subject, client secret, or browser token contract per D-13 and D-15.
    - External identity metadata never broadens authorization; adult visibility remains app role + active relationship + student SOS per D-14 and D-16.
    - Disabled/deprovisioned/unlinked identity states are represented as denial or review metadata and must not create privileged access per D-13.
    - Privacy acknowledgement routing remains tied to `/api/auth/me` / `_login_response` response fields per D-13 and D-16.
  </behavior>
  <action>
    Extend `backend/tests/test_phase32_release_gates.py` with tests named `test_qa03_identity_contract_source_has_no_oauth_callback_or_browser_token_contract`, `test_qa03_auth_capabilities_and_session_metadata_are_safe`, and `test_qa04_identity_claims_do_not_bypass_adult_visibility`. Read source files as text inside tests where source-grep is the intended release gate: assert `backend/app/api/auth.py` does not define OAuth/OIDC callback, token exchange, `access_token`, `refresh_token`, `id_token`, `authorize_redirect`, or `callback_url`; assert `backend/app/core/authorization.py` contains no `ExternalIdentity`, `provider_subject`, `raw_claims`, `groups`, or `school` authorization grant path. Use database fixtures to create a student, linked adult, unlinked adult, and SOS/no-SOS states, then call `require_permission` to prove linked adult without SOS is denied and linked adult with SOS is allowed for `adult_support_summary` with purpose `support_not_surveillance`. Do not add OAuth/OIDC/SAML/SCIM implementation per D-15.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase32_release_gates.py tests\test_auth_privacy_portals.py tests\test_phase24_reason_access.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\backend\tests\test_phase32_release_gates.py -Pattern "QA-03","QA-04","require_permission","support_not_surveillance","privacy_acknowledgement_required","callback_url","access_token"` finds all listed gate markers.
    - Targeted pytest command exits 0.
  </acceptance_criteria>
  <done>QA-03 and QA-04 backend identity/privacy invariants are explicitly release-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add QA-05 operations/readiness metadata-only backend gate</name>
  <files>backend/tests/test_phase32_release_gates.py</files>
  <read_first>
    backend/tests/test_phase25_admin_policy_operations.py
    backend/tests/test_phase31_school_pilot_operations.py
    backend/app/services/admin_operations.py
    backend/app/schemas/admin_operations.py
    .planning/phases/31-school-pilot-operations-safe-launch/31-CONTEXT.md
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - Operations dashboard serialization rejects raw identifiers, emails, notes, transcripts, answers, SOS notes, free-text reasons, raw provider claims, provider subjects, exports, reset controls, risk leaderboards, and drilldown markers per D-19 and D-20.
    - Operations copy keeps support-not-surveillance framing and avoids student risk ranking language per D-21.
    - Admin operations may show aggregate counts, readiness status, checklist status, command names, and safe remediation only per D-04 and D-20.
  </behavior>
  <action>
    Extend `backend/tests/test_phase32_release_gates.py` with tests named `test_qa05_operations_dashboard_serialization_rejects_raw_sensitive_markers` and `test_qa05_operations_forbidden_metadata_keys_cover_phase32_redlines`. Seed audit metadata containing unsafe keys and values from D-17/D-19 (`student_email`, `student_id`, `private_note`, `sos_note`, `transcript`, `self_check_answer`, `scenario_answer`, `reason_text`, `provider_subject`, `raw_claims`, `export_url`, `risk_leaderboard`) and assert `build_operations_dashboard(...).model_dump_json()` omits or sanitizes them. Assert safe command strings remain present: `npm --prefix frontend run guard:deploy`, `npm --prefix frontend run smoke:demo`, `npm --prefix frontend run smoke:pilot`, `python -m pytest tests/test_phase31_school_pilot_operations.py -q`.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase32_release_gates.py tests\test_phase31_school_pilot_operations.py tests\test_phase25_admin_policy_operations.py -q; python -m ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\backend\tests\test_phase32_release_gates.py -Pattern "QA-05","build_operations_dashboard","guard:deploy","smoke:demo","smoke:pilot","risk_leaderboard"` finds all listed strings.
    - Targeted pytest and ruff commands exit 0.
  </acceptance_criteria>
  <done>QA-05 backend operations/readiness metadata-only release gate is explicit and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config→readiness | Deployment/runtime environment values become readiness output. |
| identity→authorization | External identity metadata must not become authorization input. |
| database→operations dashboard | Sensitive database fields and audit metadata become admin operations response candidates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | I | `readiness.py` / `admin_operations.py` | mitigate | Phase 32 tests assert forbidden secret/raw-data markers are absent from serialized readiness and operations output. |
| T-32-02 | E | `authorization.py` | mitigate | Phase 32 tests assert adult visibility remains app-owned role + active link + student SOS, not provider claims. |
| T-32-03 | T | `demo_seed.py` | mitigate | Phase 32 tests assert production-pilot demo seed invocation returns false and creates no demo rows. |
| T-32-04 | R | release gates | mitigate | Requirement constants and exact test names map QA-01/QA-03/QA-04/QA-05 to executable pytest gates. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_phase32_release_gates.py -q`
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_phase32_release_gates.py tests\test_phase31_school_pilot_operations.py tests\test_phase25_admin_policy_operations.py tests\test_auth_privacy_portals.py tests\test_phase24_reason_access.py tests\test_demo_seed.py -q`
- `Set-Location D:\BeYou\backend; python -m ruff check .`
</verification>

<success_criteria>
Backend release-gate tests pass, every assertion maps to QA-01/QA-03/QA-04/QA-05, and no production behavior is changed unless tests expose a real regression per D-05.
</success_criteria>

<output>
After completion, create `.planning/phases/32-privacy-security-release-gates/32-01-SUMMARY.md`
</output>


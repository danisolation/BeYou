# 08-01 Plan: Delivery Model and Backend Email Service

<objective>
Add backend-only SOS email configuration, delivery/outbox persistence, and provider abstraction for disabled, local outbox, and SMTP modes.
</objective>

## Tasks

1. Extend settings with SOS email provider and SMTP fields.
2. Add `SosNotificationDelivery` model and Alembic migration.
3. Add `sos_email` service for disabled/local outbox/SMTP behavior.
4. Add metadata-only delivery audit events.
5. Extend readiness checks for SOS email configuration state.

## Acceptance

- Default disabled mode preserves current SOS behavior.
- Local outbox creates queued metadata rows without real sends.
- SMTP mode records sent or failed metadata without leaking credentials.


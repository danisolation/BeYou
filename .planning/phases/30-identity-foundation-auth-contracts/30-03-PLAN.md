---
phase: 30-identity-foundation-auth-contracts
plan: 03
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - backend/app/core/config.py
  - backend/app/services/readiness.py
  - backend/app/schemas/admin_operations.py
  - backend/app/services/admin_operations.py
  - backend/tests/test_phase7_readiness.py
  - backend/tests/test_phase25_admin_policy_operations.py
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-04, IDENT-07]
must_haves:
  truths:
    - "Admin/operator can see auth provider readiness as safe metadata only."
    - "Operations dashboard shows identity mapping buckets and session auth buckets without raw identifiers."
    - "Readiness/admin operations never expose client IDs, secrets, issuer URLs, callback URLs, subjects, emails, claims, groups, or tokens."
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "Auth provider readiness settings"
      contains: "auth_provider_enabled"
    - path: "backend/app/schemas/admin_operations.py"
      provides: "Auth provider, identity mapping, and session auth response schemas"
      contains: "AuthProviderReadinessSummary"
    - path: "backend/app/services/admin_operations.py"
      provides: "Safe dashboard metadata builders"
      contains: "_auth_provider_summary"
  key_links:
    - from: "backend/app/services/readiness.py"
      to: "backend/app/core/config.py"
      via: "identity_configuration check uses auth provider settings"
      pattern: "auth_provider_enabled"
    - from: "backend/app/services/admin_operations.py"
      to: "backend/app/schemas/admin_operations.py"
      via: "AdminOperationsDashboardResponse fields"
      pattern: "auth_provider="
---

<objective>
Expose Phase 30 identity readiness and operations metadata safely from the backend.

Purpose: Operators need contract visibility for future SSO without receiving secrets, raw identifiers, exports, or drilldowns.
Output: Settings, readiness checks, admin operations response fields, and backend tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
@.planning/phases/30-identity-foundation-auth-contracts/30-RESEARCH.md
@backend/app/core/config.py
@backend/app/services/readiness.py
@backend/app/schemas/admin_operations.py
@backend/app/services/admin_operations.py
@backend/tests/test_phase7_readiness.py
@backend/tests/test_phase25_admin_policy_operations.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata-only auth provider readiness settings and checks</name>
  <files>backend/app/core/config.py, backend/app/services/readiness.py, backend/tests/test_phase7_readiness.py</files>
  <read_first>
    - backend/app/core/config.py
    - backend/app/services/readiness.py
    - backend/tests/test_phase7_readiness.py
    - .planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
    - .planning/phases/30-identity-foundation-auth-contracts/30-RESEARCH.md
  </read_first>
  <behavior>
    - Settings expose `AUTH_PROVIDER_ENABLED`, `AUTH_PROVIDER_KEY`, `AUTH_PROVIDER_LABEL`, `AUTH_PROVIDER_MODE`, and `AUTH_PROVIDER_LAST_CHECK_STATUS`.
    - `identity_configuration` remains safe and reflects provider readiness plus production-pilot demo-login safety.
    - Readiness output never contains raw provider domains, URLs, client IDs, secrets, claims, subjects, emails, or tokens.
  </behavior>
  <action>In `Settings`, add fields `auth_provider_enabled: bool = Field(default=False, validation_alias="AUTH_PROVIDER_ENABLED")`, `auth_provider_key: str = Field(default="disabled", validation_alias="AUTH_PROVIDER_KEY")`, `auth_provider_label: str = Field(default="Chưa cấu hình", validation_alias="AUTH_PROVIDER_LABEL")`, `auth_provider_mode: str = Field(default="disabled", validation_alias="AUTH_PROVIDER_MODE")`, and `auth_provider_last_check_status: str | None = Field(default=None, validation_alias="AUTH_PROVIDER_LAST_CHECK_STATUS")`. Add validators that normalize key/mode to lower snake-case-like safe strings and reject values containing `://`, `@`, `?`, `#`, `client_secret`, `access_token`, `refresh_token`, or `id_token`. In `evaluate_static_readiness_checks()`, replace the current simple `identity_configuration` check with: fail when production pilot allows demo login; pass when provider enabled has safe key/label/mode and production pilot demo login is disabled; warn when provider disabled outside production pilot; pass for local/public demo when provider disabled and demo login policy is safe. Summary/remediation must be safe strings only per D-01 through D-03. Extend `test_phase7_readiness.py` with safe enabled/disabled provider cases and forbidden substring checks.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase7_readiness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` contains exact validation aliases `AUTH_PROVIDER_ENABLED`, `AUTH_PROVIDER_KEY`, `AUTH_PROVIDER_LABEL`, `AUTH_PROVIDER_MODE`, `AUTH_PROVIDER_LAST_CHECK_STATUS`.
    - `backend/app/services/readiness.py` contains `key="identity_configuration"` and references `settings.auth_provider_enabled`.
    - `backend/tests/test_phase7_readiness.py` contains assertions for `auth_provider_enabled`, `identity_configuration`, and forbidden strings `client_secret`, `issuer_url`, `callback_url`, `access_token`, `refresh_token`, `id_token`.
    - Readiness tests assert those forbidden strings are not in rendered JSON.
  </acceptance_criteria>
  <done>Provider readiness is config-derived, metadata-only, and preserves production-pilot demo-login safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add operations auth provider, identity mapping, and session auth summaries</name>
  <files>backend/app/schemas/admin_operations.py, backend/app/services/admin_operations.py, backend/tests/test_phase25_admin_policy_operations.py</files>
  <read_first>
    - backend/app/schemas/admin_operations.py
    - backend/app/services/admin_operations.py
    - backend/tests/test_phase25_admin_policy_operations.py
    - backend/app/db/models.py
    - .planning/phases/30-identity-foundation-auth-contracts/30-UI-SPEC.md
  </read_first>
  <behavior>
    - Dashboard response includes `auth_provider`, `identity_mappings`, and `session_auth`.
    - Mapping counts are bucketed by status only.
    - Session auth counts are bucketed by safe auth method/provider only.
  </behavior>
  <action>In `admin_operations.py`, add Pydantic schemas exactly named `AuthProviderReadinessSummary`, `IdentityMappingOperationsSummary`, and `SessionAuthOperationsSummary` matching UI-SPEC: provider fields `enabled`, `provider_key`, `provider_label`, `mode`, `status`, `last_check_status`, `remediation`; identity fields `by_status`, `pending_review_count`, `disabled_count`, `deprovisioned_count`; session fields `by_auth_method`, `by_provider`. Add optional response fields to `AdminOperationsDashboardResponse`: `auth_provider: AuthProviderReadinessSummary | None = None`, `identity_mappings: IdentityMappingOperationsSummary | None = None`, `session_auth: SessionAuthOperationsSummary | None = None`. In `admin_operations.py` service, import `ExternalIdentity`, `ExternalIdentityStatus`, and `Session as UserSession`. Add forbidden sanitizer keys for `raw_subject`, `provider_subject`, `raw_email`, `raw_claims`, `groups`, `school`, `class_name`, `client_id`, `client_secret`, `issuer_url`, `callback_url`, `tenant_url`, `access_token`, `refresh_token`, `id_token`, and `password_hash`. Build `_auth_provider_summary(settings)`, `_identity_mapping_summary(db)`, and `_session_auth_summary(db)` using `OperationCountBucket` with safe labels; use `func.count()` group-by status/auth_method/auth_provider_key. Add privacy note `Danh tính ngoài chỉ được hiển thị bằng metadata tổng hợp. Quyền xem học sinh vẫn do vai trò trong ứng dụng, liên kết đang hoạt động và SOS của học sinh quyết định.` Return the three summaries from `build_operations_dashboard()` per D-18 through D-20.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase25_admin_policy_operations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/admin_operations.py` contains `class AuthProviderReadinessSummary`, `class IdentityMappingOperationsSummary`, and `class SessionAuthOperationsSummary`.
    - `AdminOperationsDashboardResponse` contains `auth_provider: AuthProviderReadinessSummary | None = None`, `identity_mappings: IdentityMappingOperationsSummary | None = None`, and `session_auth: SessionAuthOperationsSummary | None = None`.
    - `backend/app/services/admin_operations.py` contains `_auth_provider_summary`, `_identity_mapping_summary`, `_session_auth_summary`, and the exact Vietnamese privacy note from the action.
    - `backend/app/services/admin_operations.py` does not expose `provider_subject_hash` in response-building code.
    - Backend operations tests assert response text excludes `raw_subject`, `raw_email`, `client_secret`, `issuer_url`, `callback_url`, `access_token`, `refresh_token`, `id_token`, `password_hash`, and a fixture email address.
  </acceptance_criteria>
  <done>Admin operations returns metadata-only provider, mapping, and session auth summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Run backend identity/readiness regression gate</name>
  <files>backend/tests/test_phase7_readiness.py, backend/tests/test_phase25_admin_policy_operations.py</files>
  <read_first>
    - backend/tests/test_phase7_readiness.py
    - backend/tests/test_phase25_admin_policy_operations.py
    - backend/app/services/readiness.py
    - backend/app/services/admin_operations.py
  </read_first>
  <action>Run the full backend commands listed in this task. If a test fails because Phase 30 fields are missing from older fixtures, fix the backend schema/service code rather than removing Phase 30 assertions. Keep all outputs free of secret/raw identity strings listed in the privacy redlines.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase7_readiness.py tests\test_phase25_admin_policy_operations.py -q</automated>
    <automated>Set-Location D:\BeYou\backend; python -m ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - Pytest command exits with code 0.
    - Ruff command exits with code 0.
    - No test assertion is loosened to allow raw provider identifiers, emails, claims, groups, tokens, secrets, exports, or drilldowns.
  </acceptance_criteria>
  <done>Backend readiness and operations identity metadata regressions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend config→readiness API | Config-derived provider metadata becomes operator-visible readiness data. |
| database→admin operations API | Identity/session rows become aggregate operations metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | I | readiness provider check | mitigate | Validators reject URL/token-like provider settings; readiness summaries use status/label/key only. |
| T-30-03-02 | I | operations dashboard | mitigate | Aggregate buckets only; forbidden sanitizer keys block raw subject/email/claims/tokens/secrets. |
| T-30-03-03 | E | provider readiness | mitigate | Provider enabled/readiness metadata is informational only; no role/group authorization controls are added. |
| T-30-03-04 | D | production pilot demo login | mitigate | `identity_configuration` fails when production pilot allows demo login. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_phase7_readiness.py tests\test_phase25_admin_policy_operations.py -q`
- `Set-Location D:\BeYou\backend; python -m ruff check .`
</verification>

<success_criteria>
Backend readiness and admin operations expose Phase 30 auth metadata safely, with no raw identifiers, secrets, tokens, exports, or drilldowns.
</success_criteria>

<output>
After completion, create `.planning/phases/30-identity-foundation-auth-contracts/30-03-SUMMARY.md`
</output>

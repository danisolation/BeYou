---
phase: 30-identity-foundation-auth-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/models.py
  - backend/alembic/versions/20260525_0011_identity_foundation.py
  - backend/app/core/sessions.py
  - backend/app/api/auth.py
  - backend/tests/test_auth_privacy_portals.py
autonomous: true
requirements: [IDENT-02, IDENT-03, IDENT-04, IDENT-05]
must_haves:
  truths:
    - "Password and seeded demo login still issue backend-owned HttpOnly session cookies."
    - "Session rows record safe auth method/provider metadata without browser tokens."
    - "External identity storage exists as provider+subject-hash mapping, not raw claim storage."
  artifacts:
    - path: "backend/app/db/models.py"
      provides: "AuthSessionMethod, ExternalIdentityStatus, ExternalIdentity, and Session auth metadata"
      contains: "class ExternalIdentity"
    - path: "backend/alembic/versions/20260525_0011_identity_foundation.py"
      provides: "Alembic migration for session metadata and external_identities"
      contains: "external_identities"
    - path: "backend/app/core/sessions.py"
      provides: "create_session metadata parameters while preserving set_session_cookie"
      contains: "auth_method"
  key_links:
    - from: "backend/app/api/auth.py"
      to: "backend/app/core/sessions.py"
      via: "login calls create_session with password/demo_password metadata"
      pattern: "create_session\\(.*auth_method"
    - from: "backend/app/core/sessions.py"
      to: "backend/app/db/models.py"
      via: "UserSession columns auth_method/auth_provider_key/external_identity_id"
      pattern: "UserSession\\(.*auth_method"
---

<objective>
Create the backend identity/session storage foundation for future SSO while preserving current password and demo-login behavior.

Purpose: Phase 30 needs safe contracts before any OAuth/OIDC callback exists; this plan adds schema and session metadata only, with no browser token storage or provider redirect flow.
Output: SQLAlchemy model changes, Alembic migration, session creation metadata, and auth regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
@.planning/phases/30-identity-foundation-auth-contracts/30-RESEARCH.md
@backend/app/db/models.py
@backend/app/core/sessions.py
@backend/app/api/auth.py
@backend/tests/test_auth_privacy_portals.py

<interfaces>
Existing session entrypoint:
```python
def create_session(db: OrmSession, user: User, response: Response, settings: Settings) -> UserSession:
    ...
    set_session_cookie(response, token, settings)
```

Existing login convergence point:
```python
def _login_response(db: OrmSession, user: User) -> LoginResponse:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add identity/session schema and Alembic migration</name>
  <files>backend/app/db/models.py, backend/alembic/versions/20260525_0011_identity_foundation.py</files>
  <read_first>
    - backend/app/db/models.py
    - backend/alembic/versions/20260522_0010_v14_privacy_controls.py
    - .planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
    - .planning/phases/30-identity-foundation-auth-contracts/30-RESEARCH.md
  </read_first>
  <behavior>
    - Model has enum `AuthSessionMethod` values `password`, `demo_password`, `sso`.
    - Model has enum `ExternalIdentityStatus` values `linked`, `pending_review`, `disabled`, `deprovisioned`.
    - `ExternalIdentity` stores provider key and subject hash, never raw subject, raw email, raw claims, groups, school/class metadata, or tokens.
  </behavior>
  <action>Add `AuthSessionMethod` and `ExternalIdentityStatus` string enums in `backend/app/db/models.py` near existing auth enums. Extend `Session` with nullable safe metadata columns: `auth_method: Mapped[str | None] = mapped_column(String(32), nullable=True)`, `auth_provider_key: Mapped[str | None] = mapped_column(String(96), nullable=True)`, and `external_identity_id: Mapped[uuid.UUID | None] = mapped_column(ForeignKey("external_identities.id"), nullable=True, index=True)`. Add model `ExternalIdentity` with table name `external_identities`, columns `id`, `provider_key` String(96) not null index, `provider_subject_hash` String(128) not null, `linked_user_id` nullable FK to `users.id` index, `status` String(32) default `pending_review`, nullable safe fields `provider_label` String(160), `email_verified` Boolean, `email_hash` String(128), `display_label` String(160), timestamps `created_at`, `updated_at`, and nullable `last_seen_at`. Add unique constraint `uq_external_identities_provider_subject` on `(provider_key, provider_subject_hash)` and indexes for status and linked_user_id. Create Alembic revision `20260525_0011_identity_foundation.py` with revision `20260525_0011`, down_revision `20260522_0010`, `op.add_column` for the three session columns, `op.create_table("external_identities", ...)`, indexes, unique constraint, and downgrade reversing all changes. Do not add raw subject/email/claim/token columns per D-03, D-05, D-07.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m ruff check app\db\models.py alembic\versions\20260525_0011_identity_foundation.py</automated>
    <automated>Set-Location D:\BeYou\backend; python -m alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/db/models.py` contains exact strings `class AuthSessionMethod`, `PASSWORD = "password"`, `DEMO_PASSWORD = "demo_password"`, `SSO = "sso"`.
    - `backend/app/db/models.py` contains exact strings `class ExternalIdentityStatus`, `LINKED = "linked"`, `PENDING_REVIEW = "pending_review"`, `DISABLED = "disabled"`, `DEPROVISIONED = "deprovisioned"`.
    - `backend/app/db/models.py` contains `class ExternalIdentity(Base):` and `__tablename__ = "external_identities"`.
    - `backend/app/db/models.py` does not contain model fields named `raw_subject`, `raw_email`, `raw_claims`, `access_token`, `refresh_token`, or `id_token`.
    - Migration file contains `revision: str = "20260525_0011"` and `down_revision: str | None = "20260522_0010"`.
    - Migration file contains `op.create_table(`, `"external_identities"`, `auth_method`, `auth_provider_key`, and `external_identity_id`.
    - `python -m alembic upgrade head` applies the migration cleanly against the configured test/development database.
    - After migration, database schema contains table `external_identities`, unique constraint/index `uq_external_identities_provider_subject`, indexes for `provider_key`, `status`, and `linked_user_id`, plus `sessions.auth_method`, `sessions.auth_provider_key`, and `sessions.external_identity_id` columns.
  </acceptance_criteria>
  <done>Schema and migration define only safe identity/session metadata, apply cleanly, and are lint-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store safe auth method/provider metadata through existing session contract</name>
  <files>backend/app/core/sessions.py, backend/app/api/auth.py, backend/tests/test_auth_privacy_portals.py</files>
  <read_first>
    - backend/app/core/sessions.py
    - backend/app/api/auth.py
    - backend/tests/test_auth_privacy_portals.py
    - backend/app/db/models.py
    - .planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
  </read_first>
  <behavior>
    - Password login creates `auth_method="password"` and `auth_provider_key="local"`.
    - Demo password login creates `auth_method="demo_password"` and `auth_provider_key="local"`.
    - SSO-style test sessions can carry `auth_method="sso"` with safe provider metadata while still using the same backend session/user contract.
    - Revoked sessions, disabled/deleted users, privacy acknowledgement routing, `dashboard_route`, and `notice_version` behavior are shared across password, demo_password, and future SSO-marked sessions.
    - HttpOnly cookie behavior and `_login_response()` response shape remain unchanged.
  </behavior>
  <action>Update `create_session` signature to accept keyword-only parameters `auth_method: str = AuthSessionMethod.PASSWORD.value`, `auth_provider_key: str = "local"`, and `external_identity_id: uuid.UUID | None = None`. Store those values on `UserSession` and keep `set_session_cookie(response, token, settings)` exactly in the function so HttpOnly cookie behavior remains backend-owned per D-08 and D-09. In `backend/app/api/auth.py`, import `AuthSessionMethod` and call `create_session(..., auth_method=AuthSessionMethod.DEMO_PASSWORD.value if user.is_demo else AuthSessionMethod.PASSWORD.value, auth_provider_key="local")`. Do not add access-token, refresh-token, ID-token, localStorage, sessionStorage, or OAuth callback logic. Extend `backend/tests/test_auth_privacy_portals.py`: in password/demo login tests assert `sessions[0].auth_method` equals `password` or `demo_password`, `sessions[0].auth_provider_key == "local"`, and response JSON still includes `dashboard_route`, `privacy_acknowledgement_required`, and `notice_version` through `_login_response()` per D-10. Add explicit IDENT-05/D-16/D-17 regression tests in `backend/tests/test_auth_privacy_portals.py` that create password, demo_password, and SSO-style session rows with safe `auth_provider_key="pilot_sso"` metadata, then prove `get_current_user` denies revoked sessions, revokes/denies inactive or deleted account sessions, and keeps student `privacy_acknowledgement_required`, `dashboard_route`, and `notice_version` routing identical through `/api/auth/me` and `_login_response()` regardless of auth method. SSO-style metadata is test-only metadata in this phase; do not implement OAuth redirect/callback.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_auth_privacy_portals.py -q</automated>
    <automated>Set-Location D:\BeYou\backend; python -m ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/sessions.py` contains `auth_method: str = AuthSessionMethod.PASSWORD.value`.
    - `backend/app/core/sessions.py` contains `auth_provider_key: str = "local"`.
    - `backend/app/core/sessions.py` contains `external_identity_id: uuid.UUID | None = None`.
    - `backend/app/core/sessions.py` still contains `httponly=True` and `set_session_cookie(response, token, settings)`.
    - `backend/app/api/auth.py` contains `AuthSessionMethod.DEMO_PASSWORD.value if user.is_demo else AuthSessionMethod.PASSWORD.value`.
    - `backend/tests/test_auth_privacy_portals.py` contains assertions for `auth_method`, `auth_provider_key`, and `"local"`.
    - `backend/tests/test_auth_privacy_portals.py` contains explicit password, demo_password, and `sso`/`pilot_sso` session metadata cases proving shared `/api/auth/me` / `_login_response` fields: `privacy_acknowledgement_required`, `dashboard_route`, and `notice_version`.
    - `backend/tests/test_auth_privacy_portals.py` proves revoked sessions are denied, disabled/inactive/deleted users cannot keep active sessions, and affected sessions are revoked or rejected through `get_current_user` per D-16.
    - `backend/tests/test_auth_privacy_portals.py` proves student privacy acknowledgement routing remains mandatory for SSO-marked sessions per D-17.
    - `backend/app/api/auth.py` and `backend/app/core/sessions.py` do not contain `access_token`, `refresh_token`, `id_token`, `localStorage`, or `sessionStorage`.
  </acceptance_criteria>
  <done>Email/password, demo, and future SSO-marked sessions use one backend-owned session/user contract with shared status, revocation, privacy, and routing behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→auth API | Login request crosses from untrusted browser into backend session creation. |
| auth API→database | Session and external identity metadata are persisted server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | I | `create_session` / browser auth | mitigate | Keep `set_session_cookie(... httponly=True ...)`; do not return or store browser access/refresh/ID tokens. |
| T-30-01-02 | E | `ExternalIdentity` schema | mitigate | Store `provider_subject_hash` only; no raw subject/email/claims/groups fields. |
| T-30-01-03 | S | demo login | mitigate | Preserve existing production-pilot demo denial before `create_session` is called. |
| T-30-01-04 | T | migration drift | mitigate | Add explicit Alembic migration and run pytest/ruff before verification. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_auth_privacy_portals.py -q`
- `Set-Location D:\BeYou\backend; python -m alembic upgrade head`
- `Set-Location D:\BeYou\backend; python -m ruff check .`
</verification>

<success_criteria>
`ExternalIdentity` and session metadata are migrated; password/demo login still uses `_login_response()` and HttpOnly cookies; no browser tokens or raw provider identifiers are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/30-identity-foundation-auth-contracts/30-01-SUMMARY.md`
</output>

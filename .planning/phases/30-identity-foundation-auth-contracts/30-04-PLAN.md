---
phase: 30-identity-foundation-auth-contracts
plan: 04
type: execute
wave: 3
depends_on: [30-01, 30-03]
files_modified:
  - backend/app/schemas/auth.py
  - backend/app/api/auth.py
  - backend/tests/test_auth_privacy_portals.py
  - frontend/lib/auth.ts
  - frontend/app/login/page.tsx
  - frontend/tests/auth-portals.test.tsx
  - frontend/tests/no-token-storage.test.ts
autonomous: true
requirements: [IDENT-03, IDENT-04, IDENT-05]
must_haves:
  truths:
    - "Production pilot can hide/disable public demo entry through a public-safe auth capability contract."
    - "Login still submits email/password through backend-owned cookie sessions."
    - "Frontend does not read, write, or store access/refresh/ID tokens."
  artifacts:
    - path: "backend/app/schemas/auth.py"
      provides: "AuthCapabilitiesResponse schema"
      contains: "class AuthCapabilitiesResponse"
    - path: "backend/app/api/auth.py"
      provides: "GET /auth/capabilities"
      contains: "capabilities"
    - path: "frontend/app/login/page.tsx"
      provides: "Demo entry visibility/copy"
      contains: "Demo công khai đang tắt cho production pilot"
  key_links:
    - from: "frontend/lib/auth.ts"
      to: "/api/auth/capabilities"
      via: "getAuthCapabilities helper"
      pattern: "auth/capabilities"
    - from: "frontend/app/login/page.tsx"
      to: "frontend/lib/auth.ts"
      via: "capability fetch controls demo shortcuts"
      pattern: "getAuthCapabilities"
---

<objective>
Add a public-safe auth capability contract so production pilot can disable demo entry without exposing provider internals.

Purpose: Backend denial already protects demo accounts; this plan prevents misleading public demo UI in production pilot while preserving cookie-authenticated login and no-token frontend behavior.
Output: Safe capability endpoint, frontend helper, login UI copy, and frontend regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
@.planning/phases/30-identity-foundation-auth-contracts/30-UI-SPEC.md
@backend/app/api/auth.py
@backend/app/schemas/auth.py
@backend/tests/test_auth_privacy_portals.py
@frontend/lib/auth.ts
@frontend/app/login/page.tsx
@frontend/tests/auth-portals.test.tsx
@frontend/tests/no-token-storage.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend auth capabilities response without secrets</name>
  <files>backend/app/schemas/auth.py, backend/app/api/auth.py, backend/tests/test_auth_privacy_portals.py</files>
  <read_first>
    - backend/app/schemas/auth.py
    - backend/app/api/auth.py
    - backend/app/core/config.py
    - backend/tests/test_auth_privacy_portals.py
    - .planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
    - .planning/phases/30-identity-foundation-auth-contracts/30-UI-SPEC.md
  </read_first>
  <behavior>
    - GET `/api/auth/capabilities` returns only safe booleans/labels/modes.
    - Production pilot with demo login disabled returns `demo_login_enabled=false` and `public_demo_entry_enabled=false`.
    - Response does not include client IDs, secrets, issuer/callback URLs, raw domains, or tokens.
  </behavior>
  <action>In `backend/app/schemas/auth.py`, add `AuthCapabilitiesResponse` with fields `demo_login_enabled: bool`, `public_demo_entry_enabled: bool`, `email_password_enabled: bool = True`, `provider_login_enabled: bool`, `provider_label: str | None = None`, `provider_mode: str | None = None`, and `production_pilot: bool`. In `backend/app/api/auth.py`, import the schema and add `@router.get("/capabilities", response_model=AuthCapabilitiesResponse)` returning `demo_login_enabled=settings.allow_demo_login and settings.is_demo_runtime`, `public_demo_entry_enabled=settings.allow_demo_login and settings.is_demo_runtime`, `email_password_enabled=True`, `provider_login_enabled=settings.auth_provider_enabled`, `provider_label=settings.auth_provider_label if settings.auth_provider_enabled else None`, `provider_mode=settings.auth_provider_mode if settings.auth_provider_enabled else None`, `production_pilot=settings.is_production_pilot`. This plan depends on 30-03 because `settings.auth_provider_enabled`, `settings.auth_provider_label`, and `settings.auth_provider_mode` are introduced there. In `backend/tests/test_auth_privacy_portals.py`, add endpoint tests that call `/api/auth/capabilities` for demo-enabled runtime, production-pilot demo-disabled runtime, provider-enabled runtime, and provider-disabled runtime. Assert the response includes only public-safe booleans/labels/modes and never contains `provider_key`, `client_id`, `client_secret`, `issuer`, `issuer_url`, `callback`, `callback_url`, raw domains such as `school.example` or `login.school.edu`, emails, subjects, claims, tokens, cookie values, or password hashes. Do not include provider key, client ID, issuer URL, callback URL, raw domain, email, subject, claims, tokens, cookie values, or password hash. Do not add OAuth redirect buttons or callback handling.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_auth_privacy_portals.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/auth.py` contains `class AuthCapabilitiesResponse`.
    - `backend/app/api/auth.py` contains `@router.get("/capabilities", response_model=AuthCapabilitiesResponse)`.
    - `backend/app/api/auth.py` contains `public_demo_entry_enabled=settings.allow_demo_login and settings.is_demo_runtime`.
    - `backend/tests/test_auth_privacy_portals.py` contains `/api/auth/capabilities` tests for demo-enabled, production-pilot demo-disabled, provider-enabled, and provider-disabled cases.
    - Backend capability tests assert forbidden strings are absent from response JSON: `client_id`, `client_secret`, `issuer`, `issuer_url`, `callback`, `callback_url`, `school.example`, `login.school.edu`, `access_token`, `refresh_token`, `id_token`, cookie values, and `password_hash`.
    - `backend/app/api/auth.py` does not contain `client_id`, `client_secret`, `issuer_url`, `callback_url`, `access_token`, `refresh_token`, `id_token`, or `raw_subject`.
  </acceptance_criteria>
  <done>Backend exposes only public-safe auth capability metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hide production-pilot demo entry in login UI without token storage</name>
  <files>frontend/lib/auth.ts, frontend/app/login/page.tsx, frontend/tests/auth-portals.test.tsx, frontend/tests/no-token-storage.test.ts</files>
  <read_first>
    - frontend/lib/auth.ts
    - frontend/app/login/page.tsx
    - frontend/tests/auth-portals.test.tsx
    - frontend/tests/no-token-storage.test.ts
    - .planning/phases/30-identity-foundation-auth-contracts/30-UI-SPEC.md
  </read_first>
  <behavior>
    - Login page hides demo role shortcut buttons when capabilities say demo entry is disabled.
    - Login page shows exact copy `Demo công khai đang tắt cho production pilot. Hãy dùng tài khoản được cấp bởi quản trị viên.`
    - `apiFetch` still uses `credentials: "include"` and no token storage is introduced.
  </behavior>
  <action>In `frontend/lib/auth.ts`, export type `AuthCapabilities` matching backend fields and function `getAuthCapabilities(): Promise<AuthCapabilities>` that calls `apiFetch<AuthCapabilities>("/api/auth/capabilities")`. In `frontend/app/login/page.tsx`, fetch capabilities on mount. If loading or fetch fails, preserve current demo role shortcut behavior for backward compatibility. If `capabilities.public_demo_entry_enabled === false`, do not render the demo role shortcut buttons and render exact copy `Demo công khai đang tắt cho production pilot. Hãy dùng tài khoản được cấp bởi quản trị viên.` plus optional safe provider disabled copy `Nhà cung cấp đăng nhập ngoài chưa bật cho pilot.` only when `provider_login_enabled` is false. Keep the email/password form and submit button available. Do not add OAuth redirect buttons, localStorage/sessionStorage writes, access-token handling, raw provider keys, issuer URLs, client IDs, callback URLs, raw domains, or support contact emails. Extend frontend tests to mock `/api/auth/capabilities` for enabled/disabled cases and assert demo shortcuts are hidden only in disabled production pilot.</action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm run test -- --run tests\auth-portals.test.tsx</automated>
    <automated>Set-Location D:\BeYou\frontend; npm run test -- --run tests\no-token-storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/auth.ts` contains `export type AuthCapabilities` and `getAuthCapabilities`.
    - `frontend/app/login/page.tsx` contains exact copy `Demo công khai đang tắt cho production pilot. Hãy dùng tài khoản được cấp bởi quản trị viên.`
    - `frontend/app/login/page.tsx` contains no `localStorage`, `sessionStorage`, `access_token`, `refresh_token`, `id_token`, `issuer_url`, `callback_url`, or `client_id`.
    - `frontend/tests/auth-portals.test.tsx` asserts demo role buttons are not rendered when `public_demo_entry_enabled` is false.
    - `frontend/tests/no-token-storage.test.ts` still asserts no storage writes.
  </acceptance_criteria>
  <done>Login/demo entry respects safe capability metadata while preserving backend-owned cookie auth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser→auth capabilities | Public client receives safe auth capability metadata. |
| login UI→auth API | User credentials are submitted to backend-owned session endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | I | `/api/auth/capabilities` | mitigate | Return booleans and safe labels only; backend tests cover demo/provider modes and assert no provider keys, URLs, raw domains, secrets, tokens, cookie values, or password hashes. |
| T-30-04-02 | I | login frontend | mitigate | Tests assert no localStorage/sessionStorage token writes and no OAuth/access-token code. |
| T-30-04-03 | S | public demo entry | mitigate | Hide demo role shortcuts when backend capability disables public demo entry. |
| T-30-04-04 | E | future provider login | mitigate | Do not add OAuth buttons/callbacks or provider role/group controls in this phase. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_auth_privacy_portals.py -q`
- `Set-Location D:\BeYou\frontend; npm run test -- --run tests\auth-portals.test.tsx`
- `Set-Location D:\BeYou\frontend; npm run test -- --run tests\no-token-storage.test.ts`
- `Set-Location D:\BeYou\frontend; npm run lint`
</verification>

<success_criteria>
Production pilot can suppress public demo entry in UI with safe metadata; email/password login and no-token browser contract remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/30-identity-foundation-auth-contracts/30-04-SUMMARY.md`
</output>

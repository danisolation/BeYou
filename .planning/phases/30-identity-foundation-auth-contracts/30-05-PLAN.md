---
phase: 30-identity-foundation-auth-contracts
plan: 05
type: execute
wave: 3
depends_on: [30-03]
files_modified:
  - frontend/lib/admin-operations-api.ts
  - frontend/app/(authenticated)/admin/operations/page.tsx
  - frontend/tests/phase11-operations-ui.test.tsx
  - frontend/tests/phase25-admin-policy-operations-ui.test.tsx
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-04, IDENT-06, IDENT-07]
must_haves:
  truths:
    - "Admin operations UI renders auth provider readiness, identity mapping buckets, and session auth method buckets."
    - "UI remains backward-compatible when Phase 30 fields are absent or null."
    - "Rendered operations UI contains no raw identifiers, emails, claims, tokens, exports, drilldowns, or group-to-role controls."
  artifacts:
    - path: "frontend/lib/admin-operations-api.ts"
      provides: "Optional Phase 30 dashboard types"
      contains: "auth_provider?: AuthProviderReadinessSummary | null"
    - path: "frontend/app/(authenticated)/admin/operations/page.tsx"
      provides: "Operations panels"
      contains: "Auth provider readiness"
    - path: "frontend/tests/phase11-operations-ui.test.tsx"
      provides: "Operations UI regression coverage"
      contains: "Session vẫn dùng cookie HttpOnly"
  key_links:
    - from: "frontend/app/(authenticated)/admin/operations/page.tsx"
      to: "frontend/lib/admin-operations-api.ts"
      via: "typed optional dashboard fields and nullish fallbacks"
      pattern: "dashboard\\.session_auth\\?\\.by_auth_method \\?\\? \\[\\]"
---

<objective>
Render the Phase 30 identity/auth operations contract in the existing metadata-only admin operations UI.

Purpose: Operators need safe visibility into provider readiness, identity mapping buckets, and session auth methods without raw identity inspection or token handling.
Output: Frontend types, operations panels, and UI regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
@.planning/phases/30-identity-foundation-auth-contracts/30-UI-SPEC.md
@frontend/lib/admin-operations-api.ts
@frontend/app/(authenticated)/admin/operations/page.tsx
@frontend/tests/phase11-operations-ui.test.tsx
@frontend/tests/phase25-admin-policy-operations-ui.test.tsx

<interfaces>
Required optional fields:
```ts
auth_provider?: AuthProviderReadinessSummary | null;
identity_mappings?: IdentityMappingOperationsSummary | null;
session_auth?: SessionAuthOperationsSummary | null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional Phase 30 admin operations types</name>
  <files>frontend/lib/admin-operations-api.ts, frontend/tests/phase11-operations-ui.test.tsx</files>
  <read_first>
    - frontend/lib/admin-operations-api.ts
    - frontend/tests/phase11-operations-ui.test.tsx
    - .planning/phases/30-identity-foundation-auth-contracts/30-UI-SPEC.md
  </read_first>
  <behavior>
    - Type fields are optional/backward-compatible.
    - Existing older operations fixtures without Phase 30 fields still render.
    - `getAdminOperationsDashboard()` keeps cookie credentials.
  </behavior>
  <action>In `frontend/lib/admin-operations-api.ts`, add exported types `AuthProviderReadinessSummary`, `IdentityMappingOperationsSummary`, and `SessionAuthOperationsSummary` exactly matching UI-SPEC. Add optional fields to `AdminOperationsDashboard`: `auth_provider?: AuthProviderReadinessSummary | null`, `identity_mappings?: IdentityMappingOperationsSummary | null`, and `session_auth?: SessionAuthOperationsSummary | null`. Keep `getAdminOperationsDashboard()` unchanged except for types; it must continue using `apiFetch`, which uses `credentials: "include"`. Update existing operations test fixtures only where a Phase 30-specific test needs data; keep at least one existing fixture without the new fields to prove backward compatibility per D-20.</action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm run test -- --run tests\phase11-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/admin-operations-api.ts` contains `export type AuthProviderReadinessSummary`.
    - `frontend/lib/admin-operations-api.ts` contains exact strings `auth_provider?: AuthProviderReadinessSummary | null`, `identity_mappings?: IdentityMappingOperationsSummary | null`, and `session_auth?: SessionAuthOperationsSummary | null`.
    - `frontend/lib/admin-operations-api.ts` still contains `return apiFetch<AdminOperationsDashboard>`.
    - At least one operations UI test fixture omits `auth_provider`, `identity_mappings`, and `session_auth`.
  </acceptance_criteria>
  <done>Frontend operations API contract accepts Phase 30 fields without breaking older payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render provider readiness, mapping buckets, and session auth panels</name>
  <files>frontend/app/(authenticated)/admin/operations/page.tsx, frontend/tests/phase11-operations-ui.test.tsx, frontend/tests/phase25-admin-policy-operations-ui.test.tsx</files>
  <read_first>
    - frontend/app/(authenticated)/admin/operations/page.tsx
    - frontend/lib/admin-operations-api.ts
    - frontend/tests/phase11-operations-ui.test.tsx
    - frontend/tests/phase25-admin-policy-operations-ui.test.tsx
    - .planning/phases/30-identity-foundation-auth-contracts/30-UI-SPEC.md
  </read_first>
  <behavior>
    - Renders panels titled `Auth provider readiness`, `Identity mapping buckets`, and `Session auth methods`.
    - Uses existing `Panel`, `MetricCard`, `StatusBadge`, `BucketList`, and nullish fallback patterns.
    - Shows required Vietnamese copy from UI-SPEC.
  </behavior>
  <action>In `AdminOperationsPage`, import Phase 30 types if needed and insert the provider readiness panel near existing `Runtime mode` / `Connectivity & session contract` panels. Add an identity/auth grid using `grid gap-4 lg:grid-cols-2` with `Identity mapping buckets` and `Session auth methods`. Implement helper renderers in the same file using existing local components only; do not initialize shadcn or add third-party UI blocks. Provider panel must show `StatusBadge`, `provider_label`, `mode`, `enabled`, safe `provider_key` only if present, `last_check_status`, and `remediation`. Use the UI-SPEC description `Theo dõi cấu hình đăng nhập ngoài bằng metadata an toàn, không hiển thị client ID, issuer, callback URL hoặc secret.` and fallback copy including heading/body `Chưa có metadata danh tính.` and `Hãy kiểm tra cấu hình provider và tải lại trang vận hành.`. Identity mapping panel must use UI-SPEC description `Tóm tắt trạng thái liên kết danh tính theo count metadata; không có email, subject, claim hoặc drilldown tài khoản.`, `BucketList` for `dashboard.identity_mappings?.by_status ?? []`, metric/count rows for `pending_review_count`, `disabled_count`, `deprovisioned_count`, exact pending copy `Một số liên kết danh tính đang chờ duyệt. Không có tài khoản nào được tự động cấp quyền.`, and empty copy `Chưa có metadata liên kết danh tính. Không có tài khoản nào được tự động cấp quyền từ claim bên ngoài.`. Session auth panel must use UI-SPEC description `Tóm tắt session backend-owned theo phương thức đăng nhập và provider an toàn; không lưu token trong trình duyệt.`, `dashboard.session_auth?.by_auth_method ?? []` and `dashboard.session_auth?.by_provider ?? []`, titles `Phương thức đăng nhập` and `Provider`, empty copy `Chưa có metadata session theo phương thức đăng nhập.`, and exact explanatory copy `Session vẫn dùng cookie HttpOnly do backend sở hữu; UI không đọc hoặc lưu access token.`. Add privacy note text `Danh tính ngoài chỉ được hiển thị bằng metadata tổng hợp. Quyền xem học sinh vẫn do vai trò trong ứng dụng, liên kết đang hoạt động và SOS của học sinh quyết định.`. Use error copy `Chưa tải được metadata đăng nhập. Hãy thử lại từ cổng quản trị.` where Phase 30 identity metadata cannot load. Tests must assert all titles/copy render, old fixtures without fields do not throw, and rendered text excludes `client_secret`, `issuer_url`, `callback_url`, `raw_subject`, `raw_email`, `access_token`, `refresh_token`, `id_token`, `RAW_`, `export`, `drilldown`, and fixture email strings.</action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm run test -- --run tests\phase11-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx</automated>
    <automated>Set-Location D:\BeYou\frontend; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains exact strings `Auth provider readiness`, `Identity mapping buckets`, and `Session auth methods`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains all required Vietnamese copy strings from this task, including exact UI-SPEC strings `Theo dõi cấu hình đăng nhập ngoài bằng metadata an toàn, không hiển thị client ID, issuer, callback URL hoặc secret.`, `Tóm tắt trạng thái liên kết danh tính theo count metadata; không có email, subject, claim hoặc drilldown tài khoản.`, `Tóm tắt session backend-owned theo phương thức đăng nhập và provider an toàn; không lưu token trong trình duyệt.`, `Chưa có metadata danh tính.`, `Hãy kiểm tra cấu hình provider và tải lại trang vận hành.`, and `Chưa tải được metadata đăng nhập. Hãy thử lại từ cổng quản trị.`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains `dashboard.identity_mappings?.by_status ?? []`, `dashboard.session_auth?.by_auth_method ?? []`, and `dashboard.session_auth?.by_provider ?? []`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` does not contain `client_secret`, `issuer_url`, `callback_url`, `raw_subject`, `raw_email`, `access_token`, `refresh_token`, `id_token`, `Export`, `Xuất`, or `drilldown` outside negative test assertions.
    - UI tests assert no raw identity/token/export/drilldown strings appear in rendered text.
  </acceptance_criteria>
  <done>Admin operations UI displays Phase 30 identity/auth metadata safely and compatibly.</done>
</task>

<task type="auto">
  <name>Task 3: Run frontend identity operations regression gate</name>
  <files>frontend/tests/phase11-operations-ui.test.tsx, frontend/tests/phase25-admin-policy-operations-ui.test.tsx</files>
  <read_first>
    - frontend/tests/phase11-operations-ui.test.tsx
    - frontend/tests/phase25-admin-policy-operations-ui.test.tsx
    - frontend/app/(authenticated)/admin/operations/page.tsx
    - frontend/lib/admin-operations-api.ts
  </read_first>
  <action>Run the frontend test/lint/build commands for the operations UI. If older fixtures fail because new fields are required, fix types/renderers to make fields optional rather than adding mandatory fixture fields. Do not add browser token storage, raw JSON viewers, export buttons, user/student drilldown links, or group-to-role mapping controls.</action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm run test -- --run tests\phase11-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx</automated>
    <automated>Set-Location D:\BeYou\frontend; npm run lint</automated>
    <automated>Set-Location D:\BeYou\frontend; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Vitest command exits with code 0.
    - Lint command exits with code 0.
    - Build command exits with code 0.
    - No rendered Phase 30 UI contains raw provider identifiers, raw email, raw claims, groups, school/class metadata, tokens, exports, JSON viewers, or drilldowns.
  </acceptance_criteria>
  <done>Frontend Phase 30 operations UI passes tests, lint, and build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operations API→browser UI | Backend metadata becomes visible to authenticated admin/operator browser. |
| frontend render tree→user | UI copy and buckets must not create privileged controls or leak raw identity data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | I | operations UI | mitigate | Render only labels/counts/status; test forbidden raw identifiers, emails, claims, and tokens are absent. |
| T-30-05-02 | I | frontend auth/session | mitigate | Use existing `apiFetch` with credentials include; no token storage or token display. |
| T-30-05-03 | E | identity mapping UI | mitigate | No auto-provision controls, group-to-role mapping, user drilldowns, or provider claim controls. |
| T-30-05-04 | R | operator interpretation | mitigate | Copy states no automatic account privileges and app role + active relationship + SOS remain authoritative. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\frontend; npm run test -- --run tests\phase11-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx`
- `Set-Location D:\BeYou\frontend; npm run lint`
- `Set-Location D:\BeYou\frontend; npm run build`
</verification>

<success_criteria>
Admin operations UI safely renders provider readiness, identity mapping buckets, and session auth buckets with backward-compatible types and no forbidden leaks/controls.
</success_criteria>

<output>
After completion, create `.planning/phases/30-identity-foundation-auth-contracts/30-05-SUMMARY.md`
</output>

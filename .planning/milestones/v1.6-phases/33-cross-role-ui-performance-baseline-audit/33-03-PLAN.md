---
phase: "33-cross-role-ui-performance-baseline-audit"
plan: "03"
type: "execute"
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - ".planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md"
  - ".planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md"
  - "frontend/scripts/phase33-artifact-redline.test.mjs"
autonomous: true
requirements:
  - "UIC-01"
  - "BASE-01"
  - "BASE-02"
  - "BASE-03"
requirements_addressed:
  - "UIC-01"
  - "BASE-01"
  - "BASE-02"
  - "BASE-03"
must_haves:
  truths:
    - "Downstream executors can consume Phase 33 artifacts without private/raw evidence exposure."
    - "Every UI/performance finding has a concrete candidate follow-up phase."
    - "Public demo evidence and live production-pilot constraints are not conflated."
    - "High-severity privacy evidence exposure is blocked by automated redline tests."
  artifacts:
    - "frontend/scripts/phase33-artifact-redline.test.mjs provides automated artifact safety checks."
    - ".planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md contains finalized routing for UI findings."
    - ".planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md contains finalized routing for performance findings."
  key_links:
    - "Redline gate to both Phase 33 artifacts via direct file scans."
    - "UI inventory to Phases 34, 35, 37, and 38 via candidate routing."
    - "Performance baseline to Phases 36, 37, and 38 via candidate routing."
    - "Final verification commands to safe artifact readiness."
---

<objective>
Validate Phase 33 artifacts against privacy redlines and complete routing of all UI/performance findings to Phases 34 through 38.

Purpose: Ensure the audit/baseline artifacts are safe, actionable, and bounded before downstream UI/performance implementation phases consume them. This plan implements D-09 through D-13 and D-18 from 33-CONTEXT.md.

Output:
- Redline gate: `frontend/scripts/phase33-artifact-redline.test.mjs`
- Finalized `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md`
- Finalized `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md`

Strict boundaries:
- Do not require or create a UI-SPEC.
- Do not add schema push, ORM changes, migrations, indexes, production logging, external APM, analytics, UI redesigns, or performance fixes.
- Do not weaken privacy acknowledgement, backend-owned HttpOnly session behavior, no-browser-token storage, role checks, active relationship checks, adult reason gates, SOS-only adult visibility, audit metadata, or admin metadata-only operations.
- If redline evidence is found, sanitize the artifact and keep only aggregate/classification metadata. Do not preserve forbidden evidence elsewhere.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact privacy redline gate</name>
  <files>
    - `frontend/scripts/phase33-artifact-redline.test.mjs`
  </files>
  <read_first>
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-CONTEXT.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-RESEARCH.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md`
    - `frontend/package.json`
    - `frontend/scripts/release-gates.test.mjs`
  </read_first>
  <action>
    Create `frontend/scripts/phase33-artifact-redline.test.mjs` using Node's built-in `node:test` and `node:assert`.

    The test must scan:
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md`

    The test must fail on:
    - Email-shaped strings.
    - UUID-shaped raw identifiers.
    - JWT-shaped strings.
    - `Bearer ` tokens.
    - `Set-Cookie`, `Cookie:`, `localStorage`, or `sessionStorage` evidence.
    - Headings or table cells that indicate raw request/response bodies, such as `raw body`, `request body`, `response body`, `full payload`, or `body dump`.
    - Raw sensitive evidence labels: `private note value`, `transcript value`, `self-check answer value`, `provider claim value`, `free-text reason value`.
    - Product-surface redline terms in evidence rows: `risk leaderboard`, `per-student drilldown`, `raw audit browser`, `destructive reset control`.

    The test may allow forbidden terms only inside explicit policy sections named `## Privacy Redlines`, because those sections document what is forbidden. Even in policy sections, email/JWT/UUID/token/cookie regex matches must still fail.

    The test must assert that every artifact contains:
    - `Privacy Redlines`
    - `Candidate follow-up phase`
    - At least one downstream phase label.
    - No `TBD` in candidate follow-up phase cells.

    Do not add this as a package.json script unless required by existing project conventions. Verification can run the file directly with `node --test`.
  </action>
  <verify>
    <automated>cd frontend; node --test scripts/phase33-artifact-redline.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/scripts/phase33-artifact-redline.test.mjs` exists.
    - The test file references both Phase 33 artifact paths.
    - The test file contains regex or string checks for email-shaped strings, UUID-shaped identifiers, JWT-shaped strings, bearer tokens, cookies, browser storage, raw bodies, risk leaderboards, drilldowns, raw audit browsers, and destructive reset controls.
    - `cd frontend; node --test scripts/phase33-artifact-redline.test.mjs` exits with code 0.
    - No production frontend/backend code is modified by this task.
  </acceptance_criteria>
  <done>Task is done when `frontend/scripts/phase33-artifact-redline.test.mjs` exists, scans both Phase 33 artifacts, enforces forbidden evidence checks, and the node test exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize downstream routing in both artifacts</name>
  <files>
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md`
  </files>
  <read_first>
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-CONTEXT.md`
    - `.planning/ROADMAP.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md`
    - `frontend/scripts/phase33-artifact-redline.test.mjs`
  </read_first>
  <action>
    Update both Phase 33 artifacts so every finding row has a concrete `Candidate follow-up phase`.

    Allowed routing values:
    - `Phase 34` for shared UI primitives, shell, navigation, role guidance, reusable loading/error/empty patterns, accessibility primitive gaps, and cross-role visual rhythm.
    - `Phase 35` for role dashboard consistency gaps on Student, Teacher, Parent, or Admin dashboards.
    - `Phase 36` for backend/database hot path, query-count, N+1, unbounded list, hydration, broad-load, or metadata aggregation candidates.
    - `Phase 37` for frontend route waterfall, duplicate fetch, render, route/build, or perceived loading candidates.
    - `Phase 38` for final release gates, threshold decisions, post-optimization comparison evidence, or accepted production-pilot constraints.

    Add or update a `## Candidate Routing Queue` section in each artifact:
    - In `33-UI-INVENTORY.md`, group findings by `Phase 34`, `Phase 35`, `Phase 37`, and `Phase 38`.
    - In `33-PERFORMANCE-BASELINE.md`, group findings by `Phase 36`, `Phase 37`, and `Phase 38`.

    Preserve Phase 33 as audit/baseline only. Do not add instructions to fix, optimize, redesign, paginate, batch, cache, index, migrate, or refactor in Phase 33. Use language such as `candidate for Phase 36` rather than implementation instructions.

    If any artifact contains forbidden raw evidence, remove the raw evidence and replace it with safe classification metadata such as `sensitive field present`, `metadata-only count`, `payload byte count`, `status category`, or `query-count candidate`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ui='.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md'; $perf='.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md'; foreach ($p in @($ui,$perf)) { if (!(Test-Path $p)) { exit 1 }; $c=Get-Content $p -Raw; if ($c -notmatch '## Candidate Routing Queue') { exit 1 }; if ($c -match '\|\s*TBD\s*\|') { exit 1 }; if ($c -match '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}') { exit 1 } }; $u=Get-Content $ui -Raw; if ($u -notmatch 'Phase 34' -or $u -notmatch 'Phase 35' -or $u -notmatch 'Phase 37' -or $u -notmatch 'Phase 38') { exit 1 }; $b=Get-Content $perf -Raw; if ($b -notmatch 'Phase 36' -or $b -notmatch 'Phase 37' -or $b -notmatch 'Phase 38') { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - Both artifacts contain `## Candidate Routing Queue`.
    - `33-UI-INVENTORY.md` contains `Phase 34`, `Phase 35`, `Phase 37`, and `Phase 38`.
    - `33-PERFORMANCE-BASELINE.md` contains `Phase 36`, `Phase 37`, and `Phase 38`.
    - Neither artifact contains `| TBD |` as a candidate follow-up phase cell.
    - Neither artifact contains an email regex match.
    - Artifact language records candidates and routing only; it does not implement or instruct Phase 33 fixes.
  </acceptance_criteria>
  <done>Task is done when both artifacts contain `## Candidate Routing Queue`, allowed phase labels, no `TBD` candidate cells, and no forbidden raw/private evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 33 safety and baseline verification</name>
  <files>
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md`
    - `frontend/tests/phase33-ui-inventory.test.tsx`
    - `frontend/scripts/phase33-frontend-baseline.test.mjs`
    - `frontend/scripts/phase33-frontend-baseline.mjs`
    - `frontend/scripts/phase33-artifact-redline.test.mjs`
    - `backend/tests/test_phase33_performance_baseline.py`
  </files>
  <read_first>
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md`
    - `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md`
    - `frontend/tests/phase33-ui-inventory.test.tsx`
    - `frontend/scripts/phase33-frontend-baseline.test.mjs`
    - `frontend/scripts/phase33-frontend-baseline.mjs`
    - `frontend/scripts/phase33-artifact-redline.test.mjs`
    - `backend/tests/test_phase33_performance_baseline.py`
  </read_first>
  <action>
    Run the final Phase 33 verification commands and resolve only audit/baseline artifact or test-helper issues found by those commands.

    Required commands:
    - `npm --prefix frontend run test -- tests/phase33-ui-inventory.test.tsx`
    - `cd frontend; node --test scripts/phase33-frontend-baseline.test.mjs`
    - `cd frontend; node --test scripts/phase33-artifact-redline.test.mjs`
    - `cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase33_performance_baseline.py -q`

    If a command fails because a helper or artifact contains forbidden raw evidence, sanitize the helper/artifact by replacing raw values with aggregate/classification metadata.

    If a command reveals an actual UI drift, performance hotspot, or privacy risk, record or reroute it in the artifacts only. Do not fix production UI/backend/frontend performance behavior in Phase 33.

    Confirm no schema-relevant ORM files were modified. Do not run schema push.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix frontend run test -- tests/phase33-ui-inventory.test.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Push-Location frontend; node --test scripts/phase33-frontend-baseline.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test scripts/phase33-artifact-redline.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Pop-Location; Push-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase33_performance_baseline.py -q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Pop-Location"</automated>
  </verify>
  <acceptance_criteria>
    - All four required commands exit with code 0.
    - Both primary artifacts exist.
    - `git diff --name-only` shows no modified files under `backend/alembic/versions/`.
    - `git diff --name-only` shows no modified schema/model files unless they are unrelated pre-existing changes; Phase 33 must not introduce schema-relevant changes.
    - Production UI/performance findings are documented and routed, not fixed, in this phase.
  </acceptance_criteria>
  <done>Task is done when all four final verification commands run fail-fast and exit 0, both artifacts exist, no schema/migration files are modified by Phase 33, and findings remain documented/routed instead of fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated artifacts to downstream phases | Phase 34-38 executors will consume Phase 33 findings and may act on unsafe or misleading evidence if not gated. |
| Artifact content to repository history | Planning artifacts are committed and must not contain raw sensitive evidence. |
| Redline gate to audit/baseline workflow | Automated checks decide whether artifacts are safe enough to proceed. |
| Routing queue to implementation plans | Candidate routing must not accidentally authorize Phase 33 fixes or privacy boundary changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-03-01 | Information Disclosure | `33-UI-INVENTORY.md`, `33-PERFORMANCE-BASELINE.md` | mitigate | Redline gate rejects email-shaped strings, UUID-shaped IDs, JWTs, bearer tokens, cookies, browser storage evidence, raw bodies, and sensitive raw evidence labels. |
| T-33-03-02 | Tampering | Candidate routing queues | mitigate | Restrict routing values to Phases 34-38 and require no `TBD` candidate phase cells. |
| T-33-03-03 | Repudiation | Final baseline evidence | mitigate | Require commands/source labels and final verification commands before summary creation. |
| T-33-03-04 | Elevation of Privilege | Performance or UI recommendations | mitigate | Artifact language must preserve privacy gates, role checks, adult reason gates, SOS-only adult visibility, audit metadata, admin metadata-only operations, and no browser token storage. |
| T-33-03-05 | Denial of Service | Redline/final verification | accept | Checks are local, bounded, and artifact/test-helper scoped; no production services are stressed. |
| T-33-03-06 | Spoofing | Environment evidence labels | mitigate | Final artifacts must distinguish local deterministic, public demo, Render/Vercel cold/warm, and unavailable live production-pilot constraints; public demo cannot be labeled as pilot proof. |
</threat_model>

<verification>
Run:
- `npm --prefix frontend run test -- tests/phase33-ui-inventory.test.tsx`
- `cd frontend; node --test scripts/phase33-frontend-baseline.test.mjs`
- `cd frontend; node --test scripts/phase33-artifact-redline.test.mjs`
- `cd backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase33_performance_baseline.py -q`

Then inspect:
- `git diff --name-only`
- Confirm only Phase 33 artifacts and test-side helper/script/test files are changed.
- Confirm no schema push is run and no schema-relevant ORM/migration files are changed.
</verification>

<success_criteria>
- UIC-01 findings are safe, routed, and preserved in `33-UI-INVENTORY.md`.
- BASE-01, BASE-02, and BASE-03 findings are safe, routed, and preserved in `33-PERFORMANCE-BASELINE.md`.
- Redline gate passes and blocks forbidden raw/private evidence.
- Every finding has a candidate follow-up phase from Phase 34 through Phase 38.
- Phase 33 remains audit/baseline only and does not implement UI or performance fixes.
</success_criteria>

<must_haves>
Truths:
- Downstream executors can consume Phase 33 artifacts without private/raw evidence exposure.
- Every UI/performance finding has a concrete candidate follow-up phase.
- Public demo evidence and live production-pilot constraints are not conflated.
- High-severity privacy evidence exposure is blocked by automated redline tests.

Artifacts:
- `frontend/scripts/phase33-artifact-redline.test.mjs` provides automated artifact safety checks.
- `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-UI-INVENTORY.md` contains finalized routing for UI findings.
- `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md` contains finalized routing for performance findings.

Key links:
- Redline gate to both Phase 33 artifacts via direct file scans.
- UI inventory to Phases 34, 35, 37, and 38 via candidate routing.
- Performance baseline to Phases 36, 37, and 38 via candidate routing.
- Final verification commands to safe artifact readiness.
</must_haves>

<output>
After completion, create `.planning/phases/33-cross-role-ui-performance-baseline-audit/33-03-SUMMARY.md`.
</output>

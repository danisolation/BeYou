---
phase: 35-role-dashboard-consistency-pass
plan: 04
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - frontend/app/(authenticated)/admin/page.tsx
autonomous: true
requirements:
  - ROLE-04
must_haves:
  truths:
    - "Admin dashboard feels visually aligned with Peerlight AI dashboard rhythm."
    - "Admin surfaces remain metadata-only."
    - "No raw exports, risk leaderboards, per-student drilldowns, raw audit browsers, or destructive reset controls are added."
  artifacts:
    - path: "frontend/app/(authenticated)/admin/page.tsx"
      provides: "Admin metadata-only dashboard consistency pass"
  key_links:
    - from: "frontend/app/(authenticated)/admin/page.tsx"
      to: "/api/admin/users"
      via: "existing apiFetch count"
      pattern: "/api/admin/users"
    - from: "frontend/app/(authenticated)/admin/page.tsx"
      to: "/api/admin/links"
      via: "existing apiFetch count"
      pattern: "/api/admin/links"
---

<objective>
Harmonize Admin dashboard presentation while preserving metadata-only operations.

Purpose: Satisfy ROLE-04 without backend/API/performance/data-loading changes.
Output: Admin dashboard has explicit metadata-only boundary and safe entry-card rhythm.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/35-role-dashboard-consistency-pass/35-CONTEXT.md
@.planning/phases/35-role-dashboard-consistency-pass/35-UI-SPEC.md
@frontend/app/(authenticated)/admin/page.tsx
@frontend/components/ui-primitives.tsx
@frontend/tests/phase32-release-gates-ui.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Admin metadata-only boundary surface</name>
  <files>frontend/app/(authenticated)/admin/page.tsx</files>
  <read_first>
    frontend/app/(authenticated)/admin/page.tsx
    frontend/components/ui-primitives.tsx
    .planning/phases/35-role-dashboard-consistency-pass/35-UI-SPEC.md
    frontend/tests/phase32-release-gates-ui.test.tsx
  </read_first>
  <behavior>
    - Admin page shows `Vai trò quản trị`.
    - Admin page shows metadata-only operations copy near the top.
    - Existing `/api/admin/users` and `/api/admin/links` count paths remain unchanged.
  </behavior>
  <action>
    In `frontend/app/(authenticated)/admin/page.tsx`, add `PrivacyBoundaryCard` to the import from `@/components/ui-primitives`. Immediately after `PageHeader`, add `PrivacyBoundaryCard` with title `Vận hành metadata-only` and description `Cổng quản trị chỉ hiển thị metadata vận hành, cấu hình và số lượng tổng hợp; không mở câu trả lời tự kiểm tra, ghi chú riêng tư, transcript chat, request body, provider claim hoặc lý do truy cập dạng tự do.`. Keep the current `PageHeader` eyebrow `Vai trò quản trị`. Do not change `Promise.all`, `/api/admin/users`, `/api/admin/links`, `apiFetch`, loading, or error behavior per D-03/D-15.
  </action>
  <acceptance_criteria>
    - `grep -n "PrivacyBoundaryCard" frontend/app/(authenticated)/admin/page.tsx` finds import and usage.
    - `grep -n "Vận hành metadata-only" frontend/app/(authenticated)/admin/page.tsx` finds boundary copy.
    - `grep -n "provider claim" frontend/app/(authenticated)/admin/page.tsx` finds raw provider claim redline copy.
    - `grep -n "/api/admin/users" frontend/app/(authenticated)/admin/page.tsx` still finds existing API path.
    - `grep -n "/api/admin/links" frontend/app/(authenticated)/admin/page.tsx` still finds existing API path.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase35-role-dashboard-consistency.test.tsx tests/phase32-release-gates-ui.test.tsx</automated>
  </verify>
  <done>Admin dashboard exposes explicit metadata-only boundary without changing data fetching.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten Admin entry-card action language and unsafe-control redlines</name>
  <files>frontend/app/(authenticated)/admin/page.tsx</files>
  <read_first>
    frontend/app/(authenticated)/admin/page.tsx
    .planning/phases/35-role-dashboard-consistency-pass/35-CONTEXT.md
    .planning/phases/35-role-dashboard-consistency-pass/35-UI-SPEC.md
  </read_first>
  <behavior>
    - Admin entry cards use metadata-safe action language.
    - Dashboard does not add raw exports, risk leaderboards, per-student drilldowns, raw audit browsers, or destructive reset controls.
  </behavior>
  <action>
    Update `AdminEntryCard` to accept optional `actionLabel?: string` with default `Mở bảng metadata`. For the operations card, pass `actionLabel="Mở bảng vận hành metadata"`. For all other Admin cards, use default `Mở bảng metadata`. Keep route destinations exactly `/admin/operations`, `/admin/reports`, `/admin/chatbot`, `/admin/mood-checkins`, `/admin/content`, `/admin/privacy-policy`, `/admin/users`, `/admin/links`. Do not add links or buttons containing `Export`, `Xuất`, `Download`, `Tải xuống`, `reset`, `drilldown`, `risk leaderboard`, `xếp hạng nguy cơ`, `Chi tiết học sinh`, or `raw audit`.
  </action>
  <acceptance_criteria>
    - `grep -n "actionLabel" frontend/app/(authenticated)/admin/page.tsx` finds prop definition and usage.
    - `grep -n "Mở bảng vận hành metadata" frontend/app/(authenticated)/admin/page.tsx` finds operations CTA.
    - `grep -n "Mở bảng metadata" frontend/app/(authenticated)/admin/page.tsx` finds default CTA.
    - `grep -En "Export|Xuất|Download|Tải xuống|reset|drilldown|risk leaderboard|xếp hạng nguy cơ|Chi tiết học sinh|raw audit" frontend/app/(authenticated)/admin/page.tsx` returns no matches except if the string appears inside an explicit negative test comment; do not add such comments to this production file.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase35-role-dashboard-consistency.test.tsx tests/phase34-final-regression.test.tsx tests/phase32-release-gates-ui.test.tsx</automated>
  </verify>
  <done>Admin entry cards are harmonized and metadata-only without unsafe controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Admin dashboard → admin APIs | Existing count data comes from `/api/admin/users` and `/api/admin/links`. |
| Admin UI → operations surfaces | Dashboard links must not introduce unsafe raw-data controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-35-04-01 | Information Disclosure | Admin dashboard copy | mitigate | Add explicit metadata-only `PrivacyBoundaryCard` forbidding raw private content/provider/request/reason data. |
| T-35-04-02 | Tampering / Elevation | Admin controls | mitigate | Do not add destructive reset/export/drilldown/leaderboard controls; tests reject unsafe labels. |
| T-35-04-03 | Information Disclosure | browser storage | mitigate | Do not touch auth/session; tests reject token storage regressions. |
| T-35-04-04 | Safety UX | load failures | mitigate | Preserve `ErrorState` on count load failure; no zero-count success UI on failure. |
</threat_model>

<verification>
Run:
`npm --prefix frontend run test -- tests/phase35-role-dashboard-consistency.test.tsx tests/phase34-final-regression.test.tsx tests/phase32-release-gates-ui.test.tsx`
</verification>

<success_criteria>
ROLE-04 is satisfied: Admin dashboard uses harmonized metadata-only panels without unsafe controls.
</success_criteria>

<output>
After completion, create `.planning/phases/35-role-dashboard-consistency-pass/35-04-SUMMARY.md`
</output>

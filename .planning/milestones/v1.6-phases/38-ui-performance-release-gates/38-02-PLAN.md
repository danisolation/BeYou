---
phase: 38-ui-performance-release-gates
plan: 02
title: Frontend Release Gates
subsystem: frontend
estimated_minutes: 15
tasks:
  - id: t1
    title: Run full frontend test suite
    description: Execute `npm --prefix frontend run test`. Record pass counts.
  - id: t2
    title: Run frontend lint and production build
    description: Execute `npm --prefix frontend run lint` and `npm --prefix frontend run build`. Record status.
  - id: t3
    title: Run Node deploy/smoke/baseline tests
    description: Execute `cd frontend && node --test scripts/phase33-frontend-baseline.test.mjs` and `npm --prefix frontend run guard:deploy`.
  - id: t4
    title: Record QA-02 evidence
    description: Append frontend gate results to 38-RELEASE-EVIDENCE.md.
dependencies: [38-01]
success_criteria:
  - Frontend tests pass with recorded count
  - Lint passes
  - Build passes
  - Deploy guards pass
  - QA-02 evidence recorded
---

# Plan 38-02: Frontend Release Gates

Run full frontend test suite, lint, build, and deploy guards. Record evidence for QA-02.

## Tasks

### Task 1: Run full frontend test suite
Execute `npm --prefix frontend run test`. Record pass counts.

### Task 2: Run frontend lint and production build
Execute `npm --prefix frontend run lint` and `npm --prefix frontend run build`. Record build status.

### Task 3: Run Node deploy/smoke/baseline tests
Execute deploy guards and baseline helper tests. Record status.

### Task 4: Record QA-02 evidence
Append frontend gate results to 38-RELEASE-EVIDENCE.md.

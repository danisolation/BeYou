---
phase: 38-ui-performance-release-gates
plan: 01
title: Backend Release Gates
subsystem: backend
estimated_minutes: 10
tasks:
  - id: t1
    title: Run full backend test suite and lint
    description: Execute `cd backend && python -m pytest` and `cd backend && ruff check .`. Record pass counts.
  - id: t2
    title: Verify Alembic schema alignment
    description: Run `cd backend && alembic check` to confirm no pending migrations.
  - id: t3
    title: Record QA-01 evidence
    description: Append backend gate results to 38-RELEASE-EVIDENCE.md.
dependencies: []
success_criteria:
  - Backend tests pass with recorded count
  - Ruff lint passes
  - Alembic check passes
  - QA-01 evidence recorded
---

# Plan 38-01: Backend Release Gates

Run full backend test suite, lint, and schema check. Record evidence for QA-01.

## Tasks

### Task 1: Run full backend test suite and lint
Execute `cd backend && python -m pytest` and `cd backend && ruff check .`. Record pass counts.

### Task 2: Verify Alembic schema alignment
Run `cd backend && alembic check` to confirm no pending migrations or new upgrade operations.

### Task 3: Record QA-01 evidence
Create `38-RELEASE-EVIDENCE.md` with backend gate results table.

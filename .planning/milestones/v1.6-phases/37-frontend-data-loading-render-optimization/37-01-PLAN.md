---
phase: 37-frontend-data-loading-render-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/dashboard-loading.ts
  - frontend/lib/sos-api.ts
  - frontend/lib/notification-preferences-api.ts
  - frontend/lib/admin-api.ts
  - frontend/scripts/phase33-frontend-baseline.mjs
  - frontend/scripts/phase33-frontend-baseline.test.mjs
  - frontend/tests/phase37-dashboard-loading.test.tsx
autonomous: true
requirements:
  - FEPERF-01
  - FEPERF-03
  - FEPERF-04
  - FEPERF-05
must_haves:
  truths:
    - "Dashboard read helpers preserve cookie credentials and add explicit cache: no-store without changing mutation helpers."
    - "Frontend route evidence counts generic apiFetch<T>(...), dashboardRead<T>(...), fetch(...), and imported helper request calls."
    - "No browser token/API response storage or broad React Query provider is introduced."
  artifacts:
    - path: "frontend/lib/dashboard-loading.ts"
      provides: "Neutral dashboard read and optional-result helpers"
    - path: "frontend/scripts/phase33-frontend-baseline.mjs"
      provides: "Aggregate-only frontend request/build evidence helper updated for Phase 37"
    - path: "frontend/tests/phase37-dashboard-loading.test.tsx"
      provides: "Focused helper/no-store/browser-storage regression coverage"
  key_links:
    - from: "frontend/lib/dashboard-loading.ts"
      to: "frontend/lib/api.ts"
      via: "dashboardRead delegates to apiFetch with cache: no-store"
      pattern: "cache: \"no-store\""
---

<objective>
Create the Phase 37 dashboard loading foundation.

Purpose: Make dashboard reads explicitly no-store, keep cookie credentials through `apiFetch`, provide a typed optional-result pattern for scoped failures, and make aggregate frontend request/build evidence accurate enough for Phase 37.
Output: Neutral dashboard loading helper, read-client updates, static evidence helper update, and focused tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-frontend-data-loading-render-optimization/37-CONTEXT.md
@.planning/phases/37-frontend-data-loading-render-optimization/37-RESEARCH.md
@.planning/phases/37-frontend-data-loading-render-optimization/37-UI-SPEC.md
@frontend/lib/api.ts
@frontend/lib/sos-api.ts
@frontend/lib/notification-preferences-api.ts
@frontend/lib/admin-api.ts
@frontend/scripts/phase33-frontend-baseline.mjs
@frontend/scripts/phase33-frontend-baseline.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add neutral no-store dashboard read helpers and tests</name>
  <files>frontend/lib/dashboard-loading.ts, frontend/tests/phase37-dashboard-loading.test.tsx</files>
  <read_first>
    frontend/lib/api.ts
    frontend/lib/sos-api.ts
    frontend/lib/notification-preferences-api.ts
    frontend/lib/admin-api.ts
    frontend/tests/phase35-role-dashboard-consistency.test.tsx
    frontend/tests/phase34-final-regression.test.tsx
    .planning/phases/37-frontend-data-loading-render-optimization/37-CONTEXT.md
    .planning/phases/37-frontend-data-loading-render-optimization/37-UI-SPEC.md
  </read_first>
  <action>
    Create `frontend/lib/dashboard-loading.ts` exporting these exact names: `DASHBOARD_READ_INIT`, `dashboardRead`, `OptionalDashboardResult`, `readyDashboardResult`, `unavailableDashboardResult`, `optionalDashboardRead`, `optionalDashboardData`. Set `DASHBOARD_READ_INIT` to `Object.freeze({ cache: "no-store" }) satisfies Readonly<RequestInit>`. Implement `dashboardRead<T>(path: string, init: RequestInit = {}): Promise<T>` by calling `apiFetch<T>(path, { ...DASHBOARD_READ_INIT, ...init, headers: init.headers })` so callers can pass headers but `credentials: "include"` remains owned by `apiFetch`. Define `OptionalDashboardResult<T>` as `{ status: "ready"; data: T } | { status: "unavailable"; message: string }`. Implement `optionalDashboardRead<T>(read: () => Promise<T>, unavailableMessage: string)` to return `readyDashboardResult(await read())` or `unavailableDashboardResult(unavailableMessage)` and do not catch or wrap primary reads. Implement `optionalDashboardData<T>(result, fallback)` to return data for ready or fallback for unavailable. Create `frontend/tests/phase37-dashboard-loading.test.tsx` with a suite label `Phase 37 dashboard loading`. Add a mock `fetch` test that calls `await dashboardRead<unknown>("/api/teacher/students")` and asserts the first fetch call options include `credentials: "include"` and `cache: "no-store"`. Add an optional-result test where `optionalDashboardRead(() => Promise.reject(new Error("boom")), "Mục này tạm thời chưa tải được.")` returns `{ status: "unavailable", message: "Mục này tạm thời chưa tải được." }`. Add static source tests scanning `lib/dashboard-loading.ts`, `lib/api.ts`, `lib/sos-api.ts`, `lib/notification-preferences-api.ts`, `lib/admin-api.ts`, and `app/(authenticated)/layout.tsx` and failing if any contain `localStorage.setItem`, `sessionStorage.setItem`, `indexedDB`, `serviceWorker`, `access_token`, `refresh_token`, `id_token`, `QueryClientProvider`, or `new QueryClient`.
  </action>
  <acceptance_criteria>
    - `grep -n "DASHBOARD_READ_INIT" frontend/lib/dashboard-loading.ts` finds the exported no-store constant.
    - `grep -n "cache: \"no-store\"" frontend/lib/dashboard-loading.ts` finds explicit no-store configuration.
    - `grep -n "export async function dashboardRead" frontend/lib/dashboard-loading.ts` finds the read helper.
    - `grep -n "type OptionalDashboardResult" frontend/lib/dashboard-loading.ts` finds the optional-result union.
    - `grep -n "optionalDashboardRead" frontend/lib/dashboard-loading.ts` finds the optional failure helper.
    - `grep -n "Phase 37 dashboard loading" frontend/tests/phase37-dashboard-loading.test.tsx` finds the suite label.
    - `grep -n "cache: \"no-store\"" frontend/tests/phase37-dashboard-loading.test.tsx` finds no-store assertion.
    - `grep -n "credentials: \"include\"" frontend/tests/phase37-dashboard-loading.test.tsx` finds credential assertion.
    - `grep -n "Mục này tạm thời chưa tải được" frontend/tests/phase37-dashboard-loading.test.tsx` finds optional unavailable copy assertion.
    - `grep -n "QueryClientProvider" frontend/tests/phase37-dashboard-loading.test.tsx` finds broad provider redline assertion.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend run test -- tests/phase37-dashboard-loading.test.tsx</automated>
  </verify>
  <done>Dashboard helper foundation exists, keeps no-store scoped to dashboard reads, and has focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Route existing dashboard GET helpers through dashboardRead</name>
  <files>frontend/lib/sos-api.ts, frontend/lib/notification-preferences-api.ts, frontend/lib/admin-api.ts</files>
  <read_first>
    frontend/lib/dashboard-loading.ts
    frontend/lib/api.ts
    frontend/lib/sos-api.ts
    frontend/lib/notification-preferences-api.ts
    frontend/lib/admin-api.ts
    frontend/app/(authenticated)/student/page.tsx
    frontend/app/(authenticated)/teacher/page.tsx
    frontend/app/(authenticated)/parent/page.tsx
    frontend/app/(authenticated)/admin/page.tsx
  </read_first>
  <action>
    Import `dashboardRead` from `@/lib/dashboard-loading` in `frontend/lib/sos-api.ts`, `frontend/lib/notification-preferences-api.ts`, and `frontend/lib/admin-api.ts`. Change only dashboard read helpers to use `dashboardRead`: `listStudentSosAlerts`, `getTeacherSosAlert`, `getParentSosAlert`, `getTeacherSupportOverview`, `getParentSupportOverview`, `getNotifications`, `getNotificationPreferences`, `getMoodCheckInReminder`, `listUsers`, and `listLinks`. Preserve mutation helpers on `apiFetch`: `createStudentSosAlert`, `updateTeacherSosStatus`, `markNotificationRead`, `updateNotificationPreferences`, `dismissMoodCheckInReminder`, `snoozeMoodCheckInReminder`, `openMoodCheckInReminder`, `createUser`, `updateUser`, `deleteUser`, `createLink`, and `revokeLink`. Do not add React Query imports, localStorage/sessionStorage/IndexedDB/service-worker code, or browser token handling.
  </action>
  <acceptance_criteria>
    - `grep -n "dashboardRead" frontend/lib/sos-api.ts` finds dashboard read usage.
    - `grep -n "dashboardRead" frontend/lib/notification-preferences-api.ts` finds dashboard read usage.
    - `grep -n "dashboardRead" frontend/lib/admin-api.ts` finds dashboard read usage.
    - `grep -n "createStudentSosAlert" -A6 frontend/lib/sos-api.ts` still shows `apiFetch<SosAlert>` and `method: "POST"`.
    - `grep -n "openMoodCheckInReminder" -A5 frontend/lib/notification-preferences-api.ts` still shows `apiFetch<MoodReminderAction>` and `method: "POST"`.
    - `grep -n "createUser" -A6 frontend/lib/admin-api.ts` still shows `apiFetch<AdminUser>` and `method: "POST"`.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'frontend/lib/sos-api.ts','frontend/lib/notification-preferences-api.ts','frontend/lib/admin-api.ts' -Pattern 'dashboardRead' -Quiet"</automated>
  </verify>
  <done>Existing dashboard GET wrappers are explicit no-store without changing mutations.</done>
</task>

<task type="auto">
  <name>Task 3: Update aggregate frontend route evidence counting</name>
  <files>frontend/scripts/phase33-frontend-baseline.mjs, frontend/scripts/phase33-frontend-baseline.test.mjs</files>
  <read_first>
    frontend/scripts/phase33-frontend-baseline.mjs
    frontend/scripts/phase33-frontend-baseline.test.mjs
    .planning/phases/33-cross-role-ui-performance-baseline-audit/33-PERFORMANCE-BASELINE.md
    .planning/phases/37-frontend-data-loading-render-optimization/37-CONTEXT.md
    frontend/app/(authenticated)/student/page.tsx
    frontend/app/(authenticated)/teacher/page.tsx
    frontend/app/(authenticated)/parent/page.tsx
    frontend/app/(authenticated)/admin/page.tsx
  </read_first>
  <action>
    Update `frontend/scripts/phase33-frontend-baseline.mjs` so `fetchCandidateCountFor` counts these exact call forms: `apiFetch(...)`, `apiFetch<Type>(...)`, `dashboardRead(...)`, `dashboardRead<Type>(...)`, and `fetch(...)`. Add a recursive local import collector with a `visited` set and a maximum depth of `3` so route files count request calls inside neutral imported helpers such as `lib/student-dashboard-loader.ts`, `lib/adult-dashboard-loader.ts`, and `lib/dashboard-loading.ts` without following node_modules. Keep `APPROVED_OUTPUT_KEYS` unchanged and keep `commandSource: "node scripts/phase33-frontend-baseline.mjs"`. Update `frontend/scripts/phase33-frontend-baseline.test.mjs` with assertions that the helper source contains `dashboardRead` and a generic-call regex, and that selected `/student`, `/teacher`, `/parent`, and `/admin` rows have numeric `fetchCandidateCount` and `waterfallCount` values with no forbidden output keys.
  </action>
  <acceptance_criteria>
    - `grep -n "dashboardRead" frontend/scripts/phase33-frontend-baseline.mjs` finds dashboardRead counting.
    - `grep -n "apiFetch.*<" frontend/scripts/phase33-frontend-baseline.mjs` or `grep -n "apiFetch\\\\s\\*.*<" frontend/scripts/phase33-frontend-baseline.mjs` finds generic apiFetch counting support.
    - `grep -n "visited" frontend/scripts/phase33-frontend-baseline.mjs` finds recursive import protection.
    - `grep -n "maxDepth" frontend/scripts/phase33-frontend-baseline.mjs` finds bounded recursion.
    - `grep -n "dashboardRead" frontend/scripts/phase33-frontend-baseline.test.mjs` finds script behavior coverage.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend; node --test scripts/phase33-frontend-baseline.test.mjs</automated>
  </verify>
  <done>Frontend baseline helper counts generic/imported dashboard request calls while remaining aggregate-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dashboard pages -> API client | Dashboard reads use neutral helpers but auth credentials stay in `apiFetch`. |
| optional supplemental API -> dashboard UI | Optional read failures become typed unavailable states, not false successful empty arrays. |
| source scan -> evidence artifact | Evidence remains aggregate route/request/build fields only. |
| helper foundation -> future role plans | Shared helpers must not become a global sensitive data cache. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37-01-01 | Information Disclosure | dashboard reads | high | mitigate | `dashboardRead` uses `cache: "no-store"` and delegates credentials to `apiFetch`; tests assert both options. |
| T-37-01-02 | Information Disclosure | browser storage | high | mitigate | Tests reject localStorage/sessionStorage/IndexedDB/serviceWorker and token markers in helper/API/auth files. |
| T-37-01-03 | Information Disclosure | broad server-state provider | high | mitigate | Tests reject `QueryClientProvider` and `new QueryClient`; no React Query provider added. |
| T-37-01-04 | Tampering | request evidence | medium | mitigate | Baseline helper keeps approved output keys and aggregate-only route labels/counts. |
| T-37-01-05 | Safety UX | optional failures | medium | mitigate | Typed optional result preserves unavailable messages for scoped UI rendering in later plans. |
</threat_model>

<verification>
Run:
`npm --prefix frontend run test -- tests/phase37-dashboard-loading.test.tsx`
`cd frontend; node --test scripts/phase33-frontend-baseline.test.mjs`
</verification>

<success_criteria>
FEPERF-01, FEPERF-03, FEPERF-04, and FEPERF-05 have a safe frontend loading foundation: credentialed no-store dashboard reads, optional-result helpers, no browser storage/provider regression, and aggregate-only request/build evidence counting.
</success_criteria>

<must_haves>
- `frontend/lib/dashboard-loading.ts` exists and exports `dashboardRead` plus `OptionalDashboardResult`.
- Dashboard GET wrappers use `dashboardRead`; mutation wrappers keep `apiFetch`.
- Phase 33 frontend baseline helper counts generic/imported request helpers and keeps approved aggregate-only output keys.
- Focused tests pass for no-store credentials, optional unavailable results, no browser storage, and no broad React Query provider.
</must_haves>

<output>
After completion, create `.planning/phases/37-frontend-data-loading-render-optimization/37-01-SUMMARY.md`.
</output>


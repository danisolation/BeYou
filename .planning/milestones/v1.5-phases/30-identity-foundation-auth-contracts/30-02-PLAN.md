---
phase: 30-identity-foundation-auth-contracts
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - backend/app/services/external_identity.py
  - backend/tests/test_identity_contracts.py
  - backend/tests/test_authorization_security.py
autonomous: true
requirements: [IDENT-02, IDENT-05, IDENT-06, IDENT-07]
must_haves:
  truths:
    - "Unknown and unlinked external identities never create privileged users automatically."
    - "Disabled, deprovisioned, inactive-user, and pending-review identities produce safe resolver statuses."
    - "IdP claims, groups, school/class metadata, and email domains do not change adult visibility."
  artifacts:
    - path: "backend/app/services/external_identity.py"
      provides: "Future SSO resolver contract without OAuth callback implementation"
      contains: "resolve_external_identity"
    - path: "backend/tests/test_identity_contracts.py"
      provides: "Resolver safety regression tests"
      contains: "unknown_identity"
    - path: "backend/tests/test_authorization_security.py"
      provides: "Authorization boundary regression"
      contains: "claims_do_not_grant_adult_visibility"
  key_links:
    - from: "backend/app/services/external_identity.py"
      to: "backend/app/db/models.py"
      via: "select ExternalIdentity by provider_key/provider_subject_hash"
      pattern: "select\\(ExternalIdentity\\)"
    - from: "backend/tests/test_authorization_security.py"
      to: "backend/app/core/authorization.py"
      via: "require_permission app role + link + SOS only"
      pattern: "require_permission"
---

<objective>
Define the external identity resolver contract and prove it cannot grant access or broaden adult visibility.

Purpose: Future OAuth/SSO needs a safe provider+subject resolver, but Phase 30 must not implement redirect/callback, auto-provisioning, email auto-merge, or provider-claim authorization.
Output: Resolver service and targeted backend security tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
@.planning/phases/30-identity-foundation-auth-contracts/30-RESEARCH.md
@backend/app/db/models.py
@backend/app/core/authorization.py
@backend/tests/test_authorization_security.py

<interfaces>
Required resolver statuses:
```python
"linked_active_user"
"pending_review"
"disabled_identity"
"deprovisioned_identity"
"linked_user_inactive"
"unknown_identity"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement external identity resolver contract</name>
  <files>backend/app/services/external_identity.py, backend/tests/test_identity_contracts.py</files>
  <read_first>
    - backend/app/db/models.py
    - backend/app/services/admin_operations.py
    - backend/tests/test_auth_privacy_portals.py
    - .planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
    - .planning/phases/30-identity-foundation-auth-contracts/30-RESEARCH.md
  </read_first>
  <behavior>
    - `hash_external_subject("pilot_sso", "Subject-123")` returns a SHA-256 hex string and never returns `Subject-123`.
    - Unknown provider+subject returns `status="unknown_identity"` and `user is None`.
    - `pending_review`, `disabled`, `deprovisioned`, linked inactive user, and linked active user each map to the required safe status.
  </behavior>
  <action>Create `backend/app/services/external_identity.py` with `ResolveExternalIdentityStatus = Literal[...]`, dataclass `ExternalIdentityResolution(status: ResolveExternalIdentityStatus, identity: ExternalIdentity | None, user: User | None)`, `hash_external_subject(provider_key: str, subject: str) -> str` using `hashlib.sha256(f"{provider_key.strip().lower()}:{subject}".encode("utf-8")).hexdigest()`, and `resolve_external_identity(db: OrmSession, provider_key: str, subject: str) -> ExternalIdentityResolution`. The resolver must query `ExternalIdentity.provider_key == normalized provider_key` and `ExternalIdentity.provider_subject_hash == hash_external_subject(...)`, never query by email, never create `User`, never mutate identity status, and never accept claims/groups/school/class metadata as authorization input per D-04 through D-07. Add `backend/tests/test_identity_contracts.py` with fixtures patterned after `test_auth_privacy_portals.py`, cleaning `ExternalIdentity`, `Session`, and `User` rows created by tests. Cover all required statuses and assert `User` count does not increase for `unknown_identity` or `pending_review`.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_identity_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/external_identity.py` contains `def hash_external_subject(` and `hashlib.sha256`.
    - `backend/app/services/external_identity.py` contains all exact status strings: `linked_active_user`, `pending_review`, `disabled_identity`, `deprovisioned_identity`, `linked_user_inactive`, `unknown_identity`.
    - `backend/app/services/external_identity.py` does not contain `create_user`, `insert(User`, `User(`, `raw_claims`, `groups`, `class_name`, or `school`.
    - `backend/tests/test_identity_contracts.py` contains tests for each resolver status string and at least one assertion that user count is unchanged.
  </acceptance_criteria>
  <done>Resolver returns safe denial/review/link statuses without auto-creating or email-merging users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock authorization boundary against provider claims</name>
  <files>backend/tests/test_authorization_security.py</files>
  <read_first>
    - backend/tests/test_authorization_security.py
    - backend/app/core/authorization.py
    - backend/app/services/external_identity.py
    - .planning/phases/30-identity-foundation-auth-contracts/30-CONTEXT.md
  </read_first>
  <behavior>
    - A teacher/parent with provider group or school/class metadata but no active app link and SOS still receives 403.
    - A linked adult without student-sent SOS still receives 403 for support-not-surveillance reads.
    - Existing active link + SOS behavior still passes.
  </behavior>
  <action>Append regression tests to `backend/tests/test_authorization_security.py` without modifying `backend/app/core/authorization.py`. Add a test named `test_external_identity_claims_do_not_grant_adult_visibility` that creates a student and teacher, constructs a fake claims dict containing `groups=["teacher", "admin"]`, `school="THPT Demo"`, `class_name="10A1"`, and `email_domain="school.example"`, then proves these values are not passed to `require_permission()` and unlinked access to `adult_support_summary` with purpose `support_not_surveillance` raises HTTP 403. Add or extend a test proving an active relationship without `SosAlert` still raises 403, while active relationship plus a student `SosAlert` allows access. This implements D-14 and D-15 by regression, not by provider-claim code.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_authorization_security.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_authorization_security.py` contains `test_external_identity_claims_do_not_grant_adult_visibility`.
    - The test file contains exact strings `groups`, `school`, `class_name`, and `email_domain` only inside test fake claims data or assertions.
    - The test file contains an assertion for HTTP status code `403` when active relationship or SOS is missing.
    - `backend/app/core/authorization.py` remains free of `ExternalIdentity`, `provider`, `claims`, `groups`, `email_domain`, `school`, and `class_name` authorization checks.
  </acceptance_criteria>
  <done>Authorization remains app-owned: role + active relationship + student SOS, not IdP claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future IdP→resolver | Untrusted external subject/claim data enters backend resolver. |
| resolver→authorization | Resolved identity must not become an authorization grant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | E | `resolve_external_identity` | mitigate | Return safe statuses only; never create users or merge by unverified email. |
| T-30-02-02 | I | identity storage | mitigate | Hash subject; do not expose raw email, claims, groups, school/class metadata, or tokens. |
| T-30-02-03 | E | `require_permission` | mitigate | Tests prove IdP claims/groups do not grant adult student visibility; active link + SOS remains mandatory. |
| T-30-02-04 | S | disabled/deprovisioned identities | mitigate | Resolver returns denial statuses for disabled/deprovisioned and inactive linked users. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_identity_contracts.py -q`
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_authorization_security.py -q`
- `Set-Location D:\BeYou\backend; python -m ruff check .`
</verification>

<success_criteria>
External identity resolution is safe, deny-by-default, non-provisioning, and disconnected from adult visibility authorization.
</success_criteria>

<output>
After completion, create `.planning/phases/30-identity-foundation-auth-contracts/30-02-SUMMARY.md`
</output>

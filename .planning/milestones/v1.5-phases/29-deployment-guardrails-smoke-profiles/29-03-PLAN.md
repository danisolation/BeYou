---
phase: 29-deployment-guardrails-smoke-profiles
plan: 03
type: execute
wave: 3
depends_on:
  - 29-02
files_modified:
  - backend/app/schemas/admin_operations.py
  - backend/app/services/admin_operations.py
  - backend/tests/test_phase11_operations_visibility.py
  - backend/tests/test_demo_seed.py
autonomous: true
requirements:
  - DEPLOY-03
  - DEPLOY-04
must_haves:
  truths:
    - "Admin operations API exposes deployment guardrail metadata only."
    - "Admin operations API exposes Demo smoke and Production pilot smoke as separate metadata records."
    - "No backend operations response serializes secrets, cookie names/values, connection strings, demo emails, raw student data, or raw IDs."
  artifacts:
    - path: "backend/app/schemas/admin_operations.py"
      provides: "DeploymentGuardrailItem and SmokeProfileItem schemas"
    - path: "backend/app/services/admin_operations.py"
      provides: "Safe guardrail and smoke profile metadata builders"
    - path: "backend/tests/test_phase11_operations_visibility.py"
      provides: "Admin operations privacy regression coverage"
    - path: "backend/tests/test_demo_seed.py"
      provides: "Updated smoke command metadata expectation"
  key_links:
    - from: "backend/app/services/admin_operations.py"
      to: "backend/app/schemas/admin_operations.py"
      via: "AdminOperationsDashboardResponse"
      pattern: "deployment_guardrails=.*smoke_profiles="
---

<objective>
Expose backend/admin operations metadata for deployment guardrails and separated smoke profiles.

Purpose: The operations UI must render trusted metadata from the backend, not hardcoded frontend-only deployment claims.
Output: Safe Pydantic schemas, service builders, and backend regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-runtime-mode-production-readiness-foundation/28-VERIFICATION.md
@.planning/phases/29-deployment-guardrails-smoke-profiles/29-RESEARCH.md
@.planning/phases/29-deployment-guardrails-smoke-profiles/29-UI-SPEC.md
@backend/app/schemas/admin_operations.py
@backend/app/services/admin_operations.py
@backend/tests/test_phase11_operations_visibility.py
@backend/tests/test_demo_seed.py

<interfaces>
Existing schema:
```python
class AdminOperationsDashboardResponse(BaseModel):
    generated_at: datetime
    privacy_notes: list[str]
    readiness: OperationReadinessSummary
    runtime: RuntimeModeSummary
    demo_seed: DemoSeedSummary
    connectivity: ConnectivitySummary
    production_smoke: list[ProductionSmokeChecklistItem]
```

Add safe fields:
```python
deployment_guardrails: list[DeploymentGuardrailItem]
smoke_profiles: list[SmokeProfileItem]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend tests for deployment/smoke metadata and forbidden serialization</name>
  <files>backend/tests/test_phase11_operations_visibility.py, backend/tests/test_demo_seed.py</files>
  <read_first>
    - backend/tests/test_phase11_operations_visibility.py
    - backend/tests/test_demo_seed.py
    - backend/app/schemas/admin_operations.py
    - backend/app/services/admin_operations.py
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-UI-SPEC.md
  </read_first>
  <behavior>
    - Operations dashboard JSON contains `deployment_guardrails`.
    - Operations dashboard JSON contains `smoke_profiles`.
    - Operations dashboard deployment guardrails include key `cors_cookie_contract` with evidence that exact expected frontend origin matched or failed without rendering the raw origin.
    - Production-pilot metadata fails when wildcard, local, non-HTTPS, or missing exact allowed frontend origin is present.
    - Smoke profiles include keys `demo_smoke` and `pilot_smoke`.
    - Demo smoke has command `npm --prefix frontend run smoke:demo`, `uses_demo_accounts: true`, `requires_readiness_ready: false`.
    - Pilot smoke has command `npm --prefix frontend run smoke:pilot`, `uses_demo_accounts: false`, `requires_readiness_ready: true`.
    - Rendered operations JSON excludes `DATABASE_URL`, `SESSION_COOKIE_NAME`, `SMTP_PASSWORD`, `FREEMODEL_API_KEY`, `student.demo@beyou.local`, `resource_id`, `beyou_session`, raw notes, transcripts, answers, and private SOS text.
  </behavior>
  <action>
    Extend `backend/tests/test_phase11_operations_visibility.py` in the existing dashboard test to assert the new `deployment_guardrails` and `smoke_profiles` keys and the exact demo/pilot metadata values. Add tests that configure expected frontend origin and allowed origins, then assert `cors_cookie_contract` passes only when the normalized expected frontend origin exactly matches a credentialed allowed origin, and fails for wildcard, local, non-HTTPS, or missing exact origin. These tests must assert evidence uses safe metadata such as `exact_allowed_origin_match=yes`, `allowed_origin_count=1`, `all_origins_https=yes`, and `credentialed_cors=yes` without rendering the raw origin string. Extend or update `backend/tests/test_demo_seed.py` line that currently asserts every command equals `npm --prefix frontend run smoke:production`; change it to assert `dashboard.smoke_profiles` contains both `npm --prefix frontend run smoke:demo` and `npm --prefix frontend run smoke:pilot`, and keep `production_smoke` only if retained for compatibility. Add forbidden string checks against `dashboard.model_dump_json()` and API `response.text`.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase11_operations_visibility.py tests\test_demo_seed.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_phase11_operations_visibility.py` contains `deployment_guardrails`.
    - `backend/tests/test_phase11_operations_visibility.py` contains `smoke_profiles`.
    - `backend/tests/test_phase11_operations_visibility.py` contains `exact_allowed_origin_match`.
    - `backend/tests/test_phase11_operations_visibility.py` contains `wildcard`.
    - `backend/tests/test_phase11_operations_visibility.py` contains `demo_smoke`.
    - `backend/tests/test_phase11_operations_visibility.py` contains `pilot_smoke`.
    - `backend/tests/test_phase11_operations_visibility.py` contains `SESSION_COOKIE_NAME`.
    - `backend/tests/test_demo_seed.py` contains `npm --prefix frontend run smoke:demo`.
    - `backend/tests/test_demo_seed.py` contains `npm --prefix frontend run smoke:pilot`.
  </acceptance_criteria>
  <done>Backend tests specify metadata-only guardrails and separated smoke profile contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement metadata-only deployment guardrails and smoke profiles</name>
  <files>backend/app/schemas/admin_operations.py, backend/app/services/admin_operations.py</files>
  <read_first>
    - backend/tests/test_phase11_operations_visibility.py
    - backend/tests/test_demo_seed.py
    - backend/app/schemas/admin_operations.py
    - backend/app/services/admin_operations.py
    - backend/app/core/config.py
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-RESEARCH.md
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-UI-SPEC.md
  </read_first>
  <action>
    In `backend/app/schemas/admin_operations.py`, add `DeploymentGuardrailItem` with fields `key: str`, `category: str`, `status: ReadinessCheckStatus`, `evidence: str`, `remediation: str | None = None`, and `command: str | None = None`. Add `SmokeProfileItem` with fields `key: str`, `label: str`, `status: ReadinessCheckStatus`, `command: str`, `uses_demo_accounts: bool`, `requires_readiness_ready: bool`, `evidence: str`, and `remediation: str | None = None`. Add `deployment_guardrails: list[DeploymentGuardrailItem]` and `smoke_profiles: list[SmokeProfileItem]` to `AdminOperationsDashboardResponse`. In `backend/app/services/admin_operations.py`, import the new schemas. Add constants `DEPLOY_GUARD_COMMAND = "npm --prefix frontend run guard:deploy"`, `DEMO_SMOKE_COMMAND = "npm --prefix frontend run smoke:demo"`, and `PILOT_SMOKE_COMMAND = "npm --prefix frontend run smoke:pilot"`. Add helper `_normalize_origin_for_match(value: str) -> str`, helper `_exact_credentialed_origin_match(settings)` returning safe booleans/counts (`expected_frontend_origin_configured`, `exact_allowed_origin_match`, `allowed_origin_count`, `has_wildcard_origin`, `has_local_origin`, `all_origins_https`, `credentialed_cors`), and helper `_deployment_guardrails(settings)` returning safe records for keys `vercel_frontend`, `render_backend`, `frontend_api_target`, and `cors_cookie_contract`. `cors_cookie_contract` must compute exact match by normalizing `settings.frontend_origin` and every configured allowed origin from `settings.frontend_origins`/fallback, then fail for production pilot when the expected frontend origin is absent, not exactly present in allowed origins, wildcard origin is present, local origin is present, any origin is non-HTTPS, `settings.session_cookie_secure` is false, or `settings.session_cookie_samesite.lower() != "none"`. Evidence must describe safe booleans/counts/kinds only, using strings such as `expected_frontend_origin_configured=yes`, `exact_allowed_origin_match=yes`, `allowed_origin_count=1`, `all_origins_https=yes`, and `credentialed_cors=yes`; it must not include raw origins or cookie names. Add `_smoke_profiles(settings, readiness_report)` returning exactly two records: `demo_smoke` labelled `Demo smoke` and `pilot_smoke` labelled `Production pilot smoke`. Demo smoke uses demo accounts and does not require readiness ready. Pilot smoke does not use demo accounts, requires readiness ready, and has `fail` when `readiness_report.status != "ready"` or `settings.allow_demo_login` or `settings.allow_demo_seed` is true in production pilot. Preserve `production_smoke` if existing frontend/tests still consume it, but change its command evidence to point to demo compatibility and pilot explicit command where relevant. Wire new fields into `build_operations_dashboard()`.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase11_operations_visibility.py tests\test_demo_seed.py -q; python -m ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/admin_operations.py` contains `class DeploymentGuardrailItem`.
    - `backend/app/schemas/admin_operations.py` contains `class SmokeProfileItem`.
    - `backend/app/schemas/admin_operations.py` contains `deployment_guardrails: list[DeploymentGuardrailItem]`.
    - `backend/app/schemas/admin_operations.py` contains `smoke_profiles: list[SmokeProfileItem]`.
    - `backend/app/services/admin_operations.py` contains `DEPLOY_GUARD_COMMAND = "npm --prefix frontend run guard:deploy"`.
    - `backend/app/services/admin_operations.py` contains `DEMO_SMOKE_COMMAND = "npm --prefix frontend run smoke:demo"`.
    - `backend/app/services/admin_operations.py` contains `PILOT_SMOKE_COMMAND = "npm --prefix frontend run smoke:pilot"`.
    - `backend/app/services/admin_operations.py` contains `_exact_credentialed_origin_match`.
    - `backend/app/services/admin_operations.py` contains `exact_allowed_origin_match`.
    - `backend/app/services/admin_operations.py` contains `allowed_origin_count`.
    - `backend/app/services/admin_operations.py` contains `has_wildcard_origin`.
    - `backend/app/services/admin_operations.py` does not add `DATABASE_URL`, `SESSION_COOKIE_NAME`, raw origin values, cookie names, raw emails, or resource IDs to guardrail/smoke metadata.
  </acceptance_criteria>
  <done>Admin operations API exposes safe deployment guardrail and smoke profile metadata for UI consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| backend settings -> admin operations response | Runtime/cookie/origin settings are converted into safe metadata. |
| admin API -> frontend UI | Admin-only metadata crosses API boundary. |
| student data tables -> operations service | Existing operations service queries counts and must not expose raw student content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-29-03-01 | I | AdminOperationsDashboardResponse | mitigate | New schemas contain statuses, booleans, command names, evidence, remediation only; tests reject forbidden strings. |
| T-29-03-02 | I | cookie/CORS metadata | mitigate | Expose `session_cookie_secure`, SameSite, counts/kinds only; do not serialize cookie name/value or raw origin lists. |
| T-29-03-03 | R | smoke_profiles evidence | mitigate | Encode `uses_demo_accounts` and `requires_readiness_ready` booleans so demo pass cannot be mistaken for pilot readiness. |
| T-29-03-04 | E | admin operations endpoint | accept | Existing endpoint remains admin-gated; this plan does not alter auth dependencies. |
</threat_model>

<verification>
Run:
```powershell
Set-Location D:\BeYou\backend
python -m pytest tests\test_phase11_operations_visibility.py tests\test_demo_seed.py -q
python -m ruff check .
```
</verification>

<success_criteria>
- Admin operations response includes `deployment_guardrails` and `smoke_profiles`.
- CORS/cookie metadata validates exact expected frontend origin matching with safe booleans/counts only.
- Smoke metadata separates `demo_smoke` and `pilot_smoke`.
- Pilot metadata requires readiness ready and no demo dependency.
- New backend metadata remains free of secrets, cookie values/names, connection strings, raw emails, raw student content, and raw IDs.
</success_criteria>

<must_haves>
- DEPLOY-03: backend/admin metadata verifies CORS/cookie deployment contract through safe booleans/statuses.
- DEPLOY-04: backend/admin metadata separates demo and pilot smoke assumptions.
</must_haves>

<output>
After completion, create `.planning/phases/29-deployment-guardrails-smoke-profiles/29-03-SUMMARY.md`.
</output>

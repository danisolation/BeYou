---
phase: 29-deployment-guardrails-smoke-profiles
plan: 04
type: execute
wave: 4
depends_on:
  - 29-01
  - 29-02
  - 29-03
files_modified:
  - frontend/lib/admin-operations-api.ts
  - frontend/app/(authenticated)/admin/operations/page.tsx
  - frontend/tests/phase11-operations-ui.test.tsx
  - README.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
must_haves:
  truths:
    - "Admin operations UI shows Deployment guardrails panel."
    - "Admin operations UI shows Smoke profiles with separate Demo smoke and Production pilot smoke cards."
    - "Deployment docs explain Vercel root, Render backend profile, env vars, smoke commands, demo-vs-pilot readiness, and rollback."
    - "Final backend/frontend regression gates pass."
  artifacts:
    - path: "frontend/lib/admin-operations-api.ts"
      provides: "Frontend TypeScript contract for deployment/smoke metadata"
    - path: "frontend/app/(authenticated)/admin/operations/page.tsx"
      provides: "Approved Phase 29 operations UI panels"
    - path: "frontend/tests/phase11-operations-ui.test.tsx"
      provides: "UI privacy and separation regression tests"
    - path: "README.md"
      provides: "Operator deployment and smoke documentation"
  key_links:
    - from: "frontend/app/(authenticated)/admin/operations/page.tsx"
      to: "frontend/lib/admin-operations-api.ts"
      via: "AdminOperationsDashboard.deployment_guardrails and smoke_profiles"
      pattern: "dashboard\\.deployment_guardrails|dashboard\\.smoke_profiles"
    - from: "README.md"
      to: "frontend/package.json"
      via: "documented command names"
      pattern: "guard:deploy|smoke:demo|smoke:pilot"
---

<objective>
Wire Phase 29 deployment/smoke metadata into the approved operations UI and operator documentation, then run final regression gates.

Purpose: Operators need metadata-only UI and docs that prevent demo smoke from being confused with production-pilot readiness.
Output: UI panels, TypeScript contracts, tests, README deployment guide, and final command gates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-deployment-guardrails-smoke-profiles/29-UI-SPEC.md
@frontend/lib/admin-operations-api.ts
@frontend/app/(authenticated)/admin/operations/page.tsx
@frontend/tests/phase11-operations-ui.test.tsx
@README.md

<interfaces>
Backend fields from Plan 03:
```ts
deployment_guardrails: Array<{
  key: string;
  category: string;
  status: "pass" | "warn" | "fail";
  evidence: string;
  remediation: string | null;
  command: string | null;
}>;

smoke_profiles: Array<{
  key: string;
  label: string;
  status: "pass" | "warn" | "fail";
  command: string;
  uses_demo_accounts: boolean;
  requires_readiness_ready: boolean;
  evidence: string;
  remediation: string | null;
}>;
```

Approved UI copy:
- `Deployment guardrails`
- `Kiểm tra Render, Vercel, API target, CORS và cookie bằng metadata an toàn.`
- `Smoke profiles`
- `Tách smoke public demo khỏi production pilot để không tạo tự tin sai từ tài khoản demo.`
- `Demo smoke`
- `Production pilot smoke`
- `không phụ thuộc tài khoản demo`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend frontend API types and operations UI tests for approved contract</name>
  <files>frontend/lib/admin-operations-api.ts, frontend/tests/phase11-operations-ui.test.tsx</files>
  <read_first>
    - frontend/lib/admin-operations-api.ts
    - frontend/tests/phase11-operations-ui.test.tsx
    - frontend/app/(authenticated)/admin/operations/page.tsx
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-UI-SPEC.md
  </read_first>
  <behavior>
    - TypeScript dashboard type contains `deployment_guardrails` and `smoke_profiles`.
    - UI test fixture includes one guardrail `frontend_api_target`, one guardrail `cors_cookie_contract`, smoke `demo_smoke`, and smoke `pilot_smoke`.
    - UI tests assert `Deployment guardrails`, `Smoke profiles`, `Demo smoke`, and `Production pilot smoke` render.
    - UI tests assert pilot card includes `không phụ thuộc tài khoản demo`.
    - UI tests assert forbidden strings are absent: `DATABASE_URL`, `SESSION_COOKIE_NAME`, `SMTP_PASSWORD`, `FREEMODEL_API_KEY`, `resource_id`, and `student.demo@beyou.local`.
  </behavior>
  <action>
    Update `frontend/lib/admin-operations-api.ts` with exported types `DeploymentGuardrailItem` and `SmokeProfileItem`, then add `deployment_guardrails: DeploymentGuardrailItem[]` and `smoke_profiles: SmokeProfileItem[]` to `AdminOperationsDashboard`. Update `frontend/tests/phase11-operations-ui.test.tsx` fixture to include safe deployment/smoke metadata. Replace assertions that expect only `Production smoke checklist` with assertions for the approved Phase 29 strings. Keep existing filter, cookie-auth fetch, no-token-storage, no-export, no-drilldown, and sensitive-field absence tests.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase11-operations-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/admin-operations-api.ts` contains `export type DeploymentGuardrailItem`.
    - `frontend/lib/admin-operations-api.ts` contains `export type SmokeProfileItem`.
    - `frontend/lib/admin-operations-api.ts` contains `deployment_guardrails: DeploymentGuardrailItem[]`.
    - `frontend/lib/admin-operations-api.ts` contains `smoke_profiles: SmokeProfileItem[]`.
    - `frontend/tests/phase11-operations-ui.test.tsx` contains `Deployment guardrails`.
    - `frontend/tests/phase11-operations-ui.test.tsx` contains `Smoke profiles`.
    - `frontend/tests/phase11-operations-ui.test.tsx` contains `không phụ thuộc tài khoản demo`.
    - `frontend/tests/phase11-operations-ui.test.tsx` contains `SESSION_COOKIE_NAME`.
  </acceptance_criteria>
  <done>Frontend contract and tests lock the approved UI/privacy behavior before page implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement approved Deployment guardrails and Smoke profiles operations UI</name>
  <files>frontend/app/(authenticated)/admin/operations/page.tsx</files>
  <read_first>
    - frontend/app/(authenticated)/admin/operations/page.tsx
    - frontend/lib/admin-operations-api.ts
    - frontend/tests/phase11-operations-ui.test.tsx
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-UI-SPEC.md
  </read_first>
  <action>
    In `frontend/app/(authenticated)/admin/operations/page.tsx`, import `DeploymentGuardrailItem` and `SmokeProfileItem`. After the privacy boundary and filters, add a panel titled `Deployment guardrails` with description `Kiểm tra Render, Vercel, API target, CORS và cookie bằng metadata an toàn.` Render guardrail cards in a responsive grid using `rounded-2xl border border-[#D7EFE8] p-4`, `StatusBadge`, evidence text, optional command block `rounded-2xl bg-secondary px-3 py-2 text-label`, and remediation text. If no guardrails exist, show `Chưa có kết quả guardrail.` and `Hãy chạy guardrail hoặc smoke command rồi kiểm tra lại metadata vận hành.` Add a second panel titled `Smoke profiles` with description `Tách smoke public demo khỏi production pilot để không tạo tự tin sai từ tài khoản demo.` Render separate cards for `Demo smoke` and `Production pilot smoke`; the pilot card must include exact Vietnamese text `Production pilot smoke yêu cầu readiness ready và không phụ thuộc tài khoản demo hoặc dữ liệu walkthrough.` Do not add deploy, rollback, reset, export, secret reveal, or destructive buttons. Do not render forbidden fields even if present in API payload. Preserve existing loading text `Đang tải dữ liệu vận hành...` and error text `Chưa tải được dữ liệu vận hành. Hãy thử lại từ cổng quản trị.`
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase11-operations-ui.test.tsx; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains `Deployment guardrails`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains `Kiểm tra Render, Vercel, API target, CORS và cookie bằng metadata an toàn.`
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains `Smoke profiles`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains `Demo smoke`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains `Production pilot smoke`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains `không phụ thuộc tài khoản demo`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` does not contain `DATABASE_URL`, `SESSION_COOKIE_NAME`, `SMTP_PASSWORD`, `FREEMODEL_API_KEY`, `resource_id`, or `student.demo@beyou.local`.
  </acceptance_criteria>
  <done>Operations UI matches the approved Phase 29 UI-SPEC and remains metadata-only.</done>
</task>

<task type="auto">
  <name>Task 3: Update deployment documentation and run final Phase 29 gates</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - render.yaml
    - frontend/vercel.json
    - frontend/package.json
    - .planning/REQUIREMENTS.md
    - .planning/phases/28-runtime-mode-production-readiness-foundation/28-VERIFICATION.md
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-RESEARCH.md
  </read_first>
  <action>
    Update `README.md` with a `Deployment guardrails & smoke profiles` section. Include exact documented values: Vercel root directory `frontend`; Vercel build command `npm run build`; Vercel install command `npm install`; Render root directory `backend`; Render build command `pip install -e .`; Render start command includes `alembic upgrade head` and `uvicorn app.main:app`; Render health path `/health/live`. Document env var keys `BEYOU_DEPLOY_PROFILE`, `BEYOU_EXPECTED_FRONTEND_URL`, `BEYOU_EXPECTED_BACKEND_URL`, `BEYOU_VERCEL_ROOT`, `NEXT_PUBLIC_API_BASE_URL`, `RUNTIME_MODE`, `ALLOW_DEMO_SEED`, `ALLOW_DEMO_LOGIN`, `FRONTEND_ORIGIN`, `FRONTEND_ORIGINS`, `SESSION_COOKIE_SECURE`, and `SESSION_COOKIE_SAMESITE` without example secret values. Document commands `npm --prefix frontend run guard:deploy`, `npm --prefix frontend run smoke:demo`, and `npm --prefix frontend run smoke:pilot`. State that public demo smoke may use seeded demo role accounts and may run when readiness is degraded/not_ready because demo seed/login can be intentionally enabled. State that production pilot smoke requires `/health/ready` status `ready`, `ALLOW_DEMO_SEED=false`, `ALLOW_DEMO_LOGIN=false`, exact HTTPS origins, secure cookies, current migrations, and no placeholder secrets; it must not depend on demo users. Add safe rollback steps: redeploy last known good frontend/backend build, revert deployment environment variables, rerun readiness, rerun guardrail, rerun the relevant smoke command, contact school/pilot owner if real users are affected, and do not use destructive database reset or raw data export as default rollback.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path README.md -Pattern "Deployment guardrails & smoke profiles","npm --prefix frontend run guard:deploy","npm --prefix frontend run smoke:demo","npm --prefix frontend run smoke:pilot","Vercel root directory.*frontend","Render root directory.*backend"; Set-Location D:\BeYou\backend; python -m pytest tests\test_phase7_readiness.py tests\test_phase11_operations_visibility.py tests\test_demo_seed.py -q; python -m ruff check .; Set-Location D:\BeYou\frontend; node --test scripts\deployment-guardrails.test.mjs scripts\smoke-profiles.test.mjs; npm test -- tests\phase11-operations-ui.test.tsx; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Deployment guardrails & smoke profiles`.
    - `README.md` contains `Vercel root directory`.
    - `README.md` contains `frontend`.
    - `README.md` contains `Render root directory`.
    - `README.md` contains `backend`.
    - `README.md` contains `npm --prefix frontend run guard:deploy`.
    - `README.md` contains `npm --prefix frontend run smoke:demo`.
    - `README.md` contains `npm --prefix frontend run smoke:pilot`.
    - `README.md` contains `ALLOW_DEMO_SEED=false`.
    - `README.md` contains `ALLOW_DEMO_LOGIN=false`.
    - `README.md` contains `không phụ thuộc` or `must not depend on demo users`.
    - `README.md` contains `do not use destructive database reset` or Vietnamese equivalent.
  </acceptance_criteria>
  <done>Documentation and final gates prove Phase 29 integration across guardrail CLI, smoke split, backend metadata, UI, and docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| admin API -> React UI | Deployment/smoke metadata renders in browser. |
| docs -> operator action | README instructions influence real deployment and rollback behavior. |
| UI fixture/test data -> rendered DOM | Test fixtures may include forbidden labels to verify non-rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-29-04-01 | I | operations page | mitigate | Render only typed safe fields; tests reject secret/cookie/raw labels and demo email strings in DOM. |
| T-29-04-02 | R | smoke profile copy | mitigate | UI and README state demo smoke vs pilot smoke assumptions with exact command names. |
| T-29-04-03 | T | operator rollback docs | mitigate | README documents config/redeploy/readiness rollback and blocks destructive reset/raw export defaults. |
| T-29-04-04 | E | operations UI actions | mitigate | No deploy/reset/export/secret reveal buttons are added; existing admin auth boundary remains unchanged. |
| T-29-04-05 | I | raw student data in UI | mitigate | UI tests preserve absence of notes, transcripts, answers, SOS notes, emails, raw IDs, exports, and risk drilldowns. |
</threat_model>

<verification>
Run:
```powershell
Set-Location D:\BeYou\backend
python -m pytest tests\test_phase7_readiness.py tests\test_phase11_operations_visibility.py tests\test_demo_seed.py -q
python -m ruff check .

Set-Location D:\BeYou\frontend
node --test scripts\deployment-guardrails.test.mjs scripts\smoke-profiles.test.mjs
npm test -- tests\phase11-operations-ui.test.tsx
npm run lint
npm run build
```
</verification>

<success_criteria>
- Operations UI renders approved Deployment guardrails and Smoke profiles panels.
- Demo smoke and Production pilot smoke appear as separate cards.
- README documents root directories, env vars, guardrail/smoke commands, demo-vs-pilot behavior, and safe rollback.
- Final backend/frontend targeted tests, lint, and build pass.
</success_criteria>

<must_haves>
- DEPLOY-01: docs and UI expose guardrail command and deployment shape.
- DEPLOY-02: docs/UI preserve frontend API target safety requirements.
- DEPLOY-03: UI consumes backend safe CORS/cookie deployment metadata.
- DEPLOY-04: UI/docs separate demo smoke from pilot smoke.
- DEPLOY-05: README contains deployment, smoke, readiness, and rollback guidance.
</must_haves>

<output>
After completion, create `.planning/phases/29-deployment-guardrails-smoke-profiles/29-04-SUMMARY.md`.
</output>

---
phase: 29-deployment-guardrails-smoke-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - frontend/package.json
  - frontend/scripts/smoke-utils.mjs
  - frontend/scripts/smoke-profiles.test.mjs
  - frontend/scripts/demo-smoke.mjs
  - frontend/scripts/pilot-smoke.mjs
  - frontend/scripts/production-smoke.mjs
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-04
must_haves:
  truths:
    - "Operator can run npm --prefix frontend run smoke:demo for seeded public demo."
    - "Operator can run npm --prefix frontend run smoke:pilot for production pilot readiness."
    - "Pilot smoke requires /health/ready status ready and never logs in with demo users."
  artifacts:
    - path: "frontend/scripts/demo-smoke.mjs"
      provides: "Seeded public demo smoke command"
    - path: "frontend/scripts/pilot-smoke.mjs"
      provides: "Production pilot smoke command without demo dependency"
    - path: "frontend/scripts/smoke-utils.mjs"
      provides: "Shared URL, health, readiness, CORS helpers"
    - path: "frontend/scripts/smoke-profiles.test.mjs"
      provides: "Regression tests for separated smoke profiles"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/scripts/demo-smoke.mjs"
      via: "smoke:demo"
      pattern: "\"smoke:demo\": \"node scripts/demo-smoke.mjs\""
    - from: "frontend/package.json"
      to: "frontend/scripts/pilot-smoke.mjs"
      via: "smoke:pilot"
      pattern: "\"smoke:pilot\": \"node scripts/pilot-smoke.mjs\""
---

<objective>
Split deployment smoke checks into explicit demo and production-pilot profiles.

Purpose: Public demo validation can use seeded accounts, but pilot validation must prove readiness without demo users.
Output: `smoke:demo`, `smoke:pilot`, shared smoke helpers, and regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-deployment-guardrails-smoke-profiles/29-RESEARCH.md
@frontend/scripts/production-smoke.mjs
@frontend/scripts/deployment-guardrails.mjs
@frontend/package.json

<interfaces>
Existing smoke behavior to preserve for demo:
- checks frontend URL
- checks backend `/health/live`
- checks backend `/health/ready`
- checks credentialed CORS preflight for `/api/auth/login`
- logs in seeded demo roles:
  - `student.demo@beyou.local`
  - `teacher.demo@beyou.local`
  - `parent.demo@beyou.local`
  - `admin.demo@beyou.local`

Pilot smoke must not include those demo emails or `BEYOU_DEMO_PASSWORD`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add smoke profile regression tests</name>
  <files>frontend/scripts/smoke-profiles.test.mjs</files>
  <read_first>
    - frontend/scripts/production-smoke.mjs
    - frontend/scripts/deployment-guardrails.mjs
    - frontend/package.json
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-RESEARCH.md
  </read_first>
  <behavior>
    - `validateSmokeUrls` fails missing frontend/backend URLs for `production_pilot`.
    - `validateSmokeUrls` fails localhost or non-HTTPS pilot URLs.
    - `evaluateReadinessForProfile("production_pilot", { status: 200, body: { status: "ready" } })` passes.
    - `evaluateReadinessForProfile("production_pilot", { status: 503, body: { status: "not_ready" } })` fails.
    - `evaluateReadinessForProfile("public_demo", { status: 503, body: { status: "not_ready" } })` warns or passes with explicit demo evidence.
    - `pilot-smoke.mjs` source does not contain `student.demo@beyou.local`, `teacher.demo@beyou.local`, `parent.demo@beyou.local`, `admin.demo@beyou.local`, or `BEYOU_DEMO_PASSWORD`.
  </behavior>
  <action>
    Create `frontend/scripts/smoke-profiles.test.mjs` with Node `node:test`. Import `validateSmokeUrls` and `evaluateReadinessForProfile` from `./smoke-utils.mjs`. Include a source-read assertion that reads `scripts/pilot-smoke.mjs` when present and rejects demo-account strings. Tests must not call live Vercel or Render URLs.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; node --test scripts\smoke-profiles.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/scripts/smoke-profiles.test.mjs` contains `evaluateReadinessForProfile`.
    - `frontend/scripts/smoke-profiles.test.mjs` contains `production_pilot`.
    - `frontend/scripts/smoke-profiles.test.mjs` contains `student.demo@beyou.local`.
    - `frontend/scripts/smoke-profiles.test.mjs` contains `BEYOU_DEMO_PASSWORD`.
  </acceptance_criteria>
  <done>Smoke profile tests define demo-vs-pilot behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared smoke helpers and explicit demo/pilot scripts</name>
  <files>frontend/scripts/smoke-utils.mjs, frontend/scripts/demo-smoke.mjs, frontend/scripts/pilot-smoke.mjs, frontend/scripts/production-smoke.mjs, frontend/package.json</files>
  <read_first>
    - frontend/scripts/smoke-profiles.test.mjs
    - frontend/scripts/production-smoke.mjs
    - frontend/scripts/deployment-guardrails.mjs
    - frontend/package.json
    - .planning/phases/29-deployment-guardrails-smoke-profiles/29-RESEARCH.md
  </read_first>
  <action>
    Create `frontend/scripts/smoke-utils.mjs` exporting `normalizeUrl`, `validateSmokeUrls`, `record`, `assertFetch`, `checkFrontendReachable`, `checkBackendLive`, `checkReadiness`, `evaluateReadinessForProfile`, `checkCredentialedCorsPreflight`, and `cookieFrom`. Implement `frontend/scripts/demo-smoke.mjs` to preserve the current seeded role flow from `production-smoke.mjs`, using four demo emails and `BEYOU_DEMO_PASSWORD`, and print final marker `DEMO_SMOKE_PASS`. Implement `frontend/scripts/pilot-smoke.mjs` to read `BEYOU_FRONTEND_URL`, `BEYOU_BACKEND_URL`, and `NEXT_PUBLIC_API_BASE_URL`, require HTTPS non-local URLs, require API base equal backend URL when provided, check frontend reachability, backend `/health/live`, backend `/health/ready`, and credentialed CORS preflight. Pilot smoke must fail unless readiness JSON status is exactly `ready`; it must not perform `/api/auth/login` POST and must not contain demo emails or `BEYOU_DEMO_PASSWORD`. Replace `frontend/scripts/production-smoke.mjs` with a compatibility runner that prints `smoke:production delegates to smoke:demo; use smoke:pilot for production_pilot readiness.` then imports/runs demo smoke. Update package scripts to include `"smoke:demo": "node scripts/demo-smoke.mjs"`, `"smoke:pilot": "node scripts/pilot-smoke.mjs"`, `"smoke:production": "node scripts/production-smoke.mjs"`, and `"test:smoke-profiles": "node --test scripts/smoke-profiles.test.mjs"`.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; node --test scripts\smoke-profiles.test.mjs; Select-String -Path package.json -Pattern '"smoke:demo"','"smoke:pilot"','"smoke:production"'; Select-String -Path scripts\production-smoke.mjs -Pattern "delegates to smoke:demo"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"smoke:demo": "node scripts/demo-smoke.mjs"`.
    - `frontend/package.json` contains `"smoke:pilot": "node scripts/pilot-smoke.mjs"`.
    - `frontend/package.json` contains `"test:smoke-profiles": "node --test scripts/smoke-profiles.test.mjs"`.
    - `frontend/scripts/demo-smoke.mjs` contains `student.demo@beyou.local`.
    - `frontend/scripts/pilot-smoke.mjs` does not contain `student.demo@beyou.local`.
    - `frontend/scripts/pilot-smoke.mjs` does not contain `BEYOU_DEMO_PASSWORD`.
    - `frontend/scripts/pilot-smoke.mjs` contains `status !== "ready"` or equivalent exact ready failure logic.
    - `frontend/scripts/production-smoke.mjs` contains `delegates to smoke:demo`.
  </acceptance_criteria>
  <done>Demo and pilot smoke profiles are separate commands with different readiness and identity assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| deployed app -> smoke scripts | Smoke scripts fetch public Vercel/Render endpoints. |
| demo credentials -> demo smoke | Seeded demo credentials are used only for public demo validation. |
| pilot smoke -> readiness endpoint | Pilot smoke trusts only public-safe readiness status and CORS headers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-29-02-01 | S | pilot-smoke.mjs | mitigate | Do not include demo emails/password; tests grep pilot source for demo strings. |
| T-29-02-02 | R | smoke:production alias | mitigate | Compatibility script explicitly states it delegates to demo and points to `smoke:pilot`. |
| T-29-02-03 | I | smoke output | mitigate | Output route/status evidence only; do not print cookies, session IDs, credentials, or response bodies. |
| T-29-02-04 | D | live smoke network calls | accept | Smoke is operator-invoked; failures are surfaced as command exit 1 without retry loops. |
</threat_model>

<verification>
Run:
```powershell
Set-Location D:\BeYou\frontend
node --test scripts\smoke-profiles.test.mjs
npm run lint
```
</verification>

<success_criteria>
- `npm --prefix frontend run smoke:demo` exists and uses seeded role accounts.
- `npm --prefix frontend run smoke:pilot` exists and requires readiness `ready`.
- Pilot smoke has no demo-account or demo-password dependency.
- `smoke:production` no longer creates false confidence because it declares demo delegation.
</success_criteria>

<must_haves>
- DEPLOY-02: smoke URL validation rejects unsafe pilot frontend/API/backend targets.
- DEPLOY-04: demo and production-pilot smoke are separate commands with separate assumptions.
</must_haves>

<output>
After completion, create `.planning/phases/29-deployment-guardrails-smoke-profiles/29-02-SUMMARY.md`.
</output>

---
phase: 32-privacy-security-release-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/scripts/release-gates.test.mjs
  - frontend/package.json
autonomous: true
requirements:
  - QA-02
  - QA-06
must_haves:
  truths:
    - "Node release gates prove deploy guardrails and smoke profiles are deterministic when live pilot credentials are absent."
    - "Pilot smoke remains separate from demo smoke and requires readiness `ready` without demo-user dependency."
    - "Guardrail and smoke outputs remain metadata-only."
  artifacts:
    - path: "frontend/scripts/release-gates.test.mjs"
      provides: "Phase 32 Node release-gate assertions"
    - path: "frontend/package.json"
      provides: "Executable release-gate script"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/scripts/release-gates.test.mjs"
      via: "test:release-gates script"
      pattern: "\"test:release-gates\""
    - from: "frontend/scripts/release-gates.test.mjs"
      to: "frontend/scripts/pilot-smoke.mjs"
      via: "source and behavior assertions"
      pattern: "pilot-smoke"
---

<objective>
Add deterministic Node release-gate tests for deployment guardrails and smoke profiles.

Purpose: QA-02 and QA-06 require deploy/smoke behavior to be provable locally, while live `smoke:pilot` is constrained when safe pilot URL/configuration is absent per D-10, D-11, D-12, and D-23.
Output: `frontend/scripts/release-gates.test.mjs` and `test:release-gates` npm script.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
@.planning/phases/32-privacy-security-release-gates/32-RESEARCH.md
@frontend/scripts/deployment-guardrails.mjs
@frontend/scripts/deployment-guardrails.test.mjs
@frontend/scripts/smoke-profiles.test.mjs
@frontend/scripts/demo-smoke.mjs
@frontend/scripts/pilot-smoke.mjs
@frontend/scripts/production-smoke.mjs
@frontend/scripts/smoke-utils.mjs
@frontend/package.json
@render.yaml
@README.md

<interfaces>
Existing npm scripts:
- `guard:deploy`: `node scripts/deployment-guardrails.mjs`
- `test:deploy-guardrails`: `node --test scripts/deployment-guardrails.test.mjs`
- `test:smoke-profiles`: `node --test scripts/smoke-profiles.test.mjs`
- `smoke:demo`: `node scripts/demo-smoke.mjs`
- `smoke:pilot`: `node scripts/pilot-smoke.mjs`
- `smoke:production`: `node scripts/production-smoke.mjs`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic QA-02 deploy/smoke release-gate Node tests</name>
  <files>frontend/scripts/release-gates.test.mjs</files>
  <read_first>
    frontend/scripts/deployment-guardrails.mjs
    frontend/scripts/deployment-guardrails.test.mjs
    frontend/scripts/smoke-profiles.test.mjs
    frontend/scripts/smoke-utils.mjs
    frontend/scripts/pilot-smoke.mjs
    frontend/scripts/demo-smoke.mjs
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - `PHASE32_NODE_REQUIREMENT_IDS` contains exactly `["QA-02", "QA-06"]`.
    - Production-pilot deploy guard validation fails unsafe Render env values: `RUNTIME_MODE=public_demo`, `ALLOW_DEMO_SEED=true`, `ALLOW_DEMO_LOGIN=true`, local/non-HTTPS frontend origins, insecure cookie settings.
    - Production-pilot smoke URL validation fails missing, localhost, non-HTTPS, and mismatched `NEXT_PUBLIC_API_BASE_URL`.
    - `pilot-smoke.mjs` source contains no demo emails and no `BEYOU_DEMO_PASSWORD`.
    - `pilot-smoke.mjs` requires readiness `ready`; `demo-smoke.mjs` may use demo accounts; `production-smoke.mjs` delegates to demo and says to use `smoke:pilot` for production-pilot readiness.
  </behavior>
  <action>
    Create `frontend/scripts/release-gates.test.mjs` using Node built-in `node:test` and `node:assert/strict`, not a new test framework per D-03. Import `validateDeploymentConfig`, `validateFrontendApiTarget`, `validateRenderEnvExpectations`, and `formatGuardrailResults` from `deployment-guardrails.mjs`; import `validateSmokeUrls` and `evaluateReadinessForProfile` from `smoke-utils.mjs`. Add source-grep tests using `readFileSync` for `pilot-smoke.mjs`, `demo-smoke.mjs`, and `production-smoke.mjs`. Test names must include `QA-02` or `QA-06`. Do not require real pilot URL, real student accounts, real school domains, or secrets per D-12.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; node --test scripts\release-gates.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\frontend\scripts\release-gates.test.mjs -Pattern "QA-02","QA-06","validateRenderEnvExpectations","validateSmokeUrls","pilot-smoke.mjs","BEYOU_DEMO_PASSWORD","production pilot readiness is not ready"` finds all listed strings.
    - `Set-Location D:\BeYou\frontend; node --test scripts\release-gates.test.mjs` exits 0.
  </acceptance_criteria>
  <done>Deterministic Node release-gate tests prove deploy and smoke split semantics without live pilot credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release-gate Node test into package scripts</name>
  <files>frontend/package.json</files>
  <read_first>
    frontend/package.json
    frontend/scripts/release-gates.test.mjs
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <action>
    Add exactly one npm script to `frontend/package.json`: `"test:release-gates": "node --test scripts/release-gates.test.mjs"`. Preserve existing scripts `guard:deploy`, `test:deploy-guardrails`, `test:smoke-profiles`, `smoke:demo`, `smoke:pilot`, `smoke:production`, and `lint`. Do not add dependencies and do not rename existing scripts.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm run test:release-gates; npm run test:deploy-guardrails; npm run test:smoke-profiles</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\frontend\package.json -Pattern '"test:release-gates": "node --test scripts/release-gates.test.mjs"'` finds exactly one match.
    - The three npm test commands exit 0.
  </acceptance_criteria>
  <done>`test:release-gates` is executable and existing deploy/smoke tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert deploy/smoke output remains metadata-only</name>
  <files>frontend/scripts/release-gates.test.mjs</files>
  <read_first>
    frontend/scripts/release-gates.test.mjs
    frontend/scripts/deployment-guardrails.mjs
    frontend/scripts/deployment-guardrails.test.mjs
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - `formatGuardrailResults` output includes statuses, keys, evidence, remediation, and command metadata only.
    - Output does not include raw secret env labels or values: `DATABASE_URL`, `SESSION_COOKIE_NAME`, `SMTP_PASSWORD`, `FREEMODEL_API_KEY`, `client_secret`, `access_token`, `refresh_token`, `id_token`.
    - Output does not include demo emails, raw origins with credentials, or token-like `eyJ` strings per D-04 and D-08.
    - Tests explicitly document that live `smoke:pilot` is constrained unless `BEYOU_FRONTEND_URL`, `BEYOU_BACKEND_URL`, `NEXT_PUBLIC_API_BASE_URL`, and a ready production-pilot deployment exist per D-11 and D-23.
  </behavior>
  <action>
    Extend `frontend/scripts/release-gates.test.mjs` with metadata-only output assertions and a test named `QA-06 live pilot smoke is documented as constrained without safe deployment env`. The test must assert the source file contains the exact string `smoke:pilot constrained without BEYOU_FRONTEND_URL, BEYOU_BACKEND_URL, NEXT_PUBLIC_API_BASE_URL, and readiness ready` as a constant comment or assertion message. This is documentation inside the deterministic test, not a pass for live smoke.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm run test:release-gates</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\frontend\scripts\release-gates.test.mjs -Pattern "metadata-only","DATABASE_URL","SESSION_COOKIE_NAME","smoke:pilot constrained without BEYOU_FRONTEND_URL, BEYOU_BACKEND_URL, NEXT_PUBLIC_API_BASE_URL, and readiness ready"` finds all listed strings.
    - `npm run test:release-gates` exits 0.
  </acceptance_criteria>
  <done>QA-02/QA-06 deploy and smoke gate outputs remain metadata-only and live pilot constraints are explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env→guardrail output | Deployment env keys and URLs are evaluated by CLI output. |
| live URLs→smoke scripts | Public or pilot URLs influence smoke pass/fail evidence. |
| package script→release gate | Operators rely on npm scripts to run the intended gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | I | `deployment-guardrails.mjs` output | mitigate | Node release tests assert guardrail output omits secret labels/values, demo emails, raw credentials, and token-like strings. |
| T-32-06 | S | `pilot-smoke.mjs` | mitigate | Source tests assert pilot smoke has no demo account/password dependency and requires readiness `ready`. |
| T-32-07 | R | live smoke constraints | mitigate | Release-gate test and later docs require constrained live pilot smoke to be recorded explicitly, not silently marked passed. |
| T-32-08 | T | `package.json` scripts | mitigate | Script wiring is exact and existing deploy/smoke script names are preserved. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\frontend; npm run test:release-gates`
- `Set-Location D:\BeYou\frontend; npm run test:deploy-guardrails`
- `Set-Location D:\BeYou\frontend; npm run test:smoke-profiles`
</verification>

<success_criteria>
QA-02 deterministic deploy/smoke behavior is executable locally, QA-06 includes the Node release-gate command, and live `smoke:pilot` constraints are explicit when real pilot deployment inputs are absent.
</success_criteria>

<output>
After completion, create `.planning/phases/32-privacy-security-release-gates/32-02-SUMMARY.md`
</output>


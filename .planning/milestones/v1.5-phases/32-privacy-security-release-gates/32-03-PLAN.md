---
phase: 32-privacy-security-release-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/phase32-release-gates-ui.test.tsx
autonomous: true
requirements:
  - QA-03
  - QA-04
  - QA-05
must_haves:
  truths:
    - "Frontend gates prove no browser token storage is introduced."
    - "Frontend gates prove operations UI stays metadata-only with no exports, reset controls, risk leaderboards, or per-student drilldowns."
    - "Frontend gates prove login/auth and privacy routing preserve existing backend-owned session behavior."
  artifacts:
    - path: "frontend/tests/phase32-release-gates-ui.test.tsx"
      provides: "Phase 32 frontend privacy and UI guardrail tests"
  key_links:
    - from: "frontend/tests/phase32-release-gates-ui.test.tsx"
      to: "frontend/lib/api.ts"
      via: "credentials include and no token storage assertions"
      pattern: "credentials: \"include\""
    - from: "frontend/tests/phase32-release-gates-ui.test.tsx"
      to: "frontend/app/(authenticated)/admin/operations/page.tsx"
      via: "DOM redline assertions"
      pattern: "AdminOperationsPage"
---

<objective>
Add frontend privacy/UI release gates without creating new product UI.

Purpose: Phase 32 has a UI hint only for frontend tests and operations surfaces; this plan adds tests/grep gates, not a UI spec or product surface.
Output: `frontend/tests/phase32-release-gates-ui.test.tsx`
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
@.planning/phases/32-privacy-security-release-gates/32-RESEARCH.md
@frontend/lib/api.ts
@frontend/lib/auth.ts
@frontend/lib/admin-operations-api.ts
@frontend/app/login/page.tsx
@frontend/app/(authenticated)/layout.tsx
@frontend/app/(authenticated)/admin/operations/page.tsx
@frontend/tests/auth-portals.test.tsx
@frontend/tests/phase11-operations-ui.test.tsx
@frontend/tests/phase25-admin-policy-operations-ui.test.tsx
@frontend/tests/phase31-school-pilot-operations-ui.test.tsx

<interfaces>
Frontend patterns:
- `apiFetch` must call `fetch(..., { credentials: "include" })`.
- `AuthUser` must remain response metadata only and should not include browser token fields.
- Operations UI consumes `AdminOperationsDashboard` and renders metadata/count/status/command fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QA-03 no-token frontend auth gate</name>
  <files>frontend/tests/phase32-release-gates-ui.test.tsx</files>
  <read_first>
    frontend/tests/auth-portals.test.tsx
    frontend/lib/api.ts
    frontend/lib/auth.ts
    frontend/app/login/page.tsx
    frontend/app/(authenticated)/layout.tsx
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - `PHASE32_FRONTEND_REQUIREMENT_IDS` contains exactly `["QA-03", "QA-04", "QA-05"]`.
    - `apiFetch` uses `credentials: "include"` and does not write `localStorage`, `sessionStorage`, cookies, access tokens, refresh tokens, or id tokens.
    - Auth source files do not contain `localStorage.setItem`, `sessionStorage.setItem`, `access_token`, `refresh_token`, `id_token`, `oauth`, `oidc`, `callback`, or `authorize_redirect`.
    - Login production-pilot capabilities hide demo role shortcuts while preserving email/password submit path per D-13 and D-15.
  </behavior>
  <action>
    Create `frontend/tests/phase32-release-gates-ui.test.tsx` using Vitest and Testing Library patterns from `auth-portals.test.tsx`. Include source-grep assertions using `readFileSync(join(process.cwd(), "..."))` against `lib/api.ts`, `lib/auth.ts`, `app/login/page.tsx`, and `app/(authenticated)/layout.tsx`. Mock `fetch` as in existing tests. Do not add OAuth/OIDC callback, token exchange, browser token storage, or new login product behavior per D-15.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase32-release-gates-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\frontend\tests\phase32-release-gates-ui.test.tsx -Pattern "QA-03","credentials: \\\"include\\\"","localStorage.setItem","sessionStorage.setItem","access_token","refresh_token","id_token"` finds all listed gate strings.
    - Targeted Vitest command exits 0.
  </acceptance_criteria>
  <done>QA-03 frontend no-token and auth capability behavior is explicitly release-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add QA-04 privacy routing and cross-role UI source gate</name>
  <files>frontend/tests/phase32-release-gates-ui.test.tsx</files>
  <read_first>
    frontend/tests/phase24-reason-access-ui.test.tsx
    frontend/tests/phase23-mood-note-sharing-ui.test.tsx
    frontend/app/(authenticated)/layout.tsx
    frontend/app/(authenticated)/admin/operations/page.tsx
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - Authenticated layout preserves privacy acknowledgement redirect before protected student content renders per D-16.
    - UI source does not add raw private notes, chatbot transcripts, self-check answers, SOS notes, raw emails, raw IDs, provider subjects, raw claims, or free-text reason displays outside existing controlled support surfaces per D-17.
    - Selective mood-note sharing behavior is not removed; tests reference existing `phase23-mood-note-sharing-ui.test.tsx` as part of QA-04 command set.
  </behavior>
  <action>
    Extend `frontend/tests/phase32-release-gates-ui.test.tsx` with tests named `QA-04 authenticated layout keeps privacy acknowledgement gate before student content` and `QA-04 frontend sources do not add raw private data display markers`. Source-grep `app/(authenticated)/layout.tsx` for `privacy_acknowledgement_required`, `router.push(\`/privacy?next=`, and `studentPathRequiresPrivacy`. Source-grep operations page for absence of unsafe render markers except static warning copy that rejects them. The forbidden regex must include `private_note|sos_note|transcript|self_check_answer|scenario_answer|provider_subject|raw_claims|student_id|recipient_id|access_reason_text`.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase32-release-gates-ui.test.tsx tests\phase23-mood-note-sharing-ui.test.tsx tests\phase24-reason-access-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\frontend\tests\phase32-release-gates-ui.test.tsx -Pattern "QA-04","privacy_acknowledgement_required","studentPathRequiresPrivacy","phase23-mood-note-sharing-ui.test.tsx","phase24-reason-access-ui.test.tsx"` finds all listed strings.
    - Targeted Vitest command exits 0.
  </acceptance_criteria>
  <done>QA-04 frontend privacy routing and cross-role privacy invariants are release-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add QA-05 operations UI redline gate</name>
  <files>frontend/tests/phase32-release-gates-ui.test.tsx</files>
  <read_first>
    frontend/tests/phase31-school-pilot-operations-ui.test.tsx
    frontend/tests/phase25-admin-policy-operations-ui.test.tsx
    frontend/tests/phase11-operations-ui.test.tsx
    frontend/app/(authenticated)/admin/operations/page.tsx
    frontend/lib/admin-operations-api.ts
    .planning/phases/31-school-pilot-operations-safe-launch/31-CONTEXT.md
    .planning/phases/32-privacy-security-release-gates/32-CONTEXT.md
  </read_first>
  <behavior>
    - Operations UI renders aggregate metadata, statuses, checklist items, safe command names, and remediation only per D-19/D-20.
    - DOM does not contain raw emails, raw IDs, provider subjects, raw claims, private notes, SOS notes, transcripts, answers, free-text reasons, export/download controls, reset controls, risk leaderboard labels, or per-student drilldown links per D-17 and D-19.
    - Copy remains support-not-surveillance and does not rank students by risk per D-21.
  </behavior>
  <action>
    Extend `frontend/tests/phase32-release-gates-ui.test.tsx` with a fixture dashboard that injects unsafe extra fields such as `student_email`, `student_id`, `provider_subject`, `raw_claims`, `private_note`, `sos_note`, `transcript`, `self_check_answer`, `scenario_answer`, `reason_text`, `export_url`, `risk_leaderboard`, and `pilot-owner@example.test`. Render `AdminOperationsPage` and assert `document.body.textContent` excludes the unsafe values and `screen.queryByRole("button"|"link", { name: /Xuất|Export|Download|Tải xuống|reset|drilldown|Chi tiết học sinh|xếp hạng nguy cơ|risk leaderboard/i })` returns null. Assert safe command strings `npm --prefix frontend run guard:deploy`, `npm --prefix frontend run smoke:demo`, and `npm --prefix frontend run smoke:pilot` remain visible when present in metadata.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase32-release-gates-ui.test.tsx tests\phase31-school-pilot-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx tests\phase11-operations-ui.test.tsx; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path D:\BeYou\frontend\tests\phase32-release-gates-ui.test.tsx -Pattern "QA-05","risk leaderboard","xếp hạng nguy cơ","guard:deploy","smoke:demo","smoke:pilot"` finds all listed strings.
    - Targeted Vitest and lint commands exit 0.
  </acceptance_criteria>
  <done>QA-05 frontend operations/privacy redlines are explicitly release-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→backend API | Frontend auth requests must use HttpOnly cookie session via `credentials: include`. |
| backend operations JSON→DOM | Metadata-only operations payload is rendered in admin UI. |
| protected route→student content | Authenticated layout decides whether protected student content renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-09 | I | `AdminOperationsPage` | mitigate | DOM tests inject unsafe fields and assert they are not rendered. |
| T-32-10 | E | frontend auth | mitigate | Source tests assert no browser token storage or OAuth callback/token exchange paths. |
| T-32-11 | I | student privacy routing | mitigate | Tests assert privacy acknowledgement redirect contract remains present before student content. |
| T-32-12 | R | UI release evidence | mitigate | Requirement ID constants and exact test names map QA-03/QA-04/QA-05 to Vitest gates. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\frontend; npm test -- tests\phase32-release-gates-ui.test.tsx`
- `Set-Location D:\BeYou\frontend; npm test -- tests\phase32-release-gates-ui.test.tsx tests\phase31-school-pilot-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx tests\phase11-operations-ui.test.tsx tests\auth-portals.test.tsx`
- `Set-Location D:\BeYou\frontend; npm run lint`
</verification>

<success_criteria>
Frontend Phase 32 gates pass and prove no-token auth, privacy acknowledgement routing, and metadata-only operations UI behavior without adding new product UI.
</success_criteria>

<output>
After completion, create `.planning/phases/32-privacy-security-release-gates/32-03-SUMMARY.md`
</output>


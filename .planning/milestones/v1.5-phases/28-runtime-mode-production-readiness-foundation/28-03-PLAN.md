---
phase: 28-runtime-mode-production-readiness-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 28-01
  - 28-02
files_modified:
  - backend/tests/test_phase7_readiness.py
  - backend/tests/test_demo_seed.py
  - backend/tests/test_auth_privacy_portals.py
  - backend/.env.example
  - render.yaml
  - README.md
autonomous: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-03
  - RUNTIME-04
  - RUNTIME-05
must_haves:
  truths:
    - "Tests prove runtime mode defaults, validation, production-pilot pass/fail readiness, public readiness minimality, and secret masking."
    - "Tests prove production pilot does not seed demo data and public demo still seeds intentionally."
    - "Tests prove production pilot blocks demo-user login without issuing session cookies."
    - "Config examples mark the current Render service as public_demo and document local safe defaults."
    - "README explains public demo readiness versus production-pilot readiness without adding deployment guard or smoke split work."
  artifacts:
    - path: "backend/tests/test_phase7_readiness.py"
      provides: "Runtime/readiness/masking regression tests"
      contains: "test_production_pilot_static_readiness_can_pass_safe_config"
    - path: "backend/tests/test_demo_seed.py"
      provides: "Seed safety regression tests"
      contains: "test_demo_seed_refuses_in_production_pilot_even_if_allow_demo_seed_true"
    - path: "backend/tests/test_auth_privacy_portals.py"
      provides: "Demo login production-pilot regression test"
      contains: "test_production_pilot_blocks_demo_user_login_without_session"
    - path: "backend/.env.example"
      provides: "Local runtime-mode example"
      contains: "RUNTIME_MODE=local_demo"
    - path: "render.yaml"
      provides: "Current public demo runtime declaration"
      contains: "RUNTIME_MODE"
    - path: "README.md"
      provides: "Operator-facing runtime/readiness explanation"
      contains: "production_pilot"
  key_links:
    - from: "backend/tests/test_phase7_readiness.py"
      to: "backend/app/services/readiness.py"
      via: "evaluate_static_readiness_checks"
      pattern: "evaluate_static_readiness_checks"
    - from: "backend/tests/test_demo_seed.py"
      to: "backend/app/seeds/demo_seed.py"
      via: "seed_demo_data"
      pattern: "seed_demo_data"
    - from: "backend/tests/test_auth_privacy_portals.py"
      to: "backend/app/api/auth.py"
      via: "POST /api/auth/login"
      pattern: "/api/auth/login"
---

<objective>
Add Phase 28 regression coverage plus minimal env/deployment/docs updates for the runtime-mode foundation.

Purpose: Make runtime production-pilot safety executable and visible to operators without adding frontend UI, deployment guard commands, smoke split, identity models, or pilot checklist UI.

Output: Targeted backend tests, sanitized environment examples, Render public-demo runtime config, and concise README runtime/readiness notes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-runtime-mode-production-readiness-foundation/28-CONTEXT.md
@.planning/phases/28-runtime-mode-production-readiness-foundation/28-RESEARCH.md
@backend/tests/test_phase7_readiness.py
@backend/tests/test_demo_seed.py
@backend/tests/test_auth_privacy_portals.py
@backend/.env.example
@render.yaml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime readiness and masking regression tests</name>
  <files>backend/tests/test_phase7_readiness.py</files>
  <read_first>
    - .planning/phases/28-runtime-mode-production-readiness-foundation/28-CONTEXT.md
    - .planning/phases/28-runtime-mode-production-readiness-foundation/28-RESEARCH.md
    - backend/app/core/config.py
    - backend/app/services/readiness.py
    - backend/app/services/admin_operations.py
    - backend/app/schemas/admin_operations.py
    - backend/tests/test_phase7_readiness.py
  </read_first>
  <action>
    Ensure `backend/tests/test_phase7_readiness.py` contains tests named exactly: `test_settings_runtime_mode_defaults_to_local_demo`, `test_settings_rejects_unknown_runtime_mode`, `test_production_pilot_readiness_flags_demo_seed_and_demo_login`, `test_production_pilot_readiness_flags_cookie_and_origin_drift`, `test_production_pilot_static_readiness_can_pass_safe_config`, `test_public_ready_response_remains_minimal_with_runtime_mode`, and `test_admin_operations_exposes_runtime_mode_without_sensitive_values`. If Plan 28-01 already added any of the production-pilot readiness tests, extend those existing tests rather than duplicating test names.

    Use direct `Settings(...)` and `evaluate_static_readiness_checks(settings)` for static tests. For the safe static pass test use: `RUNTIME_MODE="production_pilot"`, `ENVIRONMENT="production"`, `SESSION_COOKIE_SECURE=True`, `SESSION_COOKIE_SAMESITE="none"`, `FRONTEND_ORIGIN="https://pilot.example"`, `FRONTEND_ORIGINS=""`, `ALLOW_DEMO_SEED=False`, `ALLOW_DEMO_LOGIN=False`, `CHAT_PROVIDER="fallback"`, `SOS_EMAIL_PROVIDER="disabled"`. Assert all static checks are `pass`.

    For masking, serialize the admin operations dashboard and assert none of these substrings appear: `beyou_dev_password`, `DATABASE_URL`, `postgresql://`, `postgresql+psycopg://`, `FREEMODEL_API_KEY`, `SMTP_PASSWORD`, `BeYouDemo!2026`, `beyou_session`, `__Secure-beyou_session`, `student.demo@`, `teacher.demo@`, `parent.demo@`, `admin.demo@`.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests/test_phase7_readiness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` output for `tests/test_phase7_readiness.py` exits with code 0.
    - Test file contains exact test name `test_production_pilot_static_readiness_can_pass_safe_config`.
    - Test file asserts public readiness JSON keys equal `{"status", "generated_at"}`.
    - Test file asserts `runtime.mode == "production_pilot"` for admin operations metadata.
    - Test file rejects `beyou_dev_password`, `DATABASE_URL`, `FREEMODEL_API_KEY`, `SMTP_PASSWORD`, `BeYouDemo!2026`, and demo account emails from serialized readiness/operations responses.
  </acceptance_criteria>
  <done>Runtime readiness pass/fail and masking behavior are covered by targeted backend tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add seed and auth production-pilot regression tests</name>
  <files>backend/tests/test_demo_seed.py, backend/tests/test_auth_privacy_portals.py</files>
  <read_first>
    - .planning/phases/28-runtime-mode-production-readiness-foundation/28-CONTEXT.md
    - .planning/phases/28-runtime-mode-production-readiness-foundation/28-RESEARCH.md
    - backend/app/seeds/demo_seed.py
    - backend/app/api/auth.py
    - backend/app/core/config.py
    - backend/tests/test_demo_seed.py
    - backend/tests/test_auth_privacy_portals.py
  </read_first>
  <action>
    In `backend/tests/test_demo_seed.py`, add `test_demo_seed_refuses_in_production_pilot_even_if_allow_demo_seed_true`. Use `monkeypatch.setenv("RUNTIME_MODE", "production_pilot")`, `monkeypatch.setenv("ALLOW_DEMO_SEED", "true")`, and `get_settings.cache_clear()` before reading settings. Assert `seed_demo_data(db, get_settings()) is False` and assert the clean DB has no `User`, `StudentAdultLink`, `SelfCheckTest`, `Scenario`, or `MoodCheckInConfig` rows. Add `test_public_demo_seed_still_runs_when_enabled` or extend the existing idempotent seed test to set `RUNTIME_MODE="public_demo"` and `ALLOW_DEMO_SEED="true"` explicitly.

    Confirm `backend/tests/test_demo_seed.py` still contains the metadata-only connectivity assertions introduced by Plan 28-01: `dashboard.connectivity.frontend_origin_kind in {"local", "https", "other"}`, `dashboard.connectivity.allowed_origin_count == len(settings.allowed_frontend_origins)`, `dashboard.connectivity.session_cookie_secure == settings.session_cookie_secure`, `dashboard.connectivity.session_cookie_samesite == settings.session_cookie_samesite`, and serialization does not contain `session_cookie_name` or `beyou_session`. Do not reintroduce `dashboard.connectivity.session_cookie_name`.

    In `backend/tests/test_auth_privacy_portals.py`, add `test_production_pilot_blocks_demo_user_login_without_session`. Use `monkeypatch.setenv("RUNTIME_MODE", "production_pilot")`, `monkeypatch.setenv("ALLOW_DEMO_LOGIN", "false")`, `get_settings.cache_clear()`, and a fresh `TestClient(create_app())`. Create a demo user with password `secret123`, POST `/api/auth/login`, assert HTTP 403, assert response detail is exactly `Demo accounts are disabled in production pilot mode.`, assert `set-cookie` is absent, and assert `db.scalar(select(UserSession).where(UserSession.user_id == user.id)) is None`. Clear settings cache after the test.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests/test_demo_seed.py tests/test_auth_privacy_portals.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_demo_seed.py` contains exact test name `test_demo_seed_refuses_in_production_pilot_even_if_allow_demo_seed_true`.
    - `tests/test_demo_seed.py` proves public_demo with `ALLOW_DEMO_SEED=true` still creates four demo role users.
    - `tests/test_demo_seed.py` contains no `dashboard.connectivity.session_cookie_name` assertion.
    - `tests/test_demo_seed.py` asserts `frontend_origin_kind`, `allowed_origin_count`, `session_cookie_secure`, and `session_cookie_samesite` for admin operations connectivity.
    - `tests/test_auth_privacy_portals.py` contains exact test name `test_production_pilot_blocks_demo_user_login_without_session`.
    - The auth test asserts HTTP 403, exact detail `Demo accounts are disabled in production pilot mode.`, no `set-cookie`, and no `UserSession` row.
  </acceptance_criteria>
  <done>Seed and auth production-pilot safety are proven by targeted backend tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update env example, Render public-demo mode, and README runtime note</name>
  <files>backend/.env.example, render.yaml, README.md</files>
  <read_first>
    - .planning/phases/28-runtime-mode-production-readiness-foundation/28-CONTEXT.md
    - .planning/phases/28-runtime-mode-production-readiness-foundation/28-RESEARCH.md
    - backend/.env.example
    - render.yaml
    - README.md
  </read_first>
  <action>
    In `backend/.env.example`, add `RUNTIME_MODE=local_demo` immediately after `ENVIRONMENT=development` and add `ALLOW_DEMO_LOGIN=true` immediately after `ALLOW_DEMO_SEED=true`.

    In `render.yaml`, add env var `RUNTIME_MODE` with value `public_demo` after `ENVIRONMENT`, and add `ALLOW_DEMO_LOGIN` with value `true` after `ALLOW_DEMO_SEED`. Keep current `ALLOW_DEMO_SEED=true` and current seed command because this service is the public demo per D-09; do not add deployment guard commands or smoke split.

    In `README.md`, add a concise section after the live demo paragraph explaining:
    - Runtime modes are `local_demo`, `public_demo`, and `production_pilot`.
    - The hosted public demo is `public_demo` and may report `/health/ready` as `degraded` or `not_ready` while demo seeding is intentionally enabled.
    - A real `production_pilot` must use `ALLOW_DEMO_SEED=false`, `ALLOW_DEMO_LOGIN=false`, secure cookies, exact HTTPS origins, reachable database, current migrations, and no placeholder secrets.
    - Public `/health/ready` remains status/time only; admin readiness/operations are authorization-gated and metadata-only.
  </action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path backend\.env.example,render.yaml,README.md -Pattern "RUNTIME_MODE|ALLOW_DEMO_LOGIN|production_pilot|public_demo"; Set-Location D:\BeYou\backend; python -m pytest tests/test_phase7_readiness.py tests/test_demo_seed.py tests/test_auth_privacy_portals.py -q; python -m ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `backend/.env.example` contains exact line `RUNTIME_MODE=local_demo`.
    - `backend/.env.example` contains exact line `ALLOW_DEMO_LOGIN=true`.
    - `render.yaml` contains `key: RUNTIME_MODE` followed by `value: public_demo`.
    - `render.yaml` contains `key: ALLOW_DEMO_LOGIN` followed by `value: true`.
    - `README.md` contains `local_demo`, `public_demo`, and `production_pilot`.
    - `README.md` states public readiness remains status/time only and admin readiness/operations are authorization-gated metadata-only.
    - No frontend files are modified.
  </acceptance_criteria>
  <done>Runtime mode defaults and public-demo deployment intent are documented without expanding Phase 28 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test env mutation -> cached Settings | Tests that mutate env must clear settings cache to avoid cross-test config confusion. |
| repository config examples -> operators | Env examples and README can accidentally normalize unsafe pilot settings. |
| Render public demo config -> production-pilot assumptions | Current demo deployment must be explicitly marked public_demo so it is not mistaken for pilot-ready. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-28-13 | Tampering | Backend tests | mitigate | Add explicit production-pilot fail and pass tests for runtime, seed, login, cookie, origin, and masking behavior. |
| T-28-14 | Information Disclosure | Readiness/operations regressions | mitigate | Regression tests assert forbidden substrings are absent from serialized public/admin responses. |
| T-28-15 | Spoofing | `render.yaml` mode declaration | mitigate | Set current hosted demo to `RUNTIME_MODE=public_demo`; README distinguishes it from `production_pilot`. |
| T-28-16 | Information Disclosure | README/env examples | mitigate | Use only sanitized example values and no secrets; do not include provider credentials, SMTP credentials, cookie values, connection strings beyond local example, or raw student data. |
| T-28-17 | Elevation of Privilege | Demo login regression | mitigate | Add auth regression that denied production-pilot demo login creates no session and sets no cookie. |
| T-28-18 | Denial of Service | Settings cache drift in tests | mitigate | Tests that set env call `get_settings.cache_clear()` before and after mutation. |
</threat_model>

<verification>
Run full Phase 28 backend gate:
```powershell
Set-Location D:\BeYou\backend
python -m pytest tests/test_phase7_readiness.py tests/test_demo_seed.py tests/test_auth_privacy_portals.py -q
python -m ruff check .
```
</verification>

<success_criteria>
- All RUNTIME-01 through RUNTIME-05 requirements are covered by executable tests or config/docs checks.
- Render public demo is explicitly marked `public_demo`.
- Env example contains safe local defaults.
- README explains demo versus production-pilot readiness without adding frontend UI, deployment guard commands, smoke split, identity models, or pilot checklist UI.
</success_criteria>

<output>
After completion, create `.planning/phases/28-runtime-mode-production-readiness-foundation/28-03-SUMMARY.md`
</output>

---
phase: 31-school-pilot-operations-safe-launch
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - frontend/lib/admin-operations-api.ts
  - frontend/app/(authenticated)/admin/operations/page.tsx
  - frontend/tests/phase31-school-pilot-operations-ui.test.tsx
  - frontend/tests/phase11-operations-ui.test.tsx
autonomous: true
requirements: [PILOT-01, PILOT-02, PILOT-03, PILOT-05]
must_haves:
  truths:
    - "Existing `/admin/operations` page renders Phase 31 launch status, checklist, data safety, baseline setup, and rollback/handoff guidance."
    - "All Phase 31 frontend fields are optional/backward compatible."
    - "UI contains no export/download, raw JSON viewer, per-student drilldown, risk leaderboard, raw identifiers, raw notes, raw answers, raw SOS notes, raw free-text reasons, provider subjects, or claim values."
  artifacts:
    - path: "frontend/lib/admin-operations-api.ts"
      provides: "Optional Phase 31 TypeScript API contract"
      contains: "pilot_launch?: PilotLaunchSummary | null"
    - path: "frontend/app/(authenticated)/admin/operations/page.tsx"
      provides: "Admin operations Phase 31 panels"
      contains: "Sẵn sàng mở pilot trường học"
    - path: "frontend/tests/phase31-school-pilot-operations-ui.test.tsx"
      provides: "Phase 31 UI regression tests"
      contains: "Production pilot launch status"
  key_links:
    - from: "frontend/app/(authenticated)/admin/operations/page.tsx"
      to: "frontend/lib/admin-operations-api.ts"
      via: "optional pilot_* fields rendered with nullish fallbacks"
      pattern: "dashboard\\.pilot_launch"
---

<objective>
Render Phase 31 pilot launch operations metadata on the existing admin operations page.

Purpose: administrators need visible pilot launch readiness, demo/real data safety, baseline setup, and rollback/handoff guidance without leaving the metadata-only operations surface.
Output: frontend types, UI panels, and Vitest coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-school-pilot-operations-safe-launch/31-UI-SPEC.md
@frontend/lib/admin-operations-api.ts
@frontend/app/(authenticated)/admin/operations/page.tsx
@frontend/tests/phase11-operations-ui.test.tsx
@frontend/tests/phase25-admin-policy-operations-ui.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional Phase 31 frontend API types</name>
  <files>frontend/lib/admin-operations-api.ts, frontend/tests/phase31-school-pilot-operations-ui.test.tsx</files>
  <read_first>
    - frontend/lib/admin-operations-api.ts
    - frontend/tests/phase11-operations-ui.test.tsx
    - backend/app/schemas/admin_operations.py
    - .planning/phases/31-school-pilot-operations-safe-launch/31-02-PLAN.md
    - .planning/phases/31-school-pilot-operations-safe-launch/31-UI-SPEC.md
  </read_first>
  <action>In `frontend/lib/admin-operations-api.ts`, add exported type `PilotLaunchChecklistItem` with fields `key: string`, `label: string`, `status: "pass" | "warn" | "fail"`, `blocking: boolean`, `evidence: string`, `remediation: string | null`, and `command: string | null`. Add exported type `PilotLaunchSummary` with fields `status: "ready" | "needs_review" | "blocked"`, `generated_at: string`, and `checklist: PilotLaunchChecklistItem[]`. Add exported type `PilotDataSafetyBucket` with fields `key: string`, `label: string`, `count: number`, `status: "pass" | "warn" | "fail"`, `blocking: boolean`, `evidence: string`, and `remediation: string | null`. Add exported type `PilotDataSafetySummary` with fields `status: "safe" | "needs_review" | "blocked"` and `buckets: PilotDataSafetyBucket[]`. Add exported type `PilotHandoffItem` with fields `key: string`, `label: string`, `status: "pass" | "warn" | "fail"`, `guidance: string`, and `command: string | null`. Add exported type `PilotHandoffSummary` with fields `rollback: PilotHandoffItem[]`, `school_handoff: PilotHandoffItem[]`, and `baseline_setup: PilotHandoffItem[]`. Add optional fields to `AdminOperationsDashboard`: `pilot_launch?: PilotLaunchSummary | null`, `pilot_data_safety?: PilotDataSafetySummary | null`, and `pilot_handoff?: PilotHandoffSummary | null`. Do not change `getAdminOperationsDashboard()` URL construction or cookie-authenticated `apiFetch` usage. Create `frontend/tests/phase31-school-pilot-operations-ui.test.tsx` with a dashboard fixture containing all pilot fields using the exact field names above and a second fixture that omits all pilot fields to prove compatibility.</action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase31-school-pilot-operations-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/admin-operations-api.ts` contains `export type PilotLaunchChecklistItem`.
    - `frontend/lib/admin-operations-api.ts` contains `export type PilotLaunchSummary`.
    - `frontend/lib/admin-operations-api.ts` contains `export type PilotDataSafetyBucket`.
    - `frontend/lib/admin-operations-api.ts` contains `export type PilotDataSafetySummary`.
    - `frontend/lib/admin-operations-api.ts` contains `export type PilotHandoffItem`.
    - `frontend/lib/admin-operations-api.ts` contains `export type PilotHandoffSummary`.
    - `frontend/lib/admin-operations-api.ts` contains `rollback: PilotHandoffItem[]`, `school_handoff: PilotHandoffItem[]`, and `baseline_setup: PilotHandoffItem[]`.
    - `frontend/lib/admin-operations-api.ts` contains exact optional strings `pilot_launch?: PilotLaunchSummary | null`, `pilot_data_safety?: PilotDataSafetySummary | null`, and `pilot_handoff?: PilotHandoffSummary | null`.
    - `frontend/tests/phase31-school-pilot-operations-ui.test.tsx` contains a fixture without `pilot_launch`, `pilot_data_safety`, and `pilot_handoff`.
  </acceptance_criteria>
  <done>Frontend API contract accepts Phase 31 payloads without breaking old payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render pilot launch, checklist, data safety, baseline, and handoff panels</name>
  <files>frontend/app/(authenticated)/admin/operations/page.tsx, frontend/tests/phase31-school-pilot-operations-ui.test.tsx, frontend/tests/phase11-operations-ui.test.tsx</files>
  <read_first>
    - frontend/app/(authenticated)/admin/operations/page.tsx
    - frontend/lib/admin-operations-api.ts
    - frontend/tests/phase11-operations-ui.test.tsx
    - frontend/tests/phase25-admin-policy-operations-ui.test.tsx
    - .planning/phases/31-school-pilot-operations-safe-launch/31-UI-SPEC.md
  </read_first>
  <action>In `frontend/app/(authenticated)/admin/operations/page.tsx`, import the Phase 31 types. Update the header so the eyebrow contains exact `Vận hành pilot an toàn`, the main heading contains exact `Sẵn sàng mở pilot trường học`, and the intro contains exact `Theo dõi readiness, checklist launch, an toàn dữ liệu demo/thật và hướng dẫn rollback bằng metadata. Trang này không mở nội dung riêng tư của học sinh.` Also preserve the text `Vận hành metadata-only` somewhere in the header or intro so older Phase 11 UI tests still pass. Update loading copy to `Đang tải metadata vận hành pilot...` and error copy to `Chưa tải được metadata pilot. Hãy thử lại từ cổng quản trị hoặc kiểm tra readiness backend.` Extend `statusClass()` so `ready`, `safe`, `pass`, and `covered` use the safe accent class; `needs_review` and `warn` use warning class; `blocked` and `fail` use the red/destructive class. After the privacy boundary section and before deployment guardrails, render a full-width `Panel` titled `Production pilot launch status` with `data-testid="pilot-launch-status"`, an overall `StatusBadge`, and one of these exact copies based on status: `Pilot đang bị chặn. Hãy xử lý các mục fail trước khi dùng với dữ liệu học sinh thật.`, `Pilot cần rà soát thêm. Các mục warn không mở dữ liệu riêng tư nhưng cần xác nhận trước launch.`, or `Pilot sẵn sàng theo metadata hiện tại. Vẫn cần xác nhận vận hành với trường trước khi mở thực tế.` Render a `Panel` titled `Launch checklist` with `data-testid="pilot-launch-checklist"` that maps `dashboard.pilot_launch?.checklist ?? []`; empty copy must be exact `Chưa có checklist pilot. Hãy kiểm tra readiness và tải lại trang vận hành.` Render checklist cards with safe label, status badge, evidence, optional command, and optional remediation only. Render a `Panel` titled `Demo/real data safety` with `data-testid="pilot-data-safety"` using `dashboard.pilot_data_safety?.buckets ?? []`; empty copy exact `Chưa có metadata an toàn dữ liệu pilot. Không có dữ liệu thô được hiển thị.` Render a `Panel` titled `Baseline setup` with `data-testid="pilot-baseline-setup"` using `dashboard.pilot_handoff?.baseline_setup ?? []`; empty copy exact `Chưa có metadata thiết lập baseline. Hãy xác nhận nội dung, chính sách và cấu hình pilot trước khi mở cho trường.` Render a `Panel` titled `Rollback và handoff` with `data-testid="pilot-handoff-guidance"` that renders `rollback` and `school_handoff` item groups; empty copy exact `Chưa có hướng dẫn handoff. Hãy dùng quy trình rollback an toàn trong README và kiểm tra lại readiness.` Use existing `Panel`, `MetricCard`, `StatusBadge`, and `BucketList` patterns only. Do not add any buttons/actions for launch, export, download, reset, delete, raw JSON, drilldown, student details, or risk ranking.</action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase31-school-pilot-operations-ui.test.tsx tests\phase11-operations-ui.test.tsx</automated>
    <automated>Set-Location D:\BeYou\frontend; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains exact strings `Vận hành pilot an toàn`, `Sẵn sàng mở pilot trường học`, `Production pilot launch status`, `Launch checklist`, `Demo/real data safety`, `Baseline setup`, and `Rollback và handoff`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains all five `data-testid` values from the action.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains exact empty-state strings from the action.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` contains exact status copy strings `Pilot đang bị chặn`, `Pilot cần rà soát thêm`, and `Pilot sẵn sàng theo metadata hiện tại`.
    - `frontend/app/(authenticated)/admin/operations/page.tsx` does not contain `Export`, `Xuất`, `Download`, `Tải xuống`, `raw JSON`, `student.demo@`, `teacher.demo@`, `provider_subject`, `raw_claims`, `private_note`, `sos_note`, `transcript`, `self_check_answer`, `risk leaderboard`, `xếp hạng nguy cơ`, or `Chi tiết học sinh`.
    - `frontend/tests/phase31-school-pilot-operations-ui.test.tsx` asserts absence of export/download buttons, raw JSON viewers, drilldown links, raw emails, raw IDs, raw notes, transcripts, answers, SOS notes/reasons, provider subject strings, claim strings, and risk leaderboard/ranking language.
  </acceptance_criteria>
  <done>Admin operations page renders Phase 31 panels safely and accessibly.</done>
</task>

<task type="auto">
  <name>Task 3: Run frontend Phase 31 UI regression gate</name>
  <files>frontend/tests/phase31-school-pilot-operations-ui.test.tsx, frontend/app/(authenticated)/admin/operations/page.tsx, frontend/lib/admin-operations-api.ts</files>
  <read_first>
    - frontend/tests/phase31-school-pilot-operations-ui.test.tsx
    - frontend/app/(authenticated)/admin/operations/page.tsx
    - frontend/lib/admin-operations-api.ts
  </read_first>
  <action>Run the targeted frontend tests and lint. If old operations fixtures fail because `pilot_*` fields are absent, fix renderers and types with nullish fallbacks instead of requiring those fields. If text queries conflict because `Vận hành metadata-only` was moved, preserve the exact old text somewhere in the page while keeping the Phase 31 heading. Do not introduce export/download/reset/raw JSON/drilldown/risk ranking controls.</action>
  <verify>
    <automated>Set-Location D:\BeYou\frontend; npm test -- tests\phase31-school-pilot-operations-ui.test.tsx tests\phase11-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx</automated>
    <automated>Set-Location D:\BeYou\frontend; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Targeted Vitest command exits with code 0.
    - Frontend lint exits with code 0.
    - UI keeps optional field fallbacks for `dashboard.pilot_launch`, `dashboard.pilot_data_safety`, and `dashboard.pilot_handoff`.
  </acceptance_criteria>
  <done>Phase 31 operations UI passes targeted regression checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| admin operations API -> React UI | Backend pilot metadata is rendered to authenticated admin browser. |
| UI text -> operator behavior | Copy guides launch, handoff, and rollback behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-03-01 | I | operations UI | mitigate | Render only typed safe fields; tests reject raw emails, IDs, notes, transcripts, answers, SOS reasons, provider subjects, claims, exports, raw JSON, and drilldowns. |
| T-31-03-02 | E | UI controls | mitigate | Add no launch, reset, export, delete, role mapping, provider claim, or student drilldown controls. |
| T-31-03-03 | R | launch interpretation | mitigate | Copy states metadata readiness is not a final legal/clinical approval and still requires operational confirmation with school. |
| T-31-03-04 | D | older payload compatibility | mitigate | New `pilot_*` fields are optional with nullish fallbacks. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\frontend; npm test -- tests\phase31-school-pilot-operations-ui.test.tsx tests\phase11-operations-ui.test.tsx tests\phase25-admin-policy-operations-ui.test.tsx`
- `Set-Location D:\BeYou\frontend; npm run lint`
</verification>

<success_criteria>
- Existing `/admin/operations` page displays Phase 31 pilot launch panels.
- Optional fields do not crash older payloads.
- UI remains metadata-only and forbidden UI patterns are absent.
</success_criteria>

<output>
After completion, create `.planning/phases/31-school-pilot-operations-safe-launch/31-03-SUMMARY.md`.
</output>

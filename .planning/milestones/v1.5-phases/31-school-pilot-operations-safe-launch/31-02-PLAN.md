---
phase: 31-school-pilot-operations-safe-launch
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - backend/app/schemas/admin_operations.py
  - backend/app/services/admin_operations.py
  - backend/tests/test_phase31_school_pilot_operations.py
autonomous: true
requirements: [PILOT-02, PILOT-03, PILOT-05]
must_haves:
  truths:
    - "Admin operations exposes demo/real data safety as aggregate counts and statuses only."
    - "Production pilot flags active demo users, links, and walkthrough/config content without exposing raw rows."
    - "Baseline setup guidance supports real pilot launch without creating demo users, links, or walkthrough data."
  artifacts:
    - path: "backend/app/schemas/admin_operations.py"
      provides: "Pilot data safety and handoff schema contracts"
      contains: "class PilotDataSafetySummary"
    - path: "backend/app/services/admin_operations.py"
      provides: "Aggregate data safety and baseline guidance builders"
      contains: "_pilot_data_safety_summary"
    - path: "backend/tests/test_phase31_school_pilot_operations.py"
      provides: "Data safety privacy tests"
      contains: "test_pilot_data_safety_counts"
  key_links:
    - from: "backend/app/services/admin_operations.py"
      to: "backend/app/db/models.py"
      via: "aggregate count queries use is_demo/status only"
      pattern: "is_demo.is_"
---

<objective>
Add production pilot data-safety and baseline/handoff backend metadata to admin operations.

Purpose: operators must detect demo contamination and baseline readiness through counts/statuses only, without creating cleanup tools, raw exports, or drilldowns.
Output: optional `pilot_data_safety` and `pilot_handoff` backend fields plus tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-school-pilot-operations-safe-launch/31-CONTEXT.md
@.planning/phases/31-school-pilot-operations-safe-launch/31-RESEARCH.md
@backend/app/db/models.py
@backend/app/schemas/admin_operations.py
@backend/app/services/admin_operations.py
@backend/tests/test_phase31_school_pilot_operations.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pilot data safety and handoff schemas</name>
  <files>backend/app/schemas/admin_operations.py</files>
  <read_first>
    - backend/app/schemas/admin_operations.py
    - .planning/phases/31-school-pilot-operations-safe-launch/31-RESEARCH.md
  </read_first>
  <action>In `backend/app/schemas/admin_operations.py`, add class `PilotDataSafetyBucket(BaseModel)` with fields `key: str`, `label: str`, `count: int`, `status: ReadinessCheckStatus`, `blocking: bool`, `evidence: str`, and `remediation: str | None = None`. Add class `PilotDataSafetySummary(BaseModel)` with `status: Literal["safe", "needs_review", "blocked"]` and `buckets: list[PilotDataSafetyBucket]`. Add class `PilotHandoffItem(BaseModel)` with `key: str`, `label: str`, `status: ReadinessCheckStatus`, `guidance: str`, and `command: str | None = None`. Add class `PilotHandoffSummary(BaseModel)` with `rollback: list[PilotHandoffItem]`, `school_handoff: list[PilotHandoffItem]`, and `baseline_setup: list[PilotHandoffItem]`. Add optional fields to `AdminOperationsDashboardResponse`: `pilot_data_safety: PilotDataSafetySummary | None = None` and `pilot_handoff: PilotHandoffSummary | None = None` after `pilot_launch`. Keep all fields optional and do not remove existing dashboard fields.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m ruff check app\schemas\admin_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/admin_operations.py` contains `class PilotDataSafetyBucket`.
    - `backend/app/schemas/admin_operations.py` contains `class PilotDataSafetySummary`.
    - `backend/app/schemas/admin_operations.py` contains `class PilotHandoffItem`.
    - `backend/app/schemas/admin_operations.py` contains `class PilotHandoffSummary`.
    - `backend/app/schemas/admin_operations.py` contains `pilot_data_safety: PilotDataSafetySummary | None = None`.
    - `backend/app/schemas/admin_operations.py` contains `pilot_handoff: PilotHandoffSummary | None = None`.
  </acceptance_criteria>
  <done>Backend response contract includes optional data safety and handoff sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build aggregate pilot data safety buckets</name>
  <files>backend/app/services/admin_operations.py, backend/tests/test_phase31_school_pilot_operations.py</files>
  <read_first>
    - backend/app/services/admin_operations.py
    - backend/app/schemas/admin_operations.py
    - backend/app/db/models.py
    - backend/tests/test_phase31_school_pilot_operations.py
  </read_first>
  <action>In `backend/app/services/admin_operations.py`, import `PilotDataSafetyBucket`, `PilotDataSafetySummary`, `PilotHandoffItem`, and `PilotHandoffSummary`. Add helper `_pilot_data_bucket(key: str, label: str, count: int, status: str, blocking: bool, evidence: str, remediation: str | None = None) -> PilotDataSafetyBucket` that sanitizes evidence/remediation with `_safe_operation_text`. Add `_derive_data_safety_status(buckets: list[PilotDataSafetyBucket]) -> str` returning `"blocked"` if any bucket has `blocking is True`, `status == "fail"`, and `count > 0`; returning `"needs_review"` if any bucket has `status == "warn"` and `count > 0`; otherwise `"safe"`. Add `_count_demo_published_content(db)` helper or inline counts for these exact bucket keys: `demo_active_users`, `demo_active_links`, `demo_published_self_checks`, `demo_published_scenarios`, `demo_mood_configs`, `demo_policy_defaults`, `demo_notification_preferences`, `demo_reminder_states`, `demo_mood_shares`, `real_active_students`, and `real_active_adults`. Use SQLAlchemy aggregate count queries only: do not select or return row IDs, emails, titles, names, school/class values, notes, provider subjects, claims, or reason text. For production pilot, set `status="fail"` and `blocking=True` when `demo_active_users`, `demo_active_links`, `demo_published_self_checks`, `demo_published_scenarios`, or `demo_mood_configs` count is greater than zero. For production pilot, set `status="warn"` and `blocking=False` when `demo_policy_defaults`, `demo_notification_preferences`, `demo_reminder_states`, or `demo_mood_shares` count is greater than zero. For non-production-pilot runtime, demo buckets may be `pass` or `warn` but must not be blocking. For `real_active_students` and `real_active_adults`, use `blocking=False` and `status="pass"` even when count is zero. Evidence strings must be exact `count={n}; production_pilot=yes|no` and nothing else. Add `pilot_data_safety=_pilot_data_safety_summary(db, settings)` to `build_operations_dashboard()`. Add/extend backend tests `test_pilot_data_safety_counts_demo_rows_as_aggregate_metadata_only` and `test_pilot_data_safety_blocks_production_pilot_demo_dependencies`.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase31_school_pilot_operations.py -q</automated>
    <automated>Set-Location D:\BeYou\backend; python -m ruff check app\services\admin_operations.py tests\test_phase31_school_pilot_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/admin_operations.py` contains `_pilot_data_safety_summary`.
    - `backend/app/services/admin_operations.py` contains `_derive_data_safety_status`.
    - `backend/app/services/admin_operations.py` contains all exact bucket keys from the action.
    - `backend/app/services/admin_operations.py` contains `pilot_data_safety=_pilot_data_safety_summary`.
    - `backend/tests/test_phase31_school_pilot_operations.py` contains `test_pilot_data_safety_counts_demo_rows_as_aggregate_metadata_only`.
    - `backend/tests/test_phase31_school_pilot_operations.py` contains `test_pilot_data_safety_blocks_production_pilot_demo_dependencies`.
    - Backend tests assert dashboard JSON excludes `student.demo@beyou.local`, `teacher.demo@beyou.local`, UUID-like user/link IDs from seeded rows, raw self-check titles, raw scenario titles, `private_note`, `student_summary`, `sos_note`, `transcript`, `answer`, `reason_text`, and `provider_subject`.
  </acceptance_criteria>
  <done>Pilot data safety is visible only through aggregate counts and launch-safe statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add safe handoff and baseline setup metadata</name>
  <files>backend/app/services/admin_operations.py, backend/tests/test_phase31_school_pilot_operations.py</files>
  <read_first>
    - backend/app/services/admin_operations.py
    - backend/app/schemas/admin_operations.py
    - README.md
    - .planning/phases/31-school-pilot-operations-safe-launch/31-UI-SPEC.md
  </read_first>
  <action>In `backend/app/services/admin_operations.py`, add `_pilot_handoff_item(key: str, label: str, status: str, guidance: str, command: str | None = None) -> PilotHandoffItem` that sanitizes `guidance`. Add `_pilot_handoff_summary(db: OrmSession, settings: Settings) -> PilotHandoffSummary`. Return `rollback` items with exact keys and labels: `redeploy_known_good` / `Redeploy known good frontend/backend`, `rollback_config` / `Revert configuration`, `rerun_readiness_guardrails_smoke` / `Rerun readiness, guardrails, and smoke`, `notify_school_owner` / `Notify school owner`, `escalate_incident` / `Escalate incident`, `avoid_destructive_reset` / `Avoid destructive database reset`, and `avoid_raw_export` / `Avoid raw data export defaults`. Guidance must include the exact safe phrases `Redeploy the last known good Vercel frontend and Render backend build.`, `Revert deployment environment variables to the last known good values.`, `Run /health/ready, npm --prefix frontend run guard:deploy, and npm --prefix frontend run smoke:pilot.`, `Notify the school or pilot owner if real users are affected.`, `Escalate incidents through the agreed school support path.`, `Do not use destructive database reset as the default rollback path.`, and `Do not use raw data export as the default rollback path.` Return `school_handoff` items with exact keys `admin_contact_metadata`, `support_channel_metadata`, and `incident_path_metadata`, all `status="warn"` with static guidance that says contact paths are documented outside Peerlight AI and no raw contact details are stored in operations metadata. Return `baseline_setup` items with exact keys `baseline_self_checks`, `baseline_scenarios`, `baseline_mood_config`, `school_policy_defaults`, `in_app_only_reminders`, and `demo_seed_disabled_for_pilot`. Use non-demo content/policy counts for status where possible; `demo_seed_disabled_for_pilot` is pass only when `settings.is_production_pilot` and `settings.allow_demo_seed is False`. Add `pilot_handoff=_pilot_handoff_summary(db, settings)` to `build_operations_dashboard()`. Add tests `test_pilot_handoff_guidance_is_static_safe_metadata` and `test_baseline_setup_guidance_uses_counts_without_creating_demo_data` asserting the exact safe phrases and no raw contact emails, incident IDs, destructive action buttons, raw exports, student identifiers, or free-text reasons appear in serialized JSON.</action>
  <verify>
    <automated>Set-Location D:\BeYou\backend; python -m pytest tests\test_phase31_school_pilot_operations.py -q</automated>
    <automated>Set-Location D:\BeYou\backend; python -m ruff check app\services\admin_operations.py tests\test_phase31_school_pilot_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/admin_operations.py` contains `_pilot_handoff_summary`.
    - `backend/app/services/admin_operations.py` contains all rollback keys listed in the action.
    - `backend/app/services/admin_operations.py` contains all baseline keys listed in the action.
    - `backend/app/services/admin_operations.py` contains `pilot_handoff=_pilot_handoff_summary`.
    - `backend/tests/test_phase31_school_pilot_operations.py` contains `test_pilot_handoff_guidance_is_static_safe_metadata`.
    - `backend/tests/test_phase31_school_pilot_operations.py` contains `test_baseline_setup_guidance_uses_counts_without_creating_demo_data`.
    - Tests assert no rows are created by calling `build_operations_dashboard()`.
  </acceptance_criteria>
  <done>Dashboard backend provides static rollback/handoff and baseline setup guidance safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| database -> operations metadata | Demo/real row state becomes aggregate launch safety counts. |
| guidance constants -> operator action | Static rollback/handoff guidance influences real pilot operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-02-01 | I | data safety buckets | mitigate | Return aggregate counts/statuses only; tests reject emails, IDs, titles, notes, transcripts, answers, and provider subjects. |
| T-31-02-02 | T | rollback guidance | mitigate | Guidance explicitly forbids destructive database reset and raw export defaults. |
| T-31-02-03 | E | launch blockers | mitigate | Data safety status is informational metadata only and does not create privileges or cleanup actions. |
| T-31-02-04 | I | handoff contacts | mitigate | Store no contact emails, external incident IDs, school/class data, or free-text handoff details. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou\backend; python -m pytest tests\test_phase31_school_pilot_operations.py -q`
- `Set-Location D:\BeYou\backend; python -m ruff check app\services\admin_operations.py app\schemas\admin_operations.py tests\test_phase31_school_pilot_operations.py`
</verification>

<success_criteria>
- `pilot_data_safety` and `pilot_handoff` are optional metadata-only dashboard fields.
- Production pilot data safety flags active demo dependencies through aggregate buckets.
- Baseline/handoff guidance exists without write endpoints, demo data creation, raw contacts, destructive reset, or raw export defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/31-school-pilot-operations-safe-launch/31-02-SUMMARY.md`.
</output>

---
phase: 31-school-pilot-operations-safe-launch
plan: 04
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - README.md
  - .planning/PROJECT.md
autonomous: true
requirements: [PILOT-04, PILOT-05]
must_haves:
  truths:
    - "README documents production pilot launch checklist, baseline setup, rollback, and handoff guidance."
    - "Documentation rejects destructive database reset and raw data export defaults."
    - "Documentation explains real pilot baseline setup without demo seed/login or demo walkthrough dependencies."
  artifacts:
    - path: "README.md"
      provides: "Operator pilot launch and handoff guidance"
      contains: "School pilot launch checklist"
    - path: ".planning/PROJECT.md"
      provides: "Project state documentation for Phase 31 capability"
      contains: "school pilot operations"
  key_links:
    - from: "README.md"
      to: "frontend/package.json"
      via: "documented commands"
      pattern: "guard:deploy|smoke:pilot"
---

<objective>
Document safe production pilot launch, baseline setup, rollback, and handoff guidance.

Purpose: operators need repeatable instructions that match the dashboard metadata and explicitly avoid destructive or raw-data defaults.
Output: README updates and project context update.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-school-pilot-operations-safe-launch/31-CONTEXT.md
@.planning/phases/31-school-pilot-operations-safe-launch/31-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend README with school pilot launch checklist</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/31-school-pilot-operations-safe-launch/31-CONTEXT.md
  </read_first>
  <action>In `README.md`, after the `Runtime modes and readiness` or `Deployment guardrails & smoke profiles` sections, add a new section heading exactly `## School pilot launch checklist`. Include a checklist with exact items: `RUNTIME_MODE=production_pilot`, `ALLOW_DEMO_SEED=false`, `ALLOW_DEMO_LOGIN=false`, `/health/ready` returns `ready`, database migrations are at Alembic head, frontend origins are exact HTTPS origins, session cookies are Secure with SameSite=None, auth provider readiness metadata is safe, `npm --prefix frontend run guard:deploy` passes, `npm --prefix frontend run smoke:pilot` passes, and Phase 31 admin operations shows no active demo-user dependency for pilot launch. State that the admin operations dashboard is admin-gated and metadata-only. State that public demo smoke is not production-pilot proof.</action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path README.md -Pattern "School pilot launch checklist","RUNTIME_MODE=production_pilot","ALLOW_DEMO_SEED=false","ALLOW_DEMO_LOGIN=false","npm --prefix frontend run guard:deploy","npm --prefix frontend run smoke:pilot","metadata-only"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## School pilot launch checklist`.
    - `README.md` contains `RUNTIME_MODE=production_pilot`.
    - `README.md` contains `ALLOW_DEMO_SEED=false`.
    - `README.md` contains `ALLOW_DEMO_LOGIN=false`.
    - `README.md` contains `/health/ready`.
    - `README.md` contains `npm --prefix frontend run guard:deploy`.
    - `README.md` contains `npm --prefix frontend run smoke:pilot`.
    - `README.md` contains `public demo smoke is not production-pilot proof` or Vietnamese equivalent.
  </acceptance_criteria>
  <done>README includes safe launch checklist aligned with dashboard metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Document baseline setup and data-safety boundaries</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/phases/31-school-pilot-operations-safe-launch/31-RESEARCH.md
  </read_first>
  <action>In the new README pilot section, add subsection heading exactly `### Baseline setup for a real pilot`. Document that a real pilot should use non-demo baseline self-check content, non-demo scenario content, mood/check-in configuration, school privacy policy defaults, in-app-only reminder channels, and school handoff/support path documented outside raw operations metadata. State exactly that production pilot launch must not create or rely on public demo accounts, demo student-adult links, demo walkthrough content, demo mood configs, or demo note-sharing state. State that broad active real-user counts can be reviewed as metadata but zero real active users does not by itself prove launch unsafe because onboarding may happen after configuration approval. Do not include any real contact emails, student examples, school/class names, or raw identifiers.</action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path README.md -Pattern "Baseline setup for a real pilot","non-demo baseline self-check","in-app-only","must not create or rely on public demo accounts","zero real active users"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `### Baseline setup for a real pilot`.
    - `README.md` contains `non-demo baseline self-check`.
    - `README.md` contains `non-demo scenario`.
    - `README.md` contains `in-app-only reminder`.
    - `README.md` contains `must not create or rely on public demo accounts`.
    - `README.md` contains `demo student-adult links`.
    - `README.md` contains `zero real active users`.
    - `README.md` does not add `@example.com`, `student_id`, `teacher_id`, `class_name`, `school.example`, or raw contact email examples in the new section.
  </acceptance_criteria>
  <done>Baseline setup docs support real pilot launch without demo dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Document rollback and handoff without destructive/raw-data defaults</name>
  <files>README.md, .planning/PROJECT.md</files>
  <read_first>
    - README.md
    - .planning/PROJECT.md
    - .planning/phases/31-school-pilot-operations-safe-launch/31-UI-SPEC.md
  </read_first>
  <action>In README, add subsection heading exactly `### Pilot rollback and handoff`. Include exact guidance bullets: `Redeploy the last known good Vercel frontend and Render backend build.`, `Revert deployment environment variables to the last known good values.`, `Run /health/ready, npm --prefix frontend run guard:deploy, and npm --prefix frontend run smoke:pilot.`, `Notify the school or pilot owner if real users are affected.`, `Escalate incidents through the agreed school support path.`, `Do not use destructive database reset as the default rollback path.`, and `Do not use raw data export as the default rollback path.` Add one sentence that the operations dashboard shows handoff metadata and static guidance only, not contact details or incident free text. In `.planning/PROJECT.md`, update the v1.5 current state or validated section to mention Phase 31 school pilot operations are being prepared with metadata-only launch checklist/data safety/rollback guidance; do not mark Phase 31 complete yet unless execution verification has passed.</action>
  <verify>
    <automated>Set-Location D:\BeYou; Select-String -Path README.md -Pattern "Pilot rollback and handoff","Redeploy the last known good Vercel frontend and Render backend build","Do not use destructive database reset","Do not use raw data export"; Select-String -Path .planning\PROJECT.md -Pattern "metadata-only launch checklist|school pilot operations"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `### Pilot rollback and handoff`.
    - `README.md` contains all seven exact guidance bullets from the action.
    - `README.md` contains `not contact details or incident free text`.
    - `.planning/PROJECT.md` contains `metadata-only launch checklist` or `school pilot operations`.
    - `.planning/PROJECT.md` does not say Phase 31 is complete before verification.
  </acceptance_criteria>
  <done>Rollback/handoff docs are safe, non-destructive, and metadata-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| documentation -> operator action | README guidance influences pilot launch and rollback decisions. |
| project memory -> future agents | PROJECT.md context influences later phases and must not overclaim completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-04-01 | T | rollback docs | mitigate | README explicitly says not to use destructive database reset as default rollback. |
| T-31-04-02 | I | handoff docs | mitigate | No raw contacts, incident details, school/class/student examples, or identifiers are documented. |
| T-31-04-03 | R | smoke evidence | mitigate | README distinguishes public demo smoke from production-pilot proof. |
| T-31-04-04 | I | raw export | mitigate | README explicitly says not to use raw data export as default rollback. |
</threat_model>

<verification>
Run:
- `Set-Location D:\BeYou; Select-String -Path README.md -Pattern "School pilot launch checklist","Baseline setup for a real pilot","Pilot rollback and handoff","Do not use destructive database reset","Do not use raw data export"`
</verification>

<success_criteria>
- README documents launch checklist, baseline setup, rollback, and handoff.
- Docs avoid raw contacts, real student examples, destructive reset defaults, and raw export defaults.
- PROJECT.md records Phase 31 in-progress capability without claiming completion.
</success_criteria>

<output>
After completion, create `.planning/phases/31-school-pilot-operations-safe-launch/31-04-SUMMARY.md`.
</output>

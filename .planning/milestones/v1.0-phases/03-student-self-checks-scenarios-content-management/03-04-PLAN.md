---
phase: 03-student-self-checks-scenarios-content-management
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - backend/app/schemas/adult_summaries.py
  - backend/app/services/adult_summaries.py
  - backend/app/api/adult_summaries.py
  - backend/tests/test_phase3_adult_summaries.py
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "Teacher and parent can view permitted self-check summaries for actively linked students only."
    - "Adult summary responses omit raw answers, full answer text, and score breakdowns."
    - "Every adult summary read creates metadata-only sensitive_resource_read audit event."
  artifacts:
    - path: "backend/app/api/adult_summaries.py"
      provides: "Teacher/parent summary-only endpoints"
    - path: "backend/app/services/adult_summaries.py"
      provides: "Latest plus up to 5 recent/30-day summary queries and audit"
    - path: "backend/tests/test_phase3_adult_summaries.py"
      provides: "TEST-06 security regression"
  key_links:
    - from: "backend/app/api/adult_summaries.py"
      to: "backend/app/core/authorization.py"
      via: "self_check_summary support_not_surveillance permission"
    - from: "backend/app/services/adult_summaries.py"
      to: "backend/app/services/audit.py"
      via: "sensitive_resource_read event"
---

<objective>
Implement privacy-limited adult self-check summary APIs.

Purpose: linked teachers and parents can support students using summary-only data while raw answers remain private to the student.
Output: adult summary schemas, service, routes, and tests for link checks, latest/recent limits, raw-answer omission, and audit events.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-student-self-checks-scenarios-content-management/03-CONTEXT.md
@.planning/phases/03-student-self-checks-scenarios-content-management/03-UI-SPEC.md
@.planning/phases/01-safety-privacy-policy-foundation/01-AUTHORIZATION-POLICY.yml
@.planning/phases/01-safety-privacy-policy-foundation/01-AUDIT-EVENT-CATALOG.yml
@backend/app/core/authorization.py
@backend/app/services/audit.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create adult summary service and DTOs</name>
  <read_first>
    - backend/app/schemas/adult_summaries.py
    - backend/app/services/adult_summaries.py
    - backend/tests/test_phase3_adult_summaries.py
    - backend/app/db/models.py
    - backend/app/core/authorization.py
    - backend/app/services/audit.py
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-CONTEXT.md
  </read_first>
  <files>backend/app/schemas/adult_summaries.py, backend/app/services/adult_summaries.py, backend/tests/test_phase3_adult_summaries.py</files>
  <behavior>
    - Summary DTO fields are exactly student context, completion date, test type, risk/state label, advice summary, support suggestion, latest summary, recent summaries, and `is_demo`.
    - Response contains latest attempt plus at most 5 recent attempts completed in the last 30 days.
    - No raw answer text, answer list, score breakdown, or scenario history fields are present.
    - Reading summary records `sensitive_resource_read` with metadata keys `student_id`, `relationship_check`, `purpose_key`, and `decision`.
  </behavior>
  <action>
    Create `AdultSelfCheckSummaryItem` and `AdultSelfCheckSummaryResponse` in `backend/app/schemas/adult_summaries.py`. Do not include fields named `answers`, `raw_answers`, `question_text_snapshot`, `choice_text_snapshot`, `score_breakdown`, or `scenario_attempts`. Create `backend/app/services/adult_summaries.py` with `get_adult_self_check_summaries(db, adult, student_id, relationship_type)` that verifies active link through `require_permission(... resource_type="self_check_summary", purpose="support_not_surveillance")`, selects attempts for the target student ordered by `completed_at desc`, computes `latest_summary`, computes `recent_summaries` as up to 5 attempts where `completed_at >= utc_now() - 30 days`, and calls `record_audit_event` action `sensitive_resource_read`, resource_type `self_check_summary`, reason `support_not_surveillance`, status `allowed`, metadata `{"student_id": str(student_id), "relationship_check": "linked_teacher"|"linked_parent", "purpose_key": "support_not_surveillance", "decision": "summary_only"}`. This implements D-11..D-14 and does not implement adult raw-answer access.
  </action>
  <verify>
    <automated>cd backend; python -m alembic upgrade head; python -m pytest tests/test_phase3_adult_summaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AdultSelfCheckSummaryResponse" backend/app/schemas/adult_summaries.py` returns a match.
    - `grep -n "sensitive_resource_read" backend/app/services/adult_summaries.py` returns a match.
    - `grep -n "summary_only" backend/app/services/adult_summaries.py` returns a match.
    - `grep -n "raw_answers" backend/app/schemas/adult_summaries.py` returns no match.
    - `grep -n "question_text_snapshot" backend/app/schemas/adult_summaries.py` returns no match.
  </acceptance_criteria>
  <done>Adult summary data is intentionally minimized and audited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose teacher and parent summary-only endpoints</name>
  <read_first>
    - backend/app/api/adult_summaries.py
    - backend/tests/test_phase3_adult_summaries.py
    - backend/app/services/adult_summaries.py
    - backend/app/api/teacher.py
    - backend/app/api/parent.py
    - backend/app/core/sessions.py
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-UI-SPEC.md
  </read_first>
  <files>backend/app/api/adult_summaries.py, backend/tests/test_phase3_adult_summaries.py</files>
  <behavior>
    - `GET /api/teacher/students/{student_id}/self-check-summaries` allows linked teacher and denies unlinked teacher/parent/student.
    - `GET /api/parent/students/{student_id}/self-check-summaries` allows linked parent and denies unlinked teacher/parent/student.
    - Every successful read increments `audit_events` with action `sensitive_resource_read`.
  </behavior>
  <action>
    Implement routes in `backend/app/api/adult_summaries.py` using the router registered at `/api`: `GET "/teacher/students/{student_id}/self-check-summaries"` and `GET "/parent/students/{student_id}/self-check-summaries"`. Require role `teacher` or `parent`, pass `relationship_type="teacher"` or `"parent"`, and return `AdultSelfCheckSummaryResponse`. Use response copy/field names suitable for frontend labels `Tóm tắt gần nhất`, `Các lần gần đây`, `Tóm tắt gợi ý`, and `Gợi ý hỗ trợ`. Tests must assert the JSON string does not contain raw answer text values that were saved on the attempt answer rows.
  </action>
  <verify>
    <automated>cd backend; python -m alembic upgrade head; python -m pytest tests/test_phase3_adult_summaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/teacher/students/{student_id}/self-check-summaries" backend/app/api/adult_summaries.py` returns a match.
    - `grep -n "/parent/students/{student_id}/self-check-summaries" backend/app/api/adult_summaries.py` returns a match.
    - `grep -n "require_role(current_user, UserRole.TEACHER" backend/app/api/adult_summaries.py` returns a match.
    - `grep -n "require_role(current_user, UserRole.PARENT" backend/app/api/adult_summaries.py` returns a match.
  </acceptance_criteria>
  <done>Teacher and parent APIs satisfy TEST-06 with summary-only, linked-student access and audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| adult browser -> summary API | Adult requests student-scoped wellbeing summary. |
| summary API -> audit log | Sensitive read metadata is recorded. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04-01 | I | adult summary DTO | mitigate | Whitelist summary fields; tests grep/JSON-assert no raw answers or score breakdown. |
| T-03-04-02 | E | relationship checks | mitigate | Require linked teacher/parent relationship and `support_not_surveillance` purpose. |
| T-03-04-03 | R | adult reads | mitigate | Record `sensitive_resource_read` event for each successful summary read. |
| T-03-04-04 | I | audit metadata | mitigate | Audit payload contains metadata IDs/decision only and no answer text. |
| T-03-04-05 | D | summary endpoint | accept | Low-volume MVP endpoint; no new rate limit in Phase 3 beyond session auth. |
</threat_model>

<verification>
Run `cd backend; python -m alembic upgrade head; python -m pytest tests/test_phase3_adult_summaries.py -q`.
</verification>

<success_criteria>
Adult portals can retrieve only linked-student self-check summaries, latest plus 5 recent/30 days, with metadata-only audit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-student-self-checks-scenarios-content-management/03-04-SUMMARY.md`.
</output>

---
phase: 03-student-self-checks-scenarios-content-management
plan: 07
type: execute
wave: 4
depends_on: ["03-04", "03-05", "03-06"]
files_modified:
  - frontend/lib/admin-content-api.ts
  - frontend/lib/adult-summary-api.ts
  - frontend/app/(authenticated)/teacher/page.tsx
  - frontend/app/(authenticated)/parent/page.tsx
  - frontend/app/(authenticated)/teacher/students/[studentId]/self-check-summaries/page.tsx
  - frontend/app/(authenticated)/parent/students/[studentId]/self-check-summaries/page.tsx
  - frontend/app/(authenticated)/admin/page.tsx
  - frontend/app/(authenticated)/admin/content/page.tsx
  - frontend/tests/adult-admin-content-ui.test.tsx
  - frontend/tests/e2e/phase3-wellbeing-content.spec.ts
  - backend/tests/test_phase3_security_regression.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, SCEN-01, SCEN-02, SCEN-03, SCEN-04, SCEN-05, ADMIN-02, ADMIN-03]
must_haves:
  truths:
    - "Teacher/parent dashboards show summary-only self-check support cards with privacy notes."
    - "Admin can create/edit/publish/archive self-check and scenario content through UI."
    - "Final Phase 3 verification covers migrations, backend tests, frontend tests, and Playwright E2E."
  artifacts:
    - path: "frontend/app/(authenticated)/teacher/students/[studentId]/self-check-summaries/page.tsx"
      provides: "Teacher summary-only detail UI"
    - path: "frontend/app/(authenticated)/admin/content/page.tsx"
      provides: "Admin content management UI"
    - path: "frontend/tests/e2e/phase3-wellbeing-content.spec.ts"
      provides: "End-to-end Phase 3 demo flow"
  key_links:
    - from: "frontend/lib/adult-summary-api.ts"
      to: "/api/teacher/students/{studentId}/self-check-summaries"
      via: "teacher summary helper"
    - from: "frontend/lib/admin-content-api.ts"
      to: "/api/admin/content/self-checks"
      via: "admin content helper"
---

<objective>
Build adult/admin frontend surfaces and final Phase 3 verification.

Purpose: adults need privacy-limited summaries, admins need content management UI, and the full Phase 3 demo must be regression-tested end to end.
Output: adult summary UI, admin content UI, typed API helpers, Vitest tests, Playwright E2E, backend security regressions, and final verification sequence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-student-self-checks-scenarios-content-management/03-CONTEXT.md
@.planning/phases/03-student-self-checks-scenarios-content-management/03-UI-SPEC.md
@.planning/phases/02-identity-roles-links-demo-access/02-07-SUMMARY.md
@frontend/app/(authenticated)/teacher/page.tsx
@frontend/app/(authenticated)/parent/page.tsx
@frontend/app/(authenticated)/admin/page.tsx
@frontend/playwright.config.ts
</context>

<execution_notes>
Scope note: this plan remains combined because it is the final integration wave after backend, student UI, adult APIs, and admin APIs are available; the adult UI, admin UI, E2E, and security tests verify cross-role behavior that must align. Scope remains safe through read_first batching: Task 1 reads adult summary files only, Task 2 reads admin content files only, and Task 3 reads E2E/security verification files only.
</execution_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adult summary-only dashboard links and detail pages</name>
  <read_first>
    - frontend/lib/adult-summary-api.ts
    - frontend/app/(authenticated)/teacher/page.tsx
    - frontend/app/(authenticated)/parent/page.tsx
    - frontend/app/(authenticated)/teacher/students/[studentId]/self-check-summaries/page.tsx
    - frontend/app/(authenticated)/parent/students/[studentId]/self-check-summaries/page.tsx
    - frontend/tests/adult-admin-content-ui.test.tsx
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-UI-SPEC.md
  </read_first>
  <files>frontend/lib/adult-summary-api.ts, frontend/app/(authenticated)/teacher/page.tsx, frontend/app/(authenticated)/parent/page.tsx, frontend/app/(authenticated)/teacher/students/[studentId]/self-check-summaries/page.tsx, frontend/app/(authenticated)/parent/students/[studentId]/self-check-summaries/page.tsx, frontend/tests/adult-admin-content-ui.test.tsx</files>
  <behavior>
    - Teacher student cards link to `Tóm tắt tự kiểm tra được phép xem`; parent cards link to `Tóm tắt hỗ trợ của con`.
    - Adult summary detail shows latest and recent summaries with labels `Tóm tắt gần nhất`, `Các lần gần đây`, `Tóm tắt gợi ý`, `Gợi ý hỗ trợ`.
    - Privacy note `Bạn đang xem phần tóm tắt được phép xem. Câu trả lời riêng tư và chi tiết điểm không hiển thị tại đây.` is visible.
    - Adult UI does not render raw answers, full answer text, score breakdown, or scenario attempt history.
  </behavior>
  <action>
    Create `frontend/lib/adult-summary-api.ts` with `getTeacherSelfCheckSummaries(studentId)` calling `/api/teacher/students/{studentId}/self-check-summaries` and `getParentSelfCheckSummaries(studentId)` calling `/api/parent/students/{studentId}/self-check-summaries`. Update teacher and parent dashboard cards with CTA `Xem tóm tắt hỗ trợ`. Create teacher and parent summary pages with exact UI copy: teacher section title `Tóm tắt tự kiểm tra được phép xem`, parent section title `Tóm tắt hỗ trợ của con`, detail title `Tóm tắt được phép xem`, privacy note `BeYou không hiển thị câu trả lời riêng tư của học sinh tại đây. Nội dung này chỉ nhằm hỗ trợ em đúng lúc.`, and adult reminder `Bạn đang xem phần tóm tắt được phép xem, không phải toàn bộ câu trả lời riêng tư của học sinh.`. Do not add trend charts, exports, risk heatmaps, leaderboards, or scenario attempt history.
  </action>
  <verify>
    <automated>cd frontend; npm run test -- --run adult-admin-content-ui</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getTeacherSelfCheckSummaries" frontend/lib/adult-summary-api.ts` returns a match.
    - `grep -n "Xem tóm tắt hỗ trợ" "frontend/app/(authenticated)/teacher/page.tsx"` returns a match.
    - `grep -n "Xem tóm tắt hỗ trợ" "frontend/app/(authenticated)/parent/page.tsx"` returns a match.
    - `grep -R "raw\\|answer_text\\|score_breakdown\\|scenario" frontend/app/(authenticated)/teacher/students frontend/app/(authenticated)/parent/students` returns no forbidden adult detail exposure.
    - `grep -n "BeYou không hiển thị câu trả lời riêng tư" "frontend/app/(authenticated)/teacher/students/[studentId]/self-check-summaries/page.tsx"` returns a match.
  </acceptance_criteria>
  <done>Adult UI satisfies TEST-06 with exact summary-only privacy language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add admin content management UI</name>
  <read_first>
    - frontend/lib/admin-content-api.ts
    - frontend/app/(authenticated)/admin/page.tsx
    - frontend/app/(authenticated)/admin/content/page.tsx
    - frontend/tests/adult-admin-content-ui.test.tsx
    - frontend/app/(authenticated)/admin/users/page.tsx
    - frontend/app/(authenticated)/admin/links/page.tsx
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-UI-SPEC.md
  </read_first>
  <files>frontend/lib/admin-content-api.ts, frontend/app/(authenticated)/admin/page.tsx, frontend/app/(authenticated)/admin/content/page.tsx, frontend/tests/adult-admin-content-ui.test.tsx</files>
  <behavior>
    - Admin dashboard has card title `Nội dung tự kiểm tra và tình huống` and description `Tạo, chỉnh sửa và xuất bản nội dung hỗ trợ học sinh theo đúng phạm vi an toàn.`
    - Admin content page has self-check and scenario management sections with create/edit fields required by UI spec.
    - Publish/archive/delete unused draft buttons call backend helpers and show exact confirmation/error copy.
    - Status labels are exactly `draft`, `published`, and `archived`.
  </behavior>
  <action>
    Create `frontend/lib/admin-content-api.ts` with helpers for `/api/admin/content/self-checks` and `/api/admin/content/scenarios`, including create, update, publish, archive, and delete draft. Update `admin/page.tsx` with a card linking to `/admin/content`, title `Nội dung tự kiểm tra và tình huống`, description `Tạo, chỉnh sửa và xuất bản nội dung hỗ trợ học sinh theo đúng phạm vi an toàn.`, and count label `Quản lý nội dung`. Create `frontend/app/(authenticated)/admin/content/page.tsx` with two sections: `Quản lý bài tự kiểm tra` and `Quản lý tình huống`. The self-check editor must include nested editing for basic test fields (`Tên bài tự kiểm tra`, `Mô tả ngắn`, `Trạng thái nội dung`), self-check question text, answer choice text, numeric score value per answer choice, and risk thresholds with state label, minimum score, maximum score, comment, advice, positive content, and suggested next action. The scenario editor must include nested editing for scenario title, situation description, response choice text, constructive/risky signal, response-choice feedback, recommended response, lesson, and skill tags. Include `Lưu bản nháp`, `Xuất bản`, `Lưu trữ`, `Xóa bản nháp chưa dùng`, and exact destructive confirmation strings from UI spec. Keep forms fully wired to backend APIs; do not render admin student-result views or aggregate reports.
  </action>
  <verify>
    <automated>cd frontend; npm run test -- --run adult-admin-content-ui</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Nội dung tự kiểm tra và tình huống" "frontend/app/(authenticated)/admin/page.tsx"` returns a match.
    - `grep -n "Quản lý bài tự kiểm tra" "frontend/app/(authenticated)/admin/content/page.tsx"` returns a match.
    - `grep -n "Quản lý tình huống" "frontend/app/(authenticated)/admin/content/page.tsx"` returns a match.
    - `grep -n "Lưu trữ nội dung này\\?" "frontend/app/(authenticated)/admin/content/page.tsx"` returns a match.
    - `grep -n "/api/admin/content/self-checks" frontend/lib/admin-content-api.ts` returns a match.
  </acceptance_criteria>
  <done>Admin UI satisfies ADMIN-02 and ADMIN-03 without adding out-of-scope reporting/result surveillance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add final Phase 3 E2E and security regression verification</name>
  <read_first>
    - frontend/tests/e2e/phase3-wellbeing-content.spec.ts
    - frontend/playwright.config.ts
    - frontend/tests/e2e/phase2-auth-roles-admin.spec.ts
    - backend/tests/test_phase2_security_regression.py
    - .planning/phases/02-identity-roles-links-demo-access/02-07-SUMMARY.md
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-CONTEXT.md
  </read_first>
  <files>frontend/tests/e2e/phase3-wellbeing-content.spec.ts, backend/tests/test_phase3_security_regression.py</files>
  <behavior>
    - Playwright covers demo student self-check submit/result/history/detail and scenario attempt/history.
    - Playwright covers teacher and parent summary-only pages and asserts raw answer text is not visible.
    - Playwright covers admin create/publish/archive content using `/admin/content`.
    - Backend security regression covers adult raw answer denial, adult summary audit metadata, CSRF on admin content mutations, demo flags, and snapshot immutability.
  </behavior>
  <action>
    Create `backend/tests/test_phase3_security_regression.py` and `frontend/tests/e2e/phase3-wellbeing-content.spec.ts`. Reuse Phase 2 demo login pattern and demo credentials. E2E sequence: login as demo student, accept privacy if needed, open `/student/self-checks`, start `Sức khỏe cảm xúc`, answer all questions, submit, assert supportive result and `Điểm tham khảo`, open self-check history/detail and assert private answer detail; open `/student/scenarios`, choose a scenario, submit a choice, assert `Gợi ý sau lựa chọn của em`, `Cách phản hồi nên thử`, `Điều em có thể rút ra`; login teacher and parent and assert `Tóm tắt được phép xem` is visible but selected raw answer text is not; login admin, open `/admin/content`, create a draft scenario or self-check, publish, archive, and assert `admin_safety_content_changed` backend test covers audit. Final verification command must be documented in the summary and executed in this order: `cd backend; docker compose up -d postgres; python -m alembic upgrade head; $env:ALLOW_DEMO_SEED="true"; python -m app.seeds.demo_seed; python -m pytest tests -q; cd ..\frontend; npm run test -- --run; npx playwright test tests/e2e/phase3-wellbeing-content.spec.ts`.
  </action>
  <verify>
    <automated>cd backend; docker compose up -d postgres; python -m alembic upgrade head; $env:ALLOW_DEMO_SEED="true"; python -m app.seeds.demo_seed; python -m pytest tests -q; cd ..\frontend; npm run test -- --run; npx playwright test tests/e2e/phase3-wellbeing-content.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "phase3-wellbeing-content" frontend/tests/e2e/phase3-wellbeing-content.spec.ts` or file name exists with test content.
    - `grep -n "Sức khỏe cảm xúc" frontend/tests/e2e/phase3-wellbeing-content.spec.ts` returns a match.
    - `grep -n "raw answer" backend/tests/test_phase3_security_regression.py` or `grep -n "raw_answers" backend/tests/test_phase3_security_regression.py` returns a match.
    - Final verification command exits 0 with Alembic before backend pytest and Playwright after frontend tests.
  </acceptance_criteria>
  <done>Phase 3 is verified end-to-end across backend, frontend, security regressions, and demo role flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| adult/admin UI -> backend APIs | Adult and admin roles request sensitive summaries or privileged mutations. |
| E2E demo seed -> browser flows | Demo data powers browser-visible verification. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-07-01 | I | adult summary pages | mitigate | Render only summary fields and grep/test for absent raw answers/score breakdowns/scenario history. |
| T-03-07-02 | CSRF | admin content UI | mitigate | Use backend API helpers that call CSRF-protected endpoints; backend regression verifies cross-site rejection. |
| T-03-07-03 | R | admin mutations | mitigate | Backend regression asserts `admin_safety_content_changed` audit events exist and metadata is sanitized. |
| T-03-07-04 | T | snapshot immutability | mitigate | Regression edits content after an attempt and asserts history still shows snapshot values. |
| T-03-07-05 | I | audit metadata leakage | mitigate | Regression tries forbidden nested metadata keys and asserts rejection. |
| T-03-07-06 | T | demo/real mixing | mitigate | E2E and backend tests assert `Demo` badges and `is_demo=True` records from seed. |
</threat_model>

<verification>
Final sequence: `cd backend; docker compose up -d postgres; python -m alembic upgrade head; $env:ALLOW_DEMO_SEED="true"; python -m app.seeds.demo_seed; python -m pytest tests -q; cd ..\frontend; npm run test -- --run; npx playwright test tests/e2e/phase3-wellbeing-content.spec.ts`.
</verification>

<success_criteria>
Adult summary UI, admin content UI, and final verification prove every Phase 3 requirement without deferred analytics, SOS, chatbot, exports, or adult raw-answer access.
</success_criteria>

<output>
After completion, create `.planning/phases/03-student-self-checks-scenarios-content-management/03-07-SUMMARY.md`.
</output>

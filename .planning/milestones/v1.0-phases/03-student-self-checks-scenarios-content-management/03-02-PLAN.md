---
phase: 03-student-self-checks-scenarios-content-management
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/services/self_checks.py
  - backend/app/api/student_self_checks.py
  - backend/tests/test_phase3_student_self_checks.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]
must_haves:
  truths:
    - "Student can list only published active self-check tests."
    - "Student can submit multiple-choice answers and receive backend-calculated score, risk/state label, and advice."
    - "Student can view own history and own raw answer detail."
  artifacts:
    - path: "backend/app/services/self_checks.py"
      provides: "Scoring, threshold matching, snapshots, history/detail queries"
    - path: "backend/app/api/student_self_checks.py"
      provides: "Student self-check routes under /api/student/self-checks"
    - path: "backend/tests/test_phase3_student_self_checks.py"
      provides: "Backend tests for TEST-01..TEST-05"
  key_links:
    - from: "backend/app/api/student_self_checks.py"
      to: "backend/app/services/self_checks.py"
      via: "route handlers delegate scoring and history"
    - from: "backend/app/services/self_checks.py"
      to: "SelfCheckAttemptAnswer"
      via: "raw answer snapshots for student detail only"
---

<objective>
Implement student-facing self-check backend behavior.

Purpose: students need safe, backend-scored self-check submissions with private raw-answer detail and immutable result snapshots.
Output: self-check service, student API endpoints, and tests for list, submit, result, history, private detail, validation, and snapshot immutability.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-student-self-checks-scenarios-content-management/03-CONTEXT.md
@.planning/phases/03-student-self-checks-scenarios-content-management/03-RESEARCH.md
@backend/app/db/models.py
@backend/app/schemas/self_checks.py
@backend/app/api/student_self_checks.py
@backend/app/core/authorization.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement self-check scoring and immutable attempt snapshots</name>
  <read_first>
    - backend/app/services/self_checks.py
    - backend/tests/test_phase3_student_self_checks.py
    - backend/app/db/models.py
    - backend/app/schemas/self_checks.py
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-CONTEXT.md
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-RESEARCH.md
  </read_first>
  <files>backend/app/services/self_checks.py, backend/tests/test_phase3_student_self_checks.py</files>
  <behavior>
    - Published active test with all questions answered returns score equal to the sum of selected choice `score_value`.
    - Score maps to exactly one threshold using inclusive `min_score <= score <= max_score`.
    - Missing question answer, choice from another test, unpublished test, inactive test, and no matching threshold return HTTP 400 or 404 with no attempt created.
    - Editing content after completion does not change `SelfCheckAttempt` result fields or `SelfCheckAttemptAnswer` snapshots.
  </behavior>
  <action>
    Create `backend/app/services/self_checks.py` with functions `list_published_tests(db)`, `get_published_test_detail(db, test_id)`, `submit_self_check_attempt(db, student, test_id, answers)`, `list_student_attempts(db, student)`, and `get_student_attempt_detail(db, student, attempt_id)`. In `submit_self_check_attempt`, enforce D-02 and D-03 by requiring one selected choice per question, verifying each choice belongs to the submitted test, summing `score_value`, finding one threshold for exact labels `On dinh`, `Can chu y`, `Nen tim ho tro`, `Can ho tro som`, and writing `SelfCheckAttempt` plus `SelfCheckAttemptAnswer` rows in one transaction. Save `test_snapshot` with title, description, questions, choices, thresholds, and copy fields; copy `supportive_headline`, `short_comment`, `advice_summary`, `support_suggestion`, `positive_content`, `suggested_next_action`, `test_title_snapshot`, and `is_demo`. Use Vietnamese non-diagnostic defaults from the UI spec for result headline/next action per D-07, D-08, D-10 when threshold copy does not override them. Do not implement subscales, analytics, critical auto-escalation, or SOS.
  </action>
  <verify>
    <automated>cd backend; python -m alembic upgrade head; python -m pytest tests/test_phase3_student_self_checks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def submit_self_check_attempt" backend/app/services/self_checks.py` returns a match.
    - `grep -n "min_score" backend/app/services/self_checks.py` and `grep -n "max_score" backend/app/services/self_checks.py` return matches.
    - `grep -n "SelfCheckAttemptAnswer" backend/app/services/self_checks.py` returns a match.
    - `grep -n "test_snapshot" backend/app/services/self_checks.py` returns a match.
    - `grep -n "chẩn đoán" backend/app/services/self_checks.py` returns only disclaimer/default-copy usage, not diagnosis claims.
  </acceptance_criteria>
  <done>Self-check scoring is backend-owned, threshold-based, transactional, non-clinical, and snapshot-preserving.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose student self-check list, submit, result, history, and detail APIs</name>
  <read_first>
    - backend/app/api/student_self_checks.py
    - backend/tests/test_phase3_student_self_checks.py
    - backend/app/services/self_checks.py
    - backend/app/api/student.py
    - backend/app/core/sessions.py
    - backend/app/services/privacy.py
    - .planning/phases/03-student-self-checks-scenarios-content-management/03-CONTEXT.md
  </read_first>
  <files>backend/app/api/student_self_checks.py, backend/tests/test_phase3_student_self_checks.py</files>
  <behavior>
    - `GET /api/student/self-checks` returns only `published` and `is_active=true` tests.
    - `GET /api/student/self-checks/history` returns own attempts only.
    - `GET /api/student/self-checks/history/{attempt_id}` returns own raw answer snapshots and denies another student's attempt.
    - `GET /api/student/self-checks/{test_id}` returns questions and choices without score totals or thresholds.
    - `POST /api/student/self-checks/{test_id}/attempts` returns result response with `score`, `state_label`, supportive message, advice, positive content, and suggested next action.
    - Backend tests prove `GET /api/student/self-checks/history` returns history and is not captured by `GET /api/student/self-checks/{test_id}`.
  </behavior>
  <action>
    Implement routes in `backend/app/api/student_self_checks.py` in this exact declaration order to prevent `/history` from being captured by the dynamic `/{test_id}` route: `GET ""`, `GET "/history"`, `GET "/history/{attempt_id}"`, `GET "/{test_id}"`, and `POST "/{test_id}/attempts"`. Require authenticated active student role, privacy acknowledgement gate identical to `backend/app/api/student.py`, and `require_permission` with `resource_type="self_check_raw_answers"` plus `purpose="student_reflection"` for submit/detail and `resource_type="self_check_summary"` for result/history. Return Pydantic schemas from `backend/app/schemas/self_checks.py`. Ensure list/detail endpoints never return thresholds or choice scoring values to the student before submission; the result can return score as secondary metadata per D-07. Tests must create two students, prove cross-student history/detail IDOR returns 403 or 404, and explicitly call `GET /api/student/self-checks/history` to assert it returns a history response rather than invoking the `/{test_id}` detail handler.
  </action>
  <verify>
    <automated>cd backend; python -m alembic upgrade head; python -m pytest tests/test_phase3_student_self_checks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@router.get(\"/history\"" backend/app/api/student_self_checks.py` returns a match.
    - `grep -n "@router.get(\"/history/{attempt_id}\"" backend/app/api/student_self_checks.py` returns a match before `grep -n "@router.get(\"/{test_id}\"" backend/app/api/student_self_checks.py`.
    - `grep -n "@router.post(\"/{test_id}/attempts\"" backend/app/api/student_self_checks.py` returns a match.
    - `grep -n "privacy_acknowledgement_required" backend/app/api/student_self_checks.py` returns a match.
    - `grep -n "student_reflection" backend/app/api/student_self_checks.py` returns a match.
    - `grep -n "score_value" backend/app/api/student_self_checks.py` returns no route response serialization of choices before submission.
    - `backend/tests/test_phase3_student_self_checks.py` includes an automated route-regression assertion that `GET /api/student/self-checks/history` returns self-check history and is not treated as a `{test_id}` lookup.
  </acceptance_criteria>
  <done>Student APIs satisfy TEST-01 through TEST-05 with backend authorization and privacy gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| student browser -> self-check API | Student-submitted answer IDs are untrusted. |
| self-check API -> raw answer storage | Private raw answers become durable data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02-01 | T | `submit_self_check_attempt` | mitigate | Verify every selected choice belongs to the requested published active test. |
| T-03-02-02 | I | `/history/{attempt_id}` | mitigate | Require student owner check and `self_check_raw_answers` permission before returning raw answer snapshots. |
| T-03-02-03 | E | student routes | mitigate | Require active student role and privacy acknowledgement gate before self-check access. |
| T-03-02-04 | R | submission transaction | mitigate | Persist completed attempt timestamp and immutable snapshots for auditability. |
| T-03-02-05 | I | result copy | mitigate | Use non-diagnostic Vietnamese copy and avoid exposing thresholds/score breakdown before submission. |
</threat_model>

<verification>
Run `cd backend; python -m alembic upgrade head; python -m pytest tests/test_phase3_student_self_checks.py -q`.
</verification>

<success_criteria>
Student self-check backend supports published test list, answer submit, result, history, and own detail while preserving D-01..D-10.
</success_criteria>

<output>
After completion, create `.planning/phases/03-student-self-checks-scenarios-content-management/03-02-SUMMARY.md`.
</output>

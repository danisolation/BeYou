---
phase: 04-sos-workflow-adult-support-portals
plan: 02
subsystem: frontend-sos-adult-portals
wave: 2
requirements: [SOS-01, SOS-02, SOS-05, TEACH-01, TEACH-02, TEACH-03, PARENT-01, PARENT-02, PARENT-03]
---

# Phase 04 Plan 02: Student SOS UI and Adult Support Portals

## Objective

Implement Vietnamese student/adult SOS UI using existing cookie-authenticated API helpers, demo badges, role dashboards, and Phase 4 UI-SPEC copy.

## Tasks

1. Add failing Vitest coverage for:
   - student dashboard visible SOS card, confirmation requirement, POST body, and status list
   - SOS API helpers use cookie-authenticated fetch and no token storage
   - teacher dashboard renders support overview/notifications and status update controls
   - parent dashboard renders read-only SOS status and permitted support summary copy
2. Add `frontend/lib/sos-api.ts`.
3. Extend `/student` with SOS card, confirmation panel, and latest status progress.
4. Extend teacher/parent dashboards with notifications, support overview, and SOS links.
5. Add teacher update page and parent read-only page for SOS alerts.
6. Run targeted and full Vitest. Commit code and create `04-02-SUMMARY.md`.

## Verification

- `cd frontend; npm run test -- --run phase4-sos-ui`
- `cd frontend; npm run test -- --run`

## Success Criteria

- Student cannot send SOS without confirmation.
- UI copy matches `04-UI-SPEC.md`.
- Adult UI exposes support summaries/status only, not raw self-check answers.

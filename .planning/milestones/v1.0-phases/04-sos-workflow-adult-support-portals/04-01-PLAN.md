---
phase: 04-sos-workflow-adult-support-portals
plan: 01
subsystem: backend-sos-notifications
wave: 1
requirements: [SOS-03, SOS-04, SOS-05, SOS-06, TEACH-01, TEACH-02, TEACH-03, PARENT-01, PARENT-02, PARENT-03]
---

# Phase 04 Plan 01: Backend SOS Workflow and In-App Notifications

## Objective

Implement the backend data model, migration, schemas, services, authorization, routes, demo seed, and pytest coverage for SOS alerts, status events, in-app notifications, and adult support overviews.

## Tasks

1. Add failing backend tests for:
   - student creates confirmed SOS with severity/source/note and linked adult notifications
   - teacher/parent linked access and unlinked denial
   - teacher forward-only status updates create status events and audit
   - parent cannot update status
   - support overview returns warning groups without raw answers
2. Add SQLAlchemy models and Alembic migration for `sos_alerts`, `sos_status_events`, and `in_app_notifications`.
3. Add `sos_alert` authorization rules for student own access, linked adult read, and linked teacher status update.
4. Add SOS schemas/services/routes and include routers in `main.py`.
5. Extend demo seed with idempotent demo SOS alert and notifications.
6. Run backend targeted and full pytest. Commit code and create `04-01-SUMMARY.md`.

## Verification

- `cd backend; python -m alembic upgrade head`
- `cd backend; python -m pytest tests/test_phase4_sos_backend.py -q`
- `cd backend; python -m pytest -q`

## Success Criteria

- SOS-03..SOS-06 are covered by backend tests.
- TEACH/PARENT backend support APIs enforce relationship and privacy boundaries.
- Audit metadata remains raw-content-free.

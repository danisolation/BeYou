---
phase: 02-identity-roles-links-demo-access
plan: 07
type: execute
wave: 7
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - frontend/playwright.config.ts
  - frontend/tests/e2e/phase2-auth-roles-admin.spec.ts
  - backend/tests/test_phase2_security_regression.py
  - frontend/tests/no-token-storage.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, ADMIN-01]
must_haves:
  truths:
    - "Student, teacher, parent, and admin demo users can log in with email/password."
    - "Each demo role lands in the correct role portal."
    - "Student profile shows school, class, name, and linked support adults."
    - "Admin can create, edit, disable, delete demo-only, and update users/roles."
    - "Admin can create and revoke active student-teacher-parent links."
    - "Security regressions for session theft, CSRF, IDOR, audit leakage, and demo confusion are tested."
  artifacts:
    - path: "frontend/tests/e2e/phase2-auth-roles-admin.spec.ts"
      provides: "End-to-end Phase 2 role/admin flow"
    - path: "backend/tests/test_phase2_security_regression.py"
      provides: "Backend security regression tests"
    - path: "frontend/tests/no-token-storage.test.ts"
      provides: "Frontend token storage regression test"
  key_links:
    - from: "frontend/tests/e2e/phase2-auth-roles-admin.spec.ts"
      to: "backend demo seed"
      via: "demo account login"
    - from: "backend/tests/test_phase2_security_regression.py"
      to: "backend authorization/audit/session services"
      via: "pytest"
---

<objective>
Add final integration and security regression verification for the full Phase 2 goal.

Purpose: The phase is not complete until the backend, frontend, demo seed, role portals, admin workflows, and security controls work together.

Output: Playwright E2E test and backend/frontend regression tests proving the phase success criteria.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-identity-roles-links-demo-access/02-CONTEXT.md
@.planning/phases/02-identity-roles-links-demo-access/02-RESEARCH.md
@.planning/phases/02-identity-roles-links-demo-access/02-UI-SPEC.md
@.planning/phases/01-safety-privacy-policy-foundation/01-AUTHORIZATION-POLICY.yml
@.planning/phases/01-safety-privacy-policy-foundation/01-AUDIT-EVENT-CATALOG.yml
@.planning/phases/01-safety-privacy-policy-foundation/01-DEMO-DATA-POLICY.md
@backend/app/seeds/demo_seed.py
@frontend/app/login/page.tsx
@frontend/app/(authenticated)/student/page.tsx
@frontend/app/(authenticated)/admin/users/page.tsx
@frontend/app/(authenticated)/admin/links/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend security regression tests for Phase 2 controls</name>
  <files>backend/tests/test_phase2_security_regression.py</files>
  <read_first>
    - backend/app/core/security.py
    - backend/app/core/sessions.py
    - backend/app/core/authorization.py
    - backend/app/services/audit.py
    - backend/app/api/auth.py
    - backend/app/api/admin_users.py
    - backend/app/api/admin_links.py
    - .planning/phases/02-identity-roles-links-demo-access/02-CONTEXT.md
    - .planning/phases/01-safety-privacy-policy-foundation/01-AUTHORIZATION-POLICY.yml
    - .planning/phases/01-safety-privacy-policy-foundation/01-AUDIT-EVENT-CATALOG.yml
  </read_first>
  <behavior>
    - Credential stuffing/brute force control: invalid login returns generic error and never indicates whether email exists.
    - Session theft control: no API returns session cookie value in JSON.
    - Cookie prefix control: development uses `beyou_session`; any configured `__Host-` cookie requires Secure, HttpOnly, SameSite=Lax, Path=/, and no Domain.
    - CSRF control: mutating cookie-auth request with cross-site Fetch Metadata is rejected.
    - IDOR control: teacher/parent cannot access unlinked student data.
    - Audit leakage control: audit events reject passwords, tokens, session cookies, API keys, raw chat, raw self-check answers.
    - Demo confusion control: demo seed/user/link/session responses preserve `is_demo`.
  </behavior>
  <action>Create backend regression tests that directly cover the required security threats. Use pytest and existing fixtures. Test exact forbidden audit keys: `password`, `token`, `session_cookie`, `api_key`, `raw_chat_content`, `raw_self_check_answers`, `self_check_raw_answers`. Test development login response sets cookie name `beyou_session` with `HttpOnly`, `SameSite=Lax`, and `Path=/`, and does not require `Secure` for localhost HTTP. Test config/startup rejects `SESSION_COOKIE_NAME=__Host-beyou_session` when `SESSION_COOKIE_SECURE=false`. Test a production-style `__Host-beyou_session` response includes `Secure`, `HttpOnly`, `SameSite=Lax`, `Path=/`, and no `Domain`. Test role/relationship bypass attempts for teacher and parent. Test all mutating admin endpoints require admin role and reject cross-site metadata. Do not add production legal consent, OAuth, email reset, external invitations, SOS, self-check, chatbot, or reports.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_phase2_security_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains test names including `test_invalid_login_is_generic`, `test_no_session_cookie_value_in_json`, `test_session_cookie_prefix_rules`, `test_cross_site_mutation_rejected`, `test_unlinked_adult_student_access_denied`, `test_audit_rejects_forbidden_keys`, and `test_demo_flags_preserved`.
    - `python -m pytest tests/test_phase2_security_regression.py -q` exits 0.
  </acceptance_criteria>
  <done>Backend regression suite blocks high-severity Phase 2 security failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Playwright E2E demo role and admin workflow test with server startup</name>
  <files>frontend/playwright.config.ts, frontend/tests/e2e/phase2-auth-roles-admin.spec.ts, frontend/tests/no-token-storage.test.ts</files>
  <read_first>
    - frontend/app/login/page.tsx
    - frontend/app/privacy/page.tsx
    - frontend/app/(authenticated)/student/page.tsx
    - frontend/app/(authenticated)/teacher/page.tsx
    - frontend/app/(authenticated)/parent/page.tsx
    - frontend/app/(authenticated)/admin/page.tsx
    - frontend/app/(authenticated)/admin/users/page.tsx
    - frontend/app/(authenticated)/admin/links/page.tsx
    - frontend/lib/api.ts
    - backend/app/seeds/demo_seed.py
    - .planning/phases/02-identity-roles-links-demo-access/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Demo student logs in with `student.demo@beyou.local` and reaches privacy gate, acknowledges, then sees `Bảng điều khiển của em`.
    - Demo teacher logs in and sees `Cổng giáo viên`.
    - Demo parent logs in and sees `Cổng phụ huynh`.
    - Demo admin logs in and sees `Cổng quản trị`.
    - Student dashboard shows school `Trường THPT BeYou Demo`, class `10A1`, linked teacher, linked parent, demo banner, and `Demo` badge.
    - Admin can create a demo user, edit role/profile/status, disable account, delete demo-only account, create link, and revoke link.
    - Frontend tests confirm no token is written to localStorage or sessionStorage.
  </behavior>
  <action>Configure `frontend/playwright.config.ts` with base URL `http://localhost:3000` and a `webServer` array that starts both servers before E2E: backend command `powershell -NoProfile -Command "cd ..\\backend; docker compose up -d postgres; python -m alembic upgrade head; $env:ALLOW_DEMO_SEED='true'; python -m app.seeds.demo_seed; python -m uvicorn app.main:app --host 127.0.0.1 --port 8000"` with URL `http://127.0.0.1:8000/health`, and frontend command `npm run dev -- --hostname 127.0.0.1 --port 3000` with URL `http://127.0.0.1:3000/login`. Set `reuseExistingServer: !process.env.CI` for both servers. Create E2E test file that uses backend at `http://localhost:8000`, frontend at `http://localhost:3000`, database migrated, and demo seed executed by Playwright webServer. Use exact demo emails from seed and password `BeYouDemo!2026`. Test exact UI copy: `Chào mừng đến với BeYou`, `Đăng nhập`, `Đang xem dữ liệu demo - không phải hồ sơ học sinh thật.`, `Demo`, `Ai có thể xem thông tin của em?`, `Quản lý tài khoản`, and `Liên kết học sinh và người lớn hỗ trợ`. Add `no-token-storage.test.ts` that monkeypatches localStorage/sessionStorage and asserts login/auth client does not call `setItem` with session/token values. Do not use screenshots as the only assertion; assert text and route URLs.</action>
  <verify>
    <automated>cd frontend; npm run test -- --run no-token-storage; npx playwright test tests/e2e/phase2-auth-roles-admin.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Playwright test file contains all four exact demo emails.
    - Playwright test asserts routes `/student`, `/teacher`, `/parent`, and `/admin`.
    - Playwright test asserts exact demo banner text.
    - No-token-storage test fails if `localStorage.setItem` or `sessionStorage.setItem` is called for auth.
    - `frontend/playwright.config.ts` contains `webServer`, `python -m uvicorn app.main:app`, `npm run dev`, `http://127.0.0.1:8000/health`, and `http://127.0.0.1:3000/login`.
  </acceptance_criteria>
  <done>E2E test proves Phase 2 login, role portals, demo indicators, student profile, and admin workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 2 verification command sequence</name>
  <files>backend/tests/test_phase2_security_regression.py, frontend/tests/e2e/phase2-auth-roles-admin.spec.ts, frontend/tests/no-token-storage.test.ts</files>
  <read_first>
    - backend/docker-compose.yml
    - backend/alembic.ini
    - backend/app/seeds/demo_seed.py
    - frontend/playwright.config.ts
    - .planning/phases/02-identity-roles-links-demo-access/02-CONTEXT.md
  </read_first>
  <action>Run this exact final verification sequence from repository root after implementation: `cd backend; docker compose up -d postgres; python -m alembic upgrade head; $env:ALLOW_DEMO_SEED="true"; python -m app.seeds.demo_seed; python -m pytest tests -q; cd ..\frontend; npm run test -- --run; npx playwright test tests/e2e/phase2-auth-roles-admin.spec.ts`. The Playwright command must rely on `frontend/playwright.config.ts` webServer to start `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000` and `npm run dev -- --hostname 127.0.0.1 --port 3000` if they are not already running. If PowerShell rejects inline environment syntax in a different shell, use PowerShell command `$env:ALLOW_DEMO_SEED="true"` before running `python -m app.seeds.demo_seed`. Do not mark Phase 2 complete if any backend test, frontend test, or Playwright test fails.</action>
  <verify>
    <automated>cd backend; docker compose up -d postgres; python -m alembic upgrade head; $env:ALLOW_DEMO_SEED="true"; python -m app.seeds.demo_seed; python -m pytest tests -q; cd ..\frontend; npm run test -- --run; npx playwright test tests/e2e/phase2-auth-roles-admin.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Alembic migration command exits 0.
    - Demo seed command exits 0 with `ALLOW_DEMO_SEED=true`.
    - Backend pytest exits 0.
    - Frontend Vitest exits 0.
    - Playwright E2E exits 0.
    - Playwright config includes webServer startup for backend and frontend, so E2E does not assume pre-running servers.
  </acceptance_criteria>
  <done>Full Phase 2 goal is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser E2E → frontend/backend | Full user workflow exercises auth, cookies, role routing, and admin mutations. |
| Regression tests → security services | Tests intentionally attack high-risk boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-06-01 | S | login E2E/regression | mitigate | Verify generic invalid login and all demo role logins. |
| T-02-06-02 | I | session handling | mitigate | Verify no localStorage/sessionStorage auth token, no session cookie value in JSON, localhost-safe dev cookie, and production `__Host-` cookie prefix rules. |
| T-02-06-03 | T | mutating endpoints | mitigate | Verify cross-site Fetch Metadata rejection and final admin mutation flow. |
| T-02-06-04 | E | student relationship access | mitigate | Verify unlinked adult cannot access student data and linked roles see only allowed students. |
| T-02-06-05 | I | audit/log leakage | mitigate | Verify forbidden audit keys rejected. |
| T-02-06-06 | R/I | demo/real data confusion | mitigate | Verify `is_demo`, exact banner `Đang xem dữ liệu demo - không phải hồ sơ học sinh thật.`, and badge `Demo`. |
</threat_model>

<verification>
Run:
`cd backend; docker compose up -d postgres; python -m alembic upgrade head; $env:ALLOW_DEMO_SEED="true"; python -m app.seeds.demo_seed; python -m pytest tests -q; cd ..\frontend; npm run test -- --run; npx playwright test tests/e2e/phase2-auth-roles-admin.spec.ts`
</verification>

<success_criteria>
- Student, teacher, parent, and admin demo users can log in.
- Correct role portals are reached.
- Student profile includes school, class, name, and linked support adults.
- Admin can manage users, roles, statuses, demo-only deletion, and links.
- Security regression tests cover credential stuffing/brute force, session theft, CSRF, IDOR, audit leakage, and demo/real confusion.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-roles-links-demo-access/02-07-SUMMARY.md`
</output>
